# モルフォ — Web ゲーム化ロードマップ

> 「制御する」のではなく、「条件を整え、振る舞いを引き出す」。
> モックアップで描いた粘菌育成体験へ、既存の `@morpho/sim` を段階的に拡張する計画。

## 現在地

- **`@morpho/sim`** — 粘菌のローカル則 (グラフ成長 / Biomass膜 / Activity場 / 温度・毒素・水域を含む環境ツール) は完成済み。決定論テスト + ベンチ (`pnpm --filter @morpho/sim run bench`) で守られている。
- **`@morpho/web`** — M0〜M15 で「モックアップの遊び方」は一通り成立した: デイループ (仕込む→委ねる→受け取る)、環境ツール9種+Undo、3通貨経済とゆるいデイリー、個体の同定 (名前/★/特性チップ/追跡)、図鑑32枠と分岐する系統樹、大陸ステージと条件達成型の時代、モバイル縦UIとオンボーディング、PWA、GitHub Pages 自動デプロイ。**今すぐ遊べる状態にある。**
- **残っているもの** — モックアップとの差分は「機能の穴」ではなく **演出・記録・遊び心地の仕上げ** に集約された。全量は「[残ギャップ一覧](#残ギャップ一覧-2026-07-改訂)」参照。これらを M16〜M19 の4マイルストーンに再編成した。

## 完了済みマイルストーン (M0〜M15) — 要約

> 詳細な設計メモは各実装ファイル冒頭のコメントと git 履歴 (各マイルストーンのコミット/PR) に残っている。ここでは「何がどこにあるか」だけを引けるようにする。

| M | 一行要約 | 主なコード |
| --- | --- | --- |
| **M0 土台** | Vite + TS の `packages/web`。Canvas 描画、再生/一時停止/速度スライダー (×1〜24)、ツール5種、HUD、リセット、CI typecheck | `web/src/main.ts` `render.ts` |
| **M1 観察の快適化** | 差分再描画で 60fps、sim を Web Worker へ分離、成長タイムライン、環境ヒートマップ、ズーム/パン | `field-diff.ts` `sim-worker.ts` `game-proxy.ts` `timeline.ts` `camera.ts` |
| **M1.5 土地** | バイオーム5種をシードから手続き生成 + 地形テクスチャ | `web/src/game.ts` |
| **M2 個性** | seed から決定的な `Genome`、6軸個性 + 5タイプ判定、図鑑、個体ビュー | `sim/graph/genome.ts` `individuality.ts` `web/src/encyclopedia.ts` |
| **M3 環境の物語** | ステージ5種 (皿/洞窟/砂漠/都市跡/湿地)、環境の自然減衰 | `web/src/stages.ts` `sim/env/environment.ts` |
| **M4 ゆるい目標** | メインクエスト・デイリーチャレンジ・実績・ベスト記録 | `quests.ts` `challenges.ts` `achievements.ts` `scoreboard.ts` |
| **M5 子孫** | 種の採取 → 変異付き継承、過酷なステージほど変異幅が大きい | `sim/graph/genome.ts` `web/src/lineage.ts` |
| **M6 World View** | 複数コロニー、ミニマップ、Union-Find のネットワーク統合判定 | `colony-networks.ts` `minimap.ts` |
| **M7 仕上げ** | 環境音、アルバム (IndexedDB)、PWA、タッチ+ピンチ、GitHub Pages 自動デプロイ | `ambient.ts` `album.ts` `pinch.ts` `.github/workflows/pages.yml` |
| **M8 速度** | tick 0.5〜0.7ms への最適化、時間予算スケジューラ、早送りモード、perf HUD (`?debug`)、再現ベンチ | `sim/graph/*` `tick-scheduler.ts` `perf-hud.ts` |
| **M9 デイループ** | 「仕込む (prepare) → 委ねる (observe) → 受け取る (result)」の1日フェーズ。日境界で自動停止、結果パネルに3軸スコアの前日比Δ。既定は見守り (連続)、ヘッダのトグルで切替 | `day-loop.ts` `day-report.ts` `worker-protocol.ts` |
| **M10 環境システム** | sim に温度・毒素フィールド (既定は無害)、Genome に耐性2遺伝子。web に加温/冷却/毒素/乾燥ツールと stroke 単位 Undo (Ctrl+Z)、環境バランスHUDの実測値化 | `sim/env/environment.ts` `sim/graph/life.ts` `web/src/undo.ts` |
| **M11 経済/デイリー** | 3通貨 (🪙🍃🍄) の Wallet (詰み防止の自動回復付き)、ツールコスト、配置カウント型デイリー3件/日、チャレンジ3種の常時一覧化 | `wallet.ts` `dailies.ts` `challenges.ts` |
| **M12 個体の物語** | 通し番号 `ねばりのこ #n` + リネーム、★評価、特性チップ、育った環境チップ、カメラ追従、出来事イベント (障害物を迂回/胞子を生成)。**残: 突然変異演出・特徴文・統計グラフ・エリア注視 → M17** | `identity.ts` `trait-labels.ts` `sim/graph/growth.ts` |
| **M13 収集/記録** | 図鑑をタイプ×ステージ+特殊条件の 37 枠カタログへ (サムネ付きグリッド、♥、?スロット)、系統樹の分岐ツリー化 (任意の祖先から再開可)、実績12種のバッジグリッド。**残: タブUI・探索レポート・系統樹サムネ → M18** | `catalogue.ts` `catalogue-thumbs.ts` `lineage.ts` |
| **M14 大陸と時代** | 大陸ステージ (水域レイヤー + 拠点30の手続き生成 + 専用クエスト)、時代の条件達成型化 (円形プログレスリング)、ローカルタイム + 夜間トーン。WORLD/FIELD は計測の上で据え置き (拠点数がコストの支配要因)。**残: 残り時間予測・ミニマップ地形 → M16** | `era.ts` `daytime.ts` `stages.ts` `sim/env/environment.ts` |
| **M15 モバイルUI** | ハンバーガー☰ + 全画面ドロワー + 下部ツールバーの3層構成 (900px以下)、初回3ステップのコーチマーク。**残: 実機QA・アニメーション → M19** | `onboarding.ts` `web/index.html` `style.css` |

**速度の現状**: スライダー上限 ×24 に対し通常モードで実効 ×4〜7、早送り (⏩) で ×10 前後。残るボトルネックは sim の tick コスト (エッジ数に比例) のみ。ベースラインは `sim/scripts/bench.ts` で再現できる。

## 残ギャップ一覧 (2026-07 改訂)

モックアップ2枚との差分のうち **未完了のものだけ** を残した (✅ 済み27項目は削除。番号は旧棚卸し表のまま)。それぞれ M16〜M19 のどこで拾うかを固定した。

| # | モックアップの要素 | 現状 | 行き先 |
| --- | --- | --- | --- |
| 18 | 時代HUD「次の時代まで あと 12:34」の残り時間予測 | 進捗リングのみ (`era.ts`) | **M16** |
| 36 | ミニマップに地形 (水域・バイオーム) を描く | コロニー点と視野矩形のみ | **M16** |
| 21 | 速度コントロール ⏸ ▶ ▶▶ ▶▶▶ の4段ボタン | スライダー + ⏸ + ⏩ | **M16** |
| 37 | ズームスライダー (−/+) UI | ホイール/ピンチのみ | **M16** |
| 27 | 進化の記録「個体が突然変異 — 光走性が上昇」「新しい形質を獲得」 | 時代の節目と迂回/胞子イベントのみ | **M17** |
| 29 | 「このエリアを注視中」(視野内の出来事だけを見る) | 出来事が座標なしの文字列 | **M17** |
| 8 | 個体カードのタイプ別特徴文 (「太い幹をつくり、安定したネットワークを好む。」) | タイプ名チップのみ | **M17** |
| 33 | 個体ビューの 📊 統計グラフ | 撮影📷と♥のみ実装済み | **M17** |
| 7 | 収集・記録のタブ (図鑑 / マップ / メモ) | 別カードに散在、メモは無い | **M18** |
| 40 | 探索レポート | 未着手 | **M18** |
| 10 | 系統樹ノードのサムネイル | 分岐ツリーは済み、ノードは文字のみ | **M18** |
| — | 通貨増減の「+n」フロート演出 (M11で簡易フラッシュに簡略化) | フラッシュのみ | **M19** |
| — | ドロワーのスライドアニメーション / ボトムシート化 (M15で簡略化) | display 即時切替 | **M19** |
| — | 実機QA (iOS Safari / Android Chrome の safe-area・ピンチ・PWA) | この開発環境では検証不能 | **M19** (手順書化) |
| 17 | 4通貨目 (大陸探索専用) | 3通貨で経済は成立 | **見送り継続** (M19 で最終判定) |
| — | 昼夜の brightness を sim の成長応答に効かせる (M14で視覚のみに簡略化) | 夜間トーンのみ | **見送り継続** (毎tick全面書換が高コスト) |

## これからのマイルストーン (M16〜M19)

実施順は **M16 → M17 → M18 → M19**。並べ方の意図:

1. **M16** は sim 無改修・低リスクで「画面がモックアップに追いつく」— 遊んだときの見た目の満足度が最初に上がる。
2. **M17** は出来事の構造化という下地工事を含み、M18 の探索レポートがそれに依存する。
3. **M18** は記録系の集大成で、M17 のグラフ部品を再利用する。
4. **M19** は全機能が出揃ってから初めて意味を持つバランス調整と実機QA・リリース。

各マイルストーンは従来どおり「純粋関数 + vitest → DOM 配線 → e2e」の順で作る。規模の目安: S=半日、M=1〜2日、L=3日〜。

### M16 — 世界の実感: 時代予測・ミニマップ地形・速度/ズームUI (最初の1本・sim 無改修)

> World View モックアップの HUD を完成させる。すべて web 側の演出・UI で、sim・Worker プロトコルに触らないため回帰リスクが低い。

- [ ] **時代の残り時間予測「あと mm:ss」** — `era.ts` 拡張 (S)
  - `EraStatus.progress` を実時間軸でサンプリングした履歴 (`{ atMs, progress }` のリングバッファ、直近 ~20 点) から、EWMA した進捗速度で `(1 - progress) / rate` を推定する純粋関数 `estimateEraEta(samples: EraSample[]): number | null` を追加。速度が ε 以下・履歴不足・後退 (質量減) のときは `null` を返し、UI は「—」を出す。
  - サンプリングは `main.ts` の既存の HUD 更新ループに relay (2〜3秒に1点で十分。毎フレームは不要)。時代が切り替わったら履歴をクリア。
  - UI: `#era-ring` の下に「次の時代まで あと 12:34」ラベル (`formatMMSS` を `day-loop.ts` から共通化して流用)。
  - test: `test/era.test.ts` に「一定速度で進む履歴 → 正しい ETA」「停滞 → null」「時代切替後はリセット」を追加。
- [ ] **ミニマップ地形描画** — `minimap.ts` (現52行) にレイヤー追加 (M)
  - ステージ生成時に一度だけ、オフスクリーン canvas へ バイオーム基調色 + 水域 (`GridEnvironment.water`) + 障害物を低解像度で焼く `bakeTerrain(stage, env): HTMLCanvasElement`。以後の毎フレーム描画は「地形 (キャッシュ) → コロニー点 → 視野矩形」の3層合成にする。
  - 再焼きのトリガーはステージ切替/リセットのみ (プレイヤーが置く石・水は追従しない割り切り。地形の骨格が見えれば World View の読図には十分で、毎フレーム env を舐めるコストを避ける)。
  - e2e: `e2e/continent.spec.ts` に「大陸ステージのミニマップに水域ピクセルが存在する」検証 (canvas の getImageData で青系ピクセルを数える)。
- [ ] **速度コントロールの4段ボタン化** — `index.html` / `main.ts` (M)
  - モックアップ②の ⏸ ▶ ▶▶ ▶▶▶ を採用: ⏸=一時停止 / ▶=×1 / ▶▶=×8 / ▶▶▶=×24+早送りフラグON。既存のスライダーと ⏩ トグルは廃止し、内部 API (`setSpeed`/`setFastForward`) はそのまま使う。選択状態は `aria-pressed` で示し、`morpho.speedPreset.v1` に永続化。
  - デイループの observe フェーズ・見守りモードの両方で機能する (M9 の「速度は1日を消化する速さ」設計は不変)。
  - **e2e 追従が必須**: `smoke.spec.ts` / `mobile.spec.ts` / `day-loop.spec.ts` のうちスライダー (`#speed`) に触っているものを4段ボタンへ書き換える。ここがこのタスクの実質的な工数。
- [ ] **ズームスライダー UI** — `index.html` / `camera.ts` (S)
  - キャンバス下部に「− [スライダー] +」+「ピンチでズーム」ヒント (モックアップ②)。`Camera` の既存 zoom API に双方向バインド (ホイール/ピンチ操作でスライダー位置も追従)。900px以下ではヒント文言を出し、スライダーは細める。
- [ ] **受け入れ基準**: 大陸ステージで「時代リング + あと mm:ss + 地形入りミニマップ」が同時に見え、4段ボタンで消化速度を切り替えても既存 e2e が全て通る。`bench --smoke` 劣化なし (sim 無改修なので計測は web の frame 時間のみ、`?debug` で 3ms/frame 以内を維持)。

### M17 — 個体の物語の完成: 出来事の構造化・突然変異・特徴文・統計グラフ

> モックアップ②の「進化の記録」「このエリアを注視中」「📊」を成立させる。**中核は `recentEvents: string[]` を構造化イベントへ置き換える下地工事**で、M18 の探索レポートもこれに乗る。

- [ ] **出来事の構造化** — `web/src/world-events.ts` (新規) + `game.ts` 改修 (M)
  - `WorldEvent { id, day, tick, kind, text, x?, y? }` を定義し、`game.ts` の `recentEvents: string[]` (game.ts:170) と `eventToText()` (game.ts:288) を置き換える。sim の `SimEvent` は発生ノードを持っているので、emit 時にノード座標を引いて `x, y` を埋める (時代の節目など座標を持たない出来事は省略可のまま)。
  - 文言辞書は `event-text.ts` (新規) へ切り出し (M12 で見送った分)。高頻度イベントの間引きは現行ロジックを踏襲。
  - 保持数は 6 → 30 に拡張 (注視フィルタで絞ると表示が痩せるため)。UI 表示は従来どおり直近数件 + ローカルタイム。
  - test: `test/world-events.test.ts` — 間引き・リングバッファ・座標の有無。既存 `sim/test/events.test.ts` は無改修で通ること。
- [ ] **「このエリアを注視中」トグル** — `main.ts` / `ui.ts` (S、上の構造化に依存)
  - 出来事カードに 👁 トグルを追加。ON のとき `camera` の現在視野矩形と `WorldEvent.x/y` の交差でフィルタし、ヘッダに「このエリアを注視中」を表示。座標なしイベント (時代の節目等) は常に通す。カメラ移動でフィルタは追従 (視野は毎回計算、イベント側は不変)。
  - e2e: `e2e/identity.spec.ts` に「ズームインして注視ON → 視野外イベントが消える」を追加。
- [ ] **プレイ中の突然変異・形質獲得イベント** — `day-report.ts` + `trait-labels.ts` の派生で実装 (M)
  - **sim には手を入れない**。日境界 (M9 の result 遷移時) に前日の `DayRecord.traits` と当日を比較し、|Δ| が閾値 (0.08) を超えた軸があれば「個体が突然変異 — 探索性が上昇」を `WorldEvent` として emit する (1日最大1件、最大Δの軸を採用)。
  - 同様に `traitChipsFor()` の集合を日境界で前日と比較し、新しく出現したチップがあれば「新しい形質を獲得 — 高い耐乾性」を emit する (こちらは温度/毒素ツールの使用で環境が変わった日に自然に発生する)。
  - どちらも決定的 (traits 由来) なので `test/mutation-events.test.ts` で「Δが閾値未満なら無音」「複数軸なら最大の1軸」「チップ出現の検知」を固定できる。
- [ ] **タイプ別特徴文** — `trait-labels.ts` に `typeDescriptionFor(type): string` (S)
  - 5タイプそれぞれに1文の定型文 (例: 太くつなぐ型 →「太い幹をつくり、安定したネットワークを好む。」)。個体ビューの特性チップ直下の段落と、図鑑エントリのツールチップに表示。test はスナップショットで全タイプ分を固定。
- [ ] **統計グラフ 📊** — `web/src/chart.ts` (新規) + 個体ビューにボタン追加 (M)
  - 依存ライブラリなしの canvas 折れ線チャート。入力は `DayReport` の直近60日 (`day-report.ts` が M9 からこの用途を見越してリングバッファを持っている)。系列は 3軸スコア (探索/効率/安定) + 質量 (右軸)。描画は `buildChartLayout(records, w, h): ChartLayout` (純粋関数、目盛り・折れ線の座標列を返す) と `drawChart(ctx, layout)` に分離し、前者を vitest で固定する。
  - UI: 個体ビューの 📷 の隣に 📊 ボタン → モーダル (`.modal-overlay` 再利用)。データが2日未満なら「まだ記録が足りません」。
  - **これは M18 の探索レポートの部品になる** — 汎用の系列チャートとして書く (ラベル・色・軸数をオプション化)。
- [ ] **依存**: M9 (`DayRecord`)、M12 (特性チップ)。sim・Worker プロトコル無改修。
- [ ] **受け入れ基準**: 数日回すと「進化の記録」に突然変異/形質獲得が (環境を動かした日に) 現れる。ズームイン + 注視ONで足元の出来事だけが流れる。個体ビューに特徴文の段落と 📊 の推移グラフが出る。既存 e2e 全通過。

### M18 — 収集・記録ハブ: タブUI・メモ・探索レポート・系統樹サムネ

> モックアップ①の「収集・記録 (図鑑/マップ/メモ)」タブと②の「探索レポート」。散らばった記録系カードを1つの「ふりかえる場所」に束ねる。

- [ ] **「収集・記録」タブ付きパネル** — `index.html` / `ui.ts` / `style.css` (M)
  - 図鑑 (カタロググリッド) / マップ (ミニマップの拡大版 + M16 の地形) / メモ の3タブを持つ1カードに統合。既存の図鑑カード・ミニマップカードの中身 (DOM) をタブコンテンツへ移設する — 描画・更新ロジック (`catalogue.ts` / `minimap.ts`) は無改修で、`ui.ts` の配置だけを変えるのが狙い。タブ選択は `morpho.recordTab.v1` に永続化。
  - モバイル (900px以下) ではドロワー内で同じタブがそのまま使える (M15 の複製なし方針を踏襲)。
  - **e2e 追従**: `catalogue.spec.ts` / `mobile.spec.ts` の「図鑑を開く」導線がタブ経由に変わる。
- [ ] **メモ** — `web/src/notes.ts` (新規) (S)
  - 日付 (Day n) 付きの自由テキストメモ。追加/削除のみ (編集なしの割り切り)。「今日の成長を貼る」ボタンで当日の `DayRecord` サマリ (3軸+質量) を定型文として挿入できる。localStorage `morpho.notes.v1`、直近100件。`test/notes.test.ts`。
- [ ] **時代の年表の記録** — `main.ts` の時代切替検知 (`lastEraName`) に1行追加 (S)
  - `morpho.eraHistory.v1` に `{ era, day }` を追記する。探索レポートの年表と、将来のバランス調整 (M19: 何日で成熟期に届くか) の実測データになる。**M18 の他項目より先・単独でマージしてよい** (記録は早く始めるほど価値がある)。
- [ ] **探索レポート** — `web/src/report.ts` (新規) + モーダル (M)
  - 構成 (モックアップ②の 📊レポートボタン): ① 総質量・総面積の推移チャート (M17 の `chart.ts` を再利用) ② 時代の年表 (`eraHistory` から「Day 12 拡散期へ」) ③ 数値サマリ (拠点接続 n/40、探索率、発見種数 n/37、実績 n/12) ④「画像で保存」— キャンバスにレポートを合成して `toBlob` → アルバム (M7 の経路) へ保存。
  - ヘッダの 📈 ボタンから開く。`buildReport(sources): ReportData` を純粋関数にして vitest でカバー、canvas 合成は e2e でスモークのみ。
- [ ] **系統樹ノードのサムネイル** — `lineage.ts` + `catalogue-thumbs.ts` (S)
  - 種の採取時に `renderThumbnail()` を撮り、`catalogue-thumbs.ts` と同じ IndexedDB (store を分ける) に `lineageId → blob` で保存。系統樹ノード (`.lineage-node`) に 40px サムネを表示、無いノード (過去データ) は現行の文字表示のまま。マイグレーション不要 (画像が無いだけで壊れない)。
- [ ] **依存**: M16 (ミニマップ地形をマップタブで再利用)、M17 (`chart.ts`、構造化イベント)。
- [ ] **受け入れ基準**: 「収集・記録」1カードから図鑑/マップ/メモを行き来できる。探索レポートに推移グラフと年表が出て、PNG がアルバムに残る。新しく採種した子から系統樹にサムネが付く。`e2e/report.spec.ts` (新規) + 既存 e2e 全通過。

### M19 — 遊び心地の総仕上げ: セッション設計・演出・実機QA・リリース

> 機能が出揃ってから、「ゲームとして毎日さわって気持ちいいか」を1本のマイルストーンとして扱う。ここで初めてバランス数値に触る。

- [ ] **セッション設計 (バランス調整パス)** — 数値だけの変更、コード構造は不変 (M)
  - 目標体験を先に固定する: 「**1セッション = デイループ2〜3日ぶん = 実時間10〜15分**。初日のセッションで拡散期に届き、3〜4セッションで成熟期・初採種まで行ける」。
  - 実測 → 調整対象: `TICKS_PER_DAY` と速度プリセットの組合せ (1日の消化が ×8 で90秒前後になるか)、`era.ts` の昇格閾値、`wallet.ts` の日次報酬とツールコスト (1日あたりツール8〜12回使える残高感)、デイリーの要求数。
  - 計測は手プレイ + `eraHistory` (M18) の実データで行い、変更値は必ず該当 vitest の期待値も更新する。**"やらなくても大丈夫" の哲学 (詰み防止・未達ペナルティなし) は制約として維持。**
- [ ] **デイループを既定モードへ** — M9 で e2e 都合により見送った決定の再挑戦 (S)
  - オンボーディング完了直後の初回のみ「デイループで遊ぶ? / 見守りで眺める?」の2択モーダルを出し、選択を既存の `morpho.dayLoopMode.v1` に保存する (既存ユーザーは現在の選択を維持)。既存 e2e は `addInitScript` で `dayLoopMode` を見守りに固定して無改修で通す (M15 のオンボーディング既読化と同じ手口)。
- [ ] **演出の残り2件** (S×2)
  - 通貨の「+n」フロート表示: 増減時に残高チップから小さな `+12 🪙` が浮いて消える (CSS アニメーション1つ、`prefers-reduced-motion` では無効)。M11 の簡易フラッシュは維持した上での追加。
  - ドロワー/結果パネルのスライドイン: `display` 切替 → `transform` アニメーションへ。M15 で踏んだ「transform が position:fixed の containing block を変える」問題は、**アニメーション対象を内側のコンテンツ要素に限定し、オーバーレイ自体は display のまま**にして回避する。
- [ ] **実機QAの手順書化と実施** — `docs/QA.md` (新規) (S + ユーザー実施)
  - この開発環境 (ヘッドレス Chromium) では実施不能な項目をチェックリスト化する: iOS Safari / Android Chrome での safe-area (ノッチと下部ツールバーの重なり)、ドロワーとピンチ操作の競合、PWA ホーム追加とスタンドアロン起動、オフライン起動、`?debug` で 3ms/frame 以内、音声の自動再生制限。
  - 発見された問題はこのマイルストーン内で修正する (バッファとして L 相当を確保)。
- [ ] **4通貨目の最終判定** — 大陸ステージを M19 のバランスパスで遊び込んだ結果、3通貨で経済の緩急が足りているなら**正式に廃案**にし、残ギャップ一覧から消す。足りない場合のみ「大陸ステージ専用の獲得源+使い道1つ」の最小構成で追加する (判断基準: 大陸で🪙が常時カンストする/しない)。
- [ ] **リリース** — README に遊び方 (3ステップのコンセプト + スクリーンショット) と Pages URL を載せ、`CHANGELOG.md` を起こして v1.0 タグを打つ。以降の変更は CHANGELOG 運用に乗せる。
- [ ] **依存**: M16〜M18 全部 (バランスと演出は全機能の上でしか意味を持たない)。
- [ ] **受け入れ基準**: 新規プレイヤー (localStorage 空) が、オンボーディング → デイループ選択 → 初日で拡散期到達 → 「今日の成長」で🪙獲得、まで**誰の説明もなしに15分で到達**できる。実機QAチェックリストが全項目 ✅ または既知の制約として文書化されている。v1.0 が Pages で配信されている。

## アーキテクチャ方針

- **sim はステートレスに保つ** — UI 側は SimState のスナップショットを読むだけ。書き込みは `Environment.placeX()` 経由に限定する。M16〜M19 は **sim・Worker プロトコルに一切手を入れない** (M17 の突然変異イベントも DayRecord からの web 側派生で実装する)。
- **描画はラッパー** — `@morpho/web` は sim を import するだけで、sim 側に描画依存を持ち込まない。
- **時間スケールは UI に切り出す** — sim は「1 tick」しか知らない。日数・時代・デイループ・ローカルタイム・時代ETA (M16) はすべて web 側の派生量。
- **ゲームデータは web 側 + マイグレーション必須** — 永続化は localStorage (画像のみ IndexedDB)。スキーマを変えるときは必ず `vN → vN+1` のマイグレーションを書く。M17 の出来事構造化は表示用の揮発データなのでマイグレーション不要、M18 のメモ/年表は新規キー。
- **ゲームロジックは純粋関数 + vitest** — M16〜M18 の新ロジック (era ETA / world-events / mutation-events / chart layout / notes / report) も DOM と Worker から切り離した純粋モジュールとしてテストしてから配線する。
- **e2e を壊す変更は同じ PR で追従する** — M16 の速度4段化と M18 のタブ統合は既存 e2e の導線を変える。導線を変えるコミットと e2e 修正は分離しない。
- **リポジトリルートは pnpm workspaces** — install は必ずリポジトリルートで行うこと (`@morpho/web` は `../sim/src` を直接 import している)。
- **性能は計測してから** — 描画に触る変更 (M16 のミニマップ/チャート) は `?debug` の perf HUD で 3ms/frame 以内を確認する。sim ベンチは M16〜M19 では変化しないはず (変化したら設計違反のシグナル)。

## 動かし方

```sh
pnpm install      # リポジトリルートで一度だけ (workspaces が両パッケージの依存を解決する)
cd packages/web
pnpm dev
# → http://localhost:5173 で遊べる
```

デプロイ版: main へのマージで GitHub Pages へ自動デプロイされる (`.github/workflows/pages.yml`)。
