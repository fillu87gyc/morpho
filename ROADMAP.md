# モルフォ — Web ゲーム化ロードマップ

> 「制御する」のではなく、「条件を整え、振る舞いを引き出す」。
> モックアップで描いた粘菌育成体験へ、既存の `@morpho/sim` を段階的に拡張する計画。

## 現在地

- **`@morpho/sim`** — 粘菌のローカル則 (グラフ成長 / Biomass膜 / Activity場 / 環境ツール) は完成済み。決定論 + ベンチ (`pnpm --filter @morpho/sim run bench`) で守られている。
- **`@morpho/web`** — sim を Worker で回し `<canvas>` に描く「観察サンドボックス」としては完成 (M0〜M8 完了)。HUD・ツール・図鑑・系統樹・アルバム・環境音・PWA・GitHub Pages デプロイまで揃っている。M9 でデイループ、M10 で温度・毒素・水止め・Undo、M11 で通貨経済とゆるいデイリー/チャレンジ一覧化、M12 で個体の同定 (名前/★/特性チップ/追跡カメラ)、M13 で図鑑の32枠カタログ化・系統樹の分岐・実績バッジ化、M14 で大陸ステージ (水域レイヤー・拠点30の手続き生成・専用クエスト)・時代の条件達成型HUD・ローカルタイム/昼夜、M15 でモバイル縦画面のドロワー/下部ツールバー・初回オンボーディングを追加した (M12・M13・M14・M15 は一部未着手項目あり、各節を参照)。
- **モックアップの「ゲーム」への残りギャップ**: 主要な機能面のギャップはM9〜M15で概ね埋まった。残るのは主に演出の作り込み (時代の残り時間予測、ミニマップ地形描画、統計グラフ、探索レポート等) と、実機 (iOS Safari/Android Chrome) でのモバイルQA — いずれもこの開発環境では検証できないか、優先度を下げて見送ったもの。ギャップの全量は下の「[ギャップ棚卸し](#モックアップとのギャップ棚卸し-2026-07)」を参照。

## 完了済みマイルストーン (M0〜M13) — 要約

> 詳細な設計メモは各実装ファイル冒頭のコメントと git 履歴に残っている。ここでは「何がどこにあるか」だけを引けるようにする。

| M | 一行要約 | 主なコード |
| --- | --- | --- |
| **M0 土台** | Vite + TS の `packages/web`。Canvas 描画、再生/一時停止/速度スライダー (×1〜24)、ツール5種 (エサ/光/水/石/削除)、HUD、リセット、CI typecheck | `web/src/main.ts` `render.ts` |
| **M1 観察の快適化** | 差分再描画で 60fps、sim を Web Worker へ分離、Day 1/5/10… の成長タイムライン、環境ヒートマップ ON/OFF、ズーム/パン | `field-diff.ts` `sim-worker.ts` `game-proxy.ts` `timeline.ts` `camera.ts` |
| **M1.5 土地** | バイオーム (岩場/砂地/草地/水場/陽だまり) をシードから手続き生成 + 常時の地形テクスチャ | `web/src/game.ts` (placeX のみ使用、sim 無改修) |
| **M2 個性** | seed から決定的な `Genome`、6軸の個性測定 + 5タイプ判定、図鑑 (localStorage)、個体ビュー 6軸バー | `sim/graph/genome.ts` `individuality.ts` `web/src/encyclopedia.ts` |
| **M3 環境の物語** | ステージ5種 (皿/洞窟/砂漠/都市跡/湿地)、時代 (胞子期→拡散期→変形体期→成熟期、DAY 由来)、環境の自然減衰 (エサ消費/乾燥) | `web/src/stages.ts` `game.ts eraName()` `sim/env/environment.ts decay()` |
| **M4 ゆるい目標** | メインクエスト2種 (拠点接続/70%探索)、デイリーチャレンジ (日付ハッシュで3種から1つ)、実績7種、ベスト記録の自動保存 | `quests.ts` `challenges.ts` `achievements.ts` `scoreboard.ts` |
| **M5 子孫** | 種の採取 → 変異付き継承 (`createChildGenome`)、系統樹カード (線形リスト)、過酷なステージほど変異幅が大きい | `sim/graph/genome.ts` `web/src/lineage.ts` |
| **M6 World View** | 3コロニー配置、ミニマップ (クリックでズームイン)、Union-Find のネットワーク統合判定、「離れたコロニーをひとつに」クエスト | `colony-networks.ts` `minimap.ts` `camera.focusOn()` |
| **M7 仕上げ** | WebAudio 手続き生成の環境音、アルバム (IndexedDB + toBlob)、PWA (オフライン/ホーム追加)、タッチ + ピンチズーム、GitHub Pages 自動デプロイ | `ambient.ts` `album.ts` `pinch.ts` `vite.config.ts` `.github/workflows/pages.yml` |
| **M8 速度** | perf HUD (`?debug`) + 再現ベンチ + CI スモーク。crowding のグリッド化 / マルチソース BFS / index キャッシュ / diffuse 間引き / line-stamp で 1 tick 0.5〜0.7ms。typed array transfer で clone ゼロ化、時間予算スケジューラ、描画バッチで 0.8〜1.5ms/frame、早送りモード (⏩) | `sim/graph/*` `tick-scheduler.ts` `snapshot-codec.ts` `perf-hud.ts` |
| **M9 デイループ** | 「仕込む→委ねる→受け取る」の1日単位フェーズ (`prepare/observe/result`)。Worker の `runUntilTick()` で日境界に自動停止、結果パネルに3軸スコアの前日比 Δ とサムネイル。既定は見守り (連続) のまま、ヘッダのトグルでデイループへ切替 | `day-loop.ts` `day-report.ts` `worker-protocol.ts` `sim-worker.ts` |
| **M10 環境システム** | sim に温度・毒素フィールドを追加 (既定は無害、ツール使用時のみ影響)。温度は最適カーブで activity 回復/疲労に、毒素は activity 減衰+成長候補ペナルティに効く (通過は可能)。Genome に耐性2遺伝子。web は温度/冷却/毒素/水止めツールとUndo (stroke単位) を追加、環境バランスHUDを実測値化 | `sim/env/environment.ts` `sim/graph/life.ts` `sim/graph/genome.ts` `web/src/undo.ts` |
| **M11 経済/デイリー** | 3通貨 (🪙🍃🍄) の Wallet (詰み防止の自動回復付き)、ツール使用コスト+HUD、配置カウント型のゆるいデイリー3件/日、チャレンジを「1日1種」から「3種常時・任意挑戦」へ (localStorage v1→v2移行) | `web/src/wallet.ts` `web/src/dailies.ts` `web/src/challenges.ts` |
| **M12 個体の物語 (部分)** | 通し番号 (`ねばりのこ #n`) + リネーム、★評価、特性チップ (Genome閾値+M10耐性遺伝子)、育った環境チップ、カメラ追従 (`ColonyMarker.centroid` + `panToward()`)、出来事イベント追加 (ObstacleAvoided/SporeFormed)。突然変異演出・統計グラフ・エリア注視は未着手 | `web/src/identity.ts` `web/src/trait-labels.ts` `sim/graph/growth.ts` |
| **M13 収集/記録 (部分)** | 図鑑を32→37枠のカタログへ (タイプ×ステージ+特殊条件7種、M14の大陸ステージ追加で37に拡張)、サムネイル付きグリッド (IndexedDB)、お気に入り、系統樹の分岐ツリー化 (任意の祖先から再開可)、実績12種のバッジグリッド。タブUI・探索レポートは未着手 | `web/src/catalogue.ts` `web/src/catalogue-thumbs.ts` `web/src/lineage.ts` |
| **M14 大陸と時代 (部分)** | 事前計測 (`bench.ts --worldscale`) で FIELD 256・コロニー40でも実効×6を確認してから着手。時代を条件達成型へ (`era.ts`、円形プログレスリング付きHUD)。ローカルタイム`hh:mm:ss`+夜間トーン (`daytime.ts`、洞窟は無効)。大陸ステージ (`stages.ts` に追加、既存5ステージは無改修): 水域 `GridEnvironment.water` レイヤー (obstacle同居で通行不能+周囲の湿度供給)、source6/食料拠点24をポアソンディスク風に手続き生成 (`StageConfig.worldPoints()`)、専用クエスト「大陸全体に栄養を届けよう」。ミニマップ地形描画・ズームスライダー・4段速度ボタンは見送り (各節参照) | `web/src/era.ts` `web/src/daytime.ts` `web/src/stages.ts` `sim/env/environment.ts` |
| **M15 モバイルUI/オンボーディング (部分)** | ハンバーガー ☰ + 全画面ドロワー (`aside.panel.left/right`) + 下部ツールバー (`#mobile-toolbar`、既存ツールボタンの複製で JS 変更不要) の3層構成へ刷新。初回3ステップのコーチマーク (`onboarding.ts`、`morpho.onboarded.v1`)。実機QA (iOS Safari/Android Chrome) はこの開発環境では検証不能なため見送り | `web/src/onboarding.ts` `web/index.html` `web/src/style.css` |

**速度の現状**: スライダー上限 ×24 に対し通常モードで実効 ×4〜7、早送り (⏩) で ×10 前後。残るボトルネックは sim の tick コスト (エッジ数に比例) のみ。ベースライン計測値は `sim/scripts/bench.ts` で再現できる。

## モックアップとのギャップ棚卸し (2026-07)

モックアップ2枚 (モバイル版ポスター / World View 画面) を要素単位で分解し、現状と突き合わせた全量リスト。**✅ = 実装済み / 🟡 = 部分的 / ❌ = 未着手**。

### モックアップ①: モバイル版「モルフォ 〜委ねて、育ついのち〜」

| # | モックアップの要素 | 現状 | 対応 |
| --- | --- | --- | --- |
| 1 | 3画面ループ「1. 環境を整える → 2. 粘菌が育つ (オート、残り時間 02:34) → 3. 結果を観察する (今日の成長 Day 6)」 | ✅ デイループ (`day-loop.ts`、M9) で「仕込む→観察をはじめる▶→結果→つぎの日へ」が1周する。既定は見守り(連続)のままヘッダのトグルで切替 (デフォルトをデイループにする案は smoke/mobile の既存e2eを壊すため見送った) | — |
| 2 | 結果画面: 3軸スコア (探索/効率/安定) の当日値 + 前日比 Δ 表示 | ✅ `day-report.ts` (M9) が直近60日をリングバッファ保持し、結果パネルに前日比Δを表示する | — |
| 3 | 通貨 HUD (🪙120 / 🍃85 / 🍄3) — ツール使用と報酬の経済 | ✅ (M11) | — |
| 4 | ゆるいデイリー: 「エサを3つ置く (3/3)」「光を1回あてる (1/1)」「水をあたえる (0/1)」のチェックリスト。"やらなくても大丈夫" | ✅ (M11) | — |
| 5 | チャレンジ (観察の遊び) 3種が同時に一覧表示され、任意に挑める | ✅ (M11) | — |
| 6 | 図鑑: 「発見した粘菌 12/32」— サムネイル付きグリッド、★評価、未発見スロット | ✅ (M13、タイプ×ステージ+特殊条件で32種) | — |
| 7 | 収集・記録のタブ (図鑑 / マップ / メモ) | 🟡 図鑑とミニマップは別カードに散在。メモは存在しない。タブ統合は見送った | **M13 (未着手のまま)** |
| 8 | 個体カード: 「ねばりのこ #12」(名前+通し番号)、★★★★、タイプバッジ「太くつなぐタイプ」、特徴文、「育った環境」チップ (暗い/障害物が多い/エサが少ない) | 🟡 番号・★・タイプ・環境チップは実装済み (M12)。タイプ別の特徴文段落は未実装 | **M12** |
| 9 | 「この子の子孫を残す」ボタン | ✅ 「種を採取」(Day 5 以降) | — |
| 10 | 子孫ツリー: 1代目→2代目→3代目が**分岐** (1親から複数の子)、各ノードにサムネ | 🟡 分岐は実装済み (M13)。系統樹ノードへのサムネ表示は見送った (図鑑グリッドのサムネのみ) | **M13** |
| 11 | 「環境を変えると、子孫の個性も変わります」 | ✅ `mutationScaleFor()` (M5) | — |
| 12 | いろいろな環境 (洞窟/砂漠/都市跡) | ✅ +湿地 (M3) | — |
| 13 | 癒しの演出 (のんびりした音楽 + 眺めているだけで楽しい) | ✅ 環境音 (M7) | — |
| 14 | スマホ縦画面レイアウト (下部ツールバー、最小 HUD、ハンバーガーメニュー) | ✅ ハンバーガー☰ + 全画面ドロワー + 下部ツールバーの3層構成 (M15)。ボトムシート化・スライドアニメーションは見送った (即時 display 切り替えの簡易版) | — |
| 15 | コンセプトの3ステップ (環境を整える→委ねる→変化を観察する) の提示 = 初回オンボーディング | ✅ `onboarding.ts` (M15)。実操作連動ではなく「次へ」で進む3枚のコーチマークに簡略化 | — |

### モックアップ②: World View 画面

| # | モックアップの要素 | 現状 | 対応 |
| --- | --- | --- | --- |
| 16 | 大陸マップ: 水域・海岸線のある大きな世界、拠点 27/40、接続ネットワーク 163 | ✅ 大陸ステージ追加 (M14)。96×96 の皿はそのまま (WORLD/FIELD 拡大は高リスクなため見送り、下の M14 節参照)、水域レイヤー + source6/食料拠点24 = 拠点30をポアソンディスク風生成 | — |
| 17 | 4通貨 HUD (7.21k / 3.48k / 1.02k / 870) | 🟡 3通貨 (M11) のまま。M14 で大陸ステージを実装したが、4種目通貨追加は見送った (3通貨のままでも経済ループは成立するため優先度を下げた) | **見送り** |
| 18 | 時代 HUD: 「時代: 胞子期 DAY 46」+ 円形プログレス「次の時代まで あと 12:34」 | 🟡 条件達成型の時代 + 円形プログレスリングは実装 (M14, `era.ts`)。「残り時間 mm:ss」の線形予測は見送った (進捗率のみ表示) | **M14 (残り時間予測のみ未着手)** |
| 19 | 世界の目標: 「時代を『変形体期』に進める」= 時代が条件達成型の目標 | ✅ `era.ts` の条件判定 (拠点接続数/総質量/ネットワーク統合/探索率) で時代が進むようになった (M14) | — |
| 20 | メインクエスト「大陸全体に栄養を届けよう 68%」 | ✅ 大陸ステージ選択時のみ表示 (M14, `continent-nutrient` クエスト。陸地セルのうち biomass が届いたセルの比率) | — |
| 21 | 速度コントロール: ⏸ ▶ ▶▶ ▶▶▶ の4段ボタン | 🟡 スライダー + ⏸ + ⏩ のまま。4段ボタン化は見送った (機能的には同等) | **見送り** |
| 22 | 環境バランス 5軸 (光/温度/湿度/栄養/毒素) | ✅ 実フィールド平均値 (M10) | — |
| 23 | ツール「温度を変える」 | ✅ 加温/冷却 (M10) | — |
| 24 | ツール「毒素をまく」 | ✅ (M10) | — |
| 25 | ツール「水を引く / 止める」 | ✅ 乾燥ツール追加 (M10) | — |
| 26 | 「やり直す」(Undo) ボタン | ✅ stroke単位・深さ10・Ctrl+Z (M10) | — |
| 27 | 進化の記録: 「新しい形質を獲得」「個体が突然変異 — 光走性が上昇」 | 🟡 時代の節目に加え「障害物を迂回」「胞子を生成」も記録されるように (M12)。プレイ中の突然変異・形質獲得イベントは未実装 | **M12** |
| 28 | 最近の出来事: 「栄養を発見」「障害物を迂回」「分岐を強化」「胞子を生成」+ 時刻 | ✅ 「障害物を迂回」「胞子を生成」イベント (M12) + ローカルタイム表示 (M14, `#local-time`) が揃った | — |
| 29 | 「このエリアを注視中」(選択エリアのイベントだけを見る) | ❌ 出来事の構造化 (座標付き) が前提になるため、M13 以降の図鑑/系統樹再設計と合わせて着手予定 | **M13+** |
| 30 | 個体ビュー: 「ねばりのこ #25」+ ★☆ 評価 | ✅ (M12) | — |
| 31 | 個体ビューの「特性」チップ (光を避ける / 乾燥にやや強い / 迷路構造が得意) | ✅ (M12) | — |
| 32 | 「個体を追跡する」(カメラ追従) | ✅ ミニマップで対象選択→トグルで追従、手動操作で解除 (M12) | — |
| 33 | 個体ビューの 📷 / 📊 / ♥ ボタン (撮影 / 統計グラフ / お気に入り) | 🟡 撮影(アルバム)とお気に入り(図鑑エントリの♥、M13)は実装済み。統計の時系列グラフは未着手 | **M13 (残)** |
| 34 | ローカルタイム「12:18:47」+ 昼夜 | ✅ `daytime.ts` (M14)。1日を24時間にマップした時計 + 夜間トーン (洞窟は無効) | — |
| 35 | 成長の記録タイムライン (Day 1/5/10/20/30/40/現在) | ✅ (M1) | — |
| 36 | ミニマップ (地形付き) | 🟡 コロニー点と視野矩形のみ。地形・水域の描画は見送った (大陸ステージの水域は本編キャンバスでは見えるが、ミニマップには焼いていない) | **見送り** |
| 37 | ズームスライダー (−/+) + 「ピンチでズーム」ヒント | 🟡 ホイール/ピンチはあるがスライダー UI がない。見送った (機能的には同等) | **見送り** |
| 38 | 実績: バッジアイコン + 未解除の「?」ロックスロット | ✅ (M13、12種) | — |
| 39 | 図鑑「発見した要素 142/286」(粘菌タイプ以外も含む要素図鑑) | 🟡 32種のカタログへ拡張済み (M13)。「粘菌タイプ以外の要素」を含む図鑑ではない | — |
| 40 | 探索レポート ボタン | ❌ 見送った | **M13 (未着手のまま)** |

## これからのマイルストーン

実施順は **M9 → M10 → M11 → M12 → M13 → M14 → M15**。M9・M10・M11 は完了し「モックアップの遊び方」が最小構成で成立した。M12〜M15 は主要項目を実装済みだが、いくつかの演出・UI細部・実機QA (突然変異・統計グラフ・タブUI・探索レポート・エリア注視・ミニマップ地形描画・ズームスライダー・4段速度ボタン・4通貨目・時代の残り時間予測・実機での safe-area/ピンチ確認) は未着手のまま残した (各節に明記)。

### M9 — デイループ: 「仕込む → 委ねる → 受け取る」 (最優先・これで"ゲーム"になる) ✅ 完了

> モックアップ①の3画面フローそのもの。今の「流しっぱなしサンドボックス」に**1日単位の区切り**を導入する。

- [x] **フェーズのステートマシン** — `web/src/day-loop.ts` (新規)
  - `phase: 'prepare' | 'observe' | 'result'` を持つ純粋なステートマシンとして実装 (DOM/Worker 非依存、`tick-scheduler.ts` と同じ流儀)。`vitest` (`test/day-loop.test.ts`) で遷移を網羅テスト。
  - `prepare`: sim は一時停止 (速度/一時停止/早送りの操作も無効化する)。ツール配置だけができる。「観察をはじめる ▶」で `observe` へ。
  - `observe`: 1日ぶん (`TICKS_PER_DAY = 40` tick) を自動走行。**日境界の tick に到達したら自動停止**して `result` へ。走行中も従来どおりツール介入は可能。
  - `result`: sim 停止のまま「今日の成長」パネルを表示。「つぎの日へ」で `prepare` に戻る。
- [x] **Worker プロトコル拡張** — `worker-protocol.ts` に `runUntilTick(target: number | null)` 命令を追加
  - `sim-worker.ts` は `TickScheduler` の予算内で target まで (超えないよう steps を切り詰めて) 回し、到達したら自動 pause + `dayCompleted` 通知を返す。`target: null` で日境界キャップを解除する (見守りへの切替用)。速度スライダー/早送り (M8) はそのまま「1日を消化する速さ」として機能する。
  - 到達判定は Worker 側で行う (メイン側の RAF ポーリングだと速度×24 時に日境界を大きく飛び越すため)。`TickScheduler.reset()` を追加し、日開始時に前日の余り debt を持ち越さないようにした。
- [x] **観察中の残り時間表示** — モックアップの `02:34`
  - `(targetTick - state.tick) / 実効tick毎秒` から残り実時間を推定して mm:ss 表示 (`formatMMSS`)。実効速度は既に `PerfInfo` として Worker から届いているのでそのまま流用する。
- [x] **「今日の成長」結果パネル** — `web/src/day-report.ts` (新規) + モーダル UI (`#day-result-modal`)
  - 表示: `Day n` / 3軸スコア (探索・効率・安定) の当日値と**前日比 Δ** (▲▼ 付き) / その日のサムネイル (`renderer.renderThumbnail()` を日境界で撮影) / その日に起きた主な出来事 (進化の記録から抜粋、なければ「静かな一日だった…」)。
  - 日次記録 `DayRecord { day, traits, massKg, areaM2 }` を web 側でリングバッファ保持 (直近 60 日、localStorage `morpho.dayRecords.v1`)。`test/day-report.test.ts` でカバー。これは M12 の統計グラフ、M14 の時代進捗予測の下地にもなる。
  - パネル内ボタン: 「📷 アルバムへ」(既存の撮影と同じ経路) / 「つぎの日へ」。
- [x] **モード切替** — ヘッダに「デイループ / 見守り (連続)」トグル (`#day-loop-mode-toggle`)
  - 既存の連続再生も残す。**設計変更**: ロードマップ原案は「デフォルトはデイループ」だったが、それだと `prepare` フェーズで sim が一時停止するため、ボタン操作なしで DAY が自動進行することを前提にした既存の e2e (`smoke.spec.ts` / `mobile.spec.ts`) が軒並み壊れる。「既存 e2e を壊さない」を優先し、**デフォルトは見守り (連続)** のままとし、デイループは明示的にトグルする第2のモードとして実装した (選択は `localStorage` の `morpho.dayLoopMode.v1` に永続化)。
- [x] **e2e**: `e2e/day-loop.spec.ts` — 「トグルで切替 → 観察をはじめる → 結果パネル → つぎの日へ」を1周するテストと、「見守りに戻すと自動進行を再開する」テストを追加。
- **受け入れ基準**: 3クリック (トグル→開始→結果を閉じてつぎの日へ) で1日が回る。結果パネルに前日比が出る。早送り ⏩ / 速度スライダーで1日の消化速度を制御できる。リロードすると `prepare` から再開する (フェーズ自体は永続化しない。モード選択のみ永続化)。

### M10 — 環境システムを本物にする: 温度・毒素・水止め・やり直す ✅ 完了

> HUD の「温度」「毒素」は現在**派生表示の偽物** (`game.ts computeBalance()` 参照)。sim に実体を持たせ、モックアップ②のツール6種 + Undo を揃える。**sim 本体に手を入れる唯一のマイルストーン**なので、決定論テストとベンチを必ず更新する。

- [x] **sim: 温度・毒素フィールドの追加** — `sim/env/environment.ts`
  - `GridEnvironment` に `temperature: FieldGrid` (初期値 `baseTemperature`、既定 0.5) と `toxin: FieldGrid` (初期値 0) を追加。`GrowthContext` に `temperature` / `toxin` を追加。
  - `placeHeat(pos, radius, delta)` (負の delta で冷却) / `placeToxin(pos, radius, amount)` / `placeDrain(pos, radius, amount)` (`placeWater` の負量版) を追加。
  - `decay()` を拡張 (`tempRelaxRate` / `toxinDecayRate` を追加引数に): 温度は `baseTemperature` へ緩和、毒素はゆっくり 0 へ分解。省略時は従来通り変化しない (後方互換)。速度はステージ定義 (`stages.ts` の `tempRelaxPerTick`/`toxinDecayPerTick`) に持たせた。
- [x] **sim: 成長応答** — `sim/graph/life.ts` / `growth.ts` / `params.ts`
  - 温度: 最適温度カーブ (`tempOptimal`/`tempTolerance`, 既定 0.5/0.35)。`tempSuitability()` が最適から離れるほど activity の目標値への追従 (回復) を落とし、`tempOptimal+tempTolerance` を超えた高温側でのみ `fatigueGrow` の実効値が増す。
  - 毒素: `toxinPenalty` (既定 0.6) が activity の目標値を直接削り、`growth.ts` の候補スコアにもペナルティとして乗る。obstacle と違い reject はしない (通過は可能)。
  - `Genome` に `heatTolerance` / `toxinResistance` を追加し、`createGenome`/`createChildGenome` の変異対象・`applyGenome` の `tempTolerance`/`toxinPenalty` 補正に反映した。
  - **既定値は無害**: `baseTemperature` を全ステージ `tempOptimal` と同じ 0.5 に揃え、`toxin` は初期 0 なので、プレイヤーがツールを使わない限り既存の成長速度・タイミング (Day 5 種採取、Day 1 タイムライン等の e2e) に影響しない設計にした。
- [x] **sim: テスト** — `determinism.test.ts` に温度/毒素を置いた場合の決定論ケース、および「低温域で成長が控えめになる」「毒素の中は activity が低く保たれる」を追加。`environment.test.ts` に `placeHeat`/`placeToxin`/`placeDrain`/`decay` 拡張のテストを追加。`bench --smoke` で 0.436ms/tick (既存ベースライン 0.5〜0.7ms 以内) を確認。
- [x] **web: ツール拡張** — `game.ts` の `Tool` union に `'heat' | 'cool' | 'toxin' | 'drain'` を追加
  - パレット (index.html): 🔥/❄ や 💧/🏜 のサブトグルではなく、8種を独立ボタンとして並べる簡略構成にした (エサ/光/水/乾燥/石/加温/冷却/毒素+消す)。機能は同一で、実装をシンプルに保つための設計判断。
  - 環境ヒートマップ表示 (M1) に温度レイヤー (暑い=赤橙/寒い=藍青、ヒートON時のみ) を追加。毒素は obstacle と同様に常時薄紫で見えるようにした (避けたいものは常に見える方が良いという判断)。
- [x] **web: 環境バランス HUD を実測値へ** — `computeBalance()` の温度=光派生・毒素=障害物派生をやめ、実フィールド平均に置き換えた。
- [x] **web: やり直す (Undo)** — `web/src/undo.ts` (新規)
  - 環境フィールドへのスタンプだけを stroke 単位 (pointerdown〜up) で取り消す (sim の時間は巻き戻さない)。`Game.beginStroke()`/`apply()` 内の `recordBefore()`/`endStroke()`/`undoStroke()` で構成し、Worker プロトコルに `beginStroke`/`endStroke`/`undoStroke` を追加。深さ10 stroke。
  - UI: ツールパレット末尾に「↩ やり直す」ボタン + Ctrl+Z (Mac は Cmd+Z にも反応)。
  - `vitest` (`test/undo.test.ts`): 1 stroke 内の重なった複数スタンプでも逆順再生で正しく巻き戻ることをテスト。
- [x] **e2e**: `e2e/environment-tools.spec.ts` — 毒素配置での HUD 反映、Ctrl+Z での取り消し、加温/冷却+ヒート表示、水を止めるツールを検証。
- **受け入れ基準**: 毒素をまくと環境バランスの毒素表示が上がる (網が痩せて迂回する挙動は `determinism.test.ts` のsim単体テストで確認)。冷却した領域は sim レベルで成長 (エッジ数) が控えめになることを確認済み。「水を止める」で湿度が下がる。石を置き間違えても ↩ で消せる。`bench --smoke` の tick コストは劣化なし (0.436ms/tick)。

### M11 — リソース経済と「ゆるいデイリー」 ✅ 完了

> モックアップ両方にある通貨 HUD と、①の「ゆるいデイリー」チェックリスト。**"やらなくても大丈夫" の哲学を壊さない**こと (枯渇で詰まない・未達ペナルティなし) が最重要の設計制約。

- [x] **通貨システム** — `web/src/wallet.ts` (新規)
  - 通貨は3種 (モックアップ①準拠): 🪙 しずく (基軸) / 🍃 若葉 (デイリー・チャレンジ報酬) / 🍄 胞子石 (希少: 実績解除報酬 + M14 で時代到達報酬を追加)。モックアップ②の4種目 (大陸探索専用通貨) は見送った — 3通貨のままでも大陸ステージの経済ループは成立するため。
  - 獲得: 日次結果 (M9 の result で成長量に応じて 🪙 を付与)、ゆるいデイリー全達成 (🍃)、チャレンジ初回達成 (🍃)、実績解除 (🍄)、時代が切り替わった節目 (🍄、M14)。
  - 消費: ツール使用コスト (`TOOL_COSTS` に一元化: エサ/水/乾燥/石=🪙、加温/冷却/毒素=🍃。光・削除は無料)。
  - **詰み防止**: 🪙 は下限 (20) を割っていると 4 秒に1枚、下限まで自動回復する (`tickRecovery()`)。
  - 永続化: localStorage (`morpho.wallet.v1`)。増減履歴を直近 50 件保持。`test/wallet.test.ts` でカバー。
- [x] **HUD** — ヘッダに通貨3種の残高チップ (`#cur-sizuku`/`#cur-wakaba`/`#cur-horoishi`)。増減時に一瞬アクセントカラーへ「フラッシュ」させる (**簡略化**: モックアップ原案の「+n フロート表示」は、実装をシンプルに保つため簡易なフラッシュ強調に代えた)。パレットは各ツールにコスト表示、残高不足時はグレーアウト + タイトルツールチップに理由を表示。
- [x] **ゆるいデイリー** — `web/src/dailies.ts` (新規)
  - 「エサを3つ置く」「光を1回あてる」等の**配置カウントタスク**を1日3件、日付ハッシュから重複なく決定的に選出。
  - 進捗ソース: `main.ts` の `applyAt()` (ツール適用イベント) をカウント。sim の状態は見ない。
  - UI: 「ゆるいデイリー」カードにチェックリスト (n/m) を表示。全達成で 🍃 ボーナス。**未達でも何も起きない**。「やらなくても大丈夫。あなたのペースで粘菌は育っていきます。」を必ず添えた。`test/dailies.test.ts` でカバー。
- [x] **チャレンジの一覧化** — `challenges.ts` を「1日1種ランダム」から「3種を常時表示・任意挑戦」へ変更。達成状態は種別ごとに記録し、それぞれ初回達成で 🍃 報酬。既存の `morpho.challenges.v1` (日付ごとの記録) は `v2` (種別ごとの記録) へ自動マイグレーションする。
- [x] **依存**: M9 (日次報酬の付与タイミング)、M10 (新ツールへのコスト設定) は満たした。
- **受け入れ基準**: 通貨が貯まる→使う→足りない (グレーアウト)→翌日回復、のループが成立する。デイリー/チャレンジを無視して放置しても何のペナルティも警告も出ない。`e2e/economy.spec.ts` で通貨HUD・デイリー3件・チャレンジ3件表示・エサ配置での消費を確認。

### M12 — 個体の物語: 名前・★・特性・追跡・出来事 🟡 部分完了

> モックアップの個体は「ねばりのこ #25 ★★★」であり、ただのパラメータ束ではない。個体を**キャラクター**にする。

- [x] **個体の同定** — `web/src/identity.ts` (新規)
  - `ねばりのこ #n` の通し番号を発番 (localStorage カウンタ、リセット/ステージ切替の世代交代で +1、全プレイ履歴を通じて増え続ける)。個体ビューのヘッダに表示。
  - リネーム可 (個体ビューの名前をタップ→ `prompt()` で編集)。**簡略化**: モックアップのインライン編集ではなく `window.prompt()` を使った (実装をシンプルに保つための判断)。
- [x] **★評価** — `starsOf(individuality): 1..5` 純関数 (`web/src/trait-labels.ts`)。6軸の平均を分位で量子化。個体ビューのヘッダに表示 (モックアップ②の ★★★☆☆)。閾値は `test/trait-labels.test.ts` で固定。
- [x] **特性チップ** — `web/src/trait-labels.ts` (`traitChipsFor()`)
  - `Genome` + `Traits` から日本語の特性文を導出: 「光を避ける」「乾燥にやや強い」「熱に強い」「毒に鈍感」(M10 耐性遺伝子) / 「枝分かれが多い」「伸びが早い」/ 「迷路構造が得意」(簡易版: 効率性の閾値のみで判定、環境暴露履歴は追跡しない) / 既存タイプラベルを先頭に含め、2〜4個に収める。
  - **未着手**: モックアップ①のタイプ別定型文段落 (「太い幹をつくり、安定したネットワークを好む。」) は今回実装していない。
- [x] **育った環境チップ** — `environmentTagsFor(balance)` で現在の環境バランスから「暗い」「乾いた土地」「エサが少ない」「毒素が多い」「暑い」「寒い」を導出し、個体ビューに常時表示。**スコープ縮小**: 障害物密度は `EnvBalance` に対応軸がないため対象外。また「採取/図鑑登録時に固定して保存」ではなく、個体ビューでは常に現在値を表示する簡易版 (図鑑/系統樹への保存は M13 で扱う)。
- [x] **個体を追跡する** — カメラ追従モード。`colony-networks.ts` の `ColonyMarker` に `centroid` (ネットワーク全体のノード位置の重心) を追加し、`Camera.panToward()` で毎フレーム滑らかに寄せる。ミニマップクリックで追跡対象を選択、ヘッダの「🎯 追跡」トグルで on/off。手動ホイール/パン/ピンチで追従解除。
- [x] **出来事の充実 (イベント種別)** — `sim/events/bus.ts` + `growth.ts`
  - `ObstacleAvoided` (成長候補が障害物で棄却された後、別方向で成功した時に emit) / `SporeFormed` (横方向出芽=lateralBud が新しい末端を作った時に emit、"胞子を生成"の演出に流用) を追加。`game.ts` の `eventToText()` で「障害物を迂回」「胞子を生成」の文言に変換 (LoopCreated 等と同様に高頻度発火を間引く)。`sim/test/events.test.ts` でカバー。
  - **未着手**: 独立した `event-text.ts` 辞書ファイルへの切り出し、「このエリアを注視中」トグル (出来事へ座標を持たせて視野内フィルタする改修は、既存の `recentEvents: string[]` を構造化データへ置き換える必要があり、図鑑/系統樹再設計と合わせて着手する)。ローカルタイム表示は M14 で対応済み。
- [ ] **未着手: プレイ中の突然変異** — 「進化の記録」への「個体が突然変異」イベント追加は今回見送った。
- [ ] **未着手: 統計グラフ (📊)**、**お気に入り (♥)** — M13 の図鑑グリッド再設計と合わせて着手する。
- [x] **依存**: M9 (`DayRecord`)、M10 (耐性遺伝子) は満たした。
- **受け入れ基準 (達成分)**: 個体ビューに「ねばりのこ #n」「★★★☆☆」と特性チップ・環境チップが見え、追跡モードで重心を追いかけられる。出来事欄に「障害物を迂回」「胞子を生成」が (発生条件を満たせば) 流れる。`e2e/identity.spec.ts` で改名・世代交代・追跡解除を検証。
- **未達の受け入れ基準**: 突然変異が「進化の記録」に定期的に現れる、というモックアップ②の演出は未実装 (上記参照)。

### M13 — 収集・記録: 図鑑グリッド・分岐する系統樹・実績バッジ・探索レポート 🟡 部分完了

> モックアップ①の「収集・記録」パネルと、②の実績/図鑑/探索レポート。**集める楽しみ**を作る。

- [x] **図鑑を 32 枠の枠組みへ** — `web/src/catalogue.ts` (新規) + `encyclopedia.ts` を v2 化
  - 種 = **タイプ (5) × ステージ (5)** の組合せ (例: 「太くつなぐ型 × 洞窟」=「ほらあなのねばりのこ」) で 25 種、特殊条件7種 (毒素の中で育った/3世代目以降/★5到達/全拠点接続/熱耐性が高い/毒耐性が高い/オールラウンド) を加えて **32 種**。**スコープ調整**: ロードマップ原案は「環境タグ (M12)」との組合せだったが、環境タグは多値・重複しうるため一意なカタログキーにしにくく、代わりに一意な `StageId` を第二軸に採用した。
  - `record()` は1回の発見で複数枠 (型×ステージ + 満たした特殊条件すべて) を同時に埋める。`morpho.encyclopedia.v1` → `v2` へ自動マイグレーション (旧5タイプ発見は「皿」変種として引き継ぐ)。`test/catalogue.test.ts` `test/encyclopedia.test.ts` でカバー。
- [x] **図鑑グリッド UI** — サムネイル付きグリッド (モックアップ①)
  - 新規発見時だけ `renderThumbnail()` でサムネを撮り、`catalogue-thumbs.ts` (新規、album.ts と同様の IndexedDB store) に保存。★ (M12) をオーバーレイ。未発見スロットは「?」。ヘッダに `n/32`。クリックでお気に入り (♥ 相当) を切り替え、`title` ツールチップで説明文・発見日を表示 (**簡略化**: モックアップの専用詳細カードではなくブラウザ標準ツールチップにした)。
- [x] **系統樹を分岐ツリーへ** — `lineage.ts` の線形リストを木構造に
  - `LineageEntry` に `id`/`parentId` を追加。**1つの親から何度でも採種できる**ようにし (`startFrom()` で任意の祖先を選び直せる)、系統樹カードの「この子から始める」ボタンでその祖先の genome を継承した個体からその場で再開する。
  - 描画: 世代ごとの行に並べる simple tree (`.lineage-row`/`.lineage-node`)。`morpho.lineage.v1` → `v2` へ自動マイグレーション (既存の線形履歴は一本鎖の木として読み込む)。`test/lineage.test.ts` + `e2e/lineage-tree.spec.ts` でカバー。
- [x] **実績バッジ UI** — バッジグリッドへ (解除済み=🏅アイコン、未解除=「?」ロック)。M9〜M12 の新要素に対応する実績5種 (七日の見守り/解毒の民/星読み/経済の民/やり直し上手) を追加し、計 **12種**に。
- [ ] **未着手: タブ UI (図鑑 / マップ / メモ)** — 図鑑・ミニマップ・メモを1つのタブ付きパネルに統合する改修は見送った (各カードは引き続き個別に表示)。
- [ ] **未着手: 探索レポート** — 総面積・総質量の推移グラフ、接続の歴史年表、PNG書き出しを含む探索レポートモーダルは見送った。
- [x] **依存**: M12 (★・サムネ発番の仕組み) は満たした。
- **受け入れ基準 (達成分)**: 図鑑が `n/32` のグリッド見た目 (サムネ、★、?スロット) になる。1つの親から2匹の子を育てると系統樹が枝分かれして表示される (`e2e/lineage-tree.spec.ts`)。実績に「?」の未解除スロットが並ぶ (`e2e/catalogue.spec.ts`)。
- **未達の受け入れ基準**: タブ UI と探索レポートは未実装。

### M14 — 大陸と時代: 大きな世界・時代の目標化・昼夜 🟡 部分完了

> モックアップ②の世界観スケール。「拠点 27/40」「接続ネットワーク 163」「次の時代まで あと 12:34」を成立させる。**パフォーマンスの前提が変わる唯一のマイルストーン**なので、着手前に bench で上限を測る。

- [x] **事前計測** — `bench.ts --worldscale` を追加し、`FIELD 96→192/256` × コロニー `3→20→40` の tick コスト曲線を測った。結果: フィールド解像度はコストにほぼ影響しない (diffuse は既に最適化済み)。コロニー/エッジ数がコストを支配するが、最大構成 (FIELD 256, コロニー40) でも実効 ×6.0 を確認 (要求ライン ×4 を上回る余裕)。**この計測結果から、`WORLD`/`FIELD` 自体を拡大しなくても拠点数を増やすだけで「大陸」らしさを十分表現できると判断し、下記のとおりスコープを絞った。**
- [x] **「大陸」ステージ** — `stages.ts` に追加 (既存5ステージは無改修)。**スコープ縮小**: `WORLD`/`FIELD` (100/96) は据え置き、拠点密度を上げる方向で実装した (ステージごとに worldSize を可変にする改修は camera/minimap/renderer/worker snapshot プロトコルへの波及が大きく、ここまでの計測で「拠点数がコストの支配要因」と分かった以上、危険度に見合わないと判断)。
  - 水域: `GridEnvironment.water: FieldGrid` (新規) を追加。**スコープ簡略化**: 通行不能の判定は `obstacle` フィールドに同じ形を重ね書きして実現し (`placeWaterBody()`)、growth/life 側のコード変更をゼロにした。`water` は描画が石と水を塗り分けるための専用マーカーとしてのみ使う。周囲の湿度を底上げする効果も持つ (「通行不能だが湿度を供給する」という要件どおり)。value noise 標高マップの代わりに、複数の湖ブロブをランダム配置する簡易生成にした (視覚的な結果はほぼ同等)。
  - 拠点: source 6 箇所 + 食料拠点 24 個 (計30) を `StageConfig.worldPoints()` (新規、棄却サンプリングによるポアソンディスク風配置) で手続き生成。既存5ステージは `worldPoints` を定義しないため、rng 消費順・挙動は一切変わらない (`game.ts` の `SOURCE_POINTS`/`FOOD_POINTS` は `DEFAULT_SOURCE_POINTS`/`DEFAULT_FOOD_POINTS` に改名の上、`Game.sourcePoints`/`foodPoints` というインスタンス state に格上げした — これが `worldPoints?()` を受け皿にする改修の実体)。
- [x] **メインクエスト「大陸全体に栄養を届けよう」** — `quests.ts` に `continent-nutrient` を追加。進捗は「陸地セル (water が立っていないセル) のうち biomass が一定以上あるセルの比率」(`Game.computeLandCoverage()`、`DerivedSnapshot` 側の間引き済み経路で計算)。大陸ステージのときだけカードを表示する (`ui.ts` で `stage.id` を見て `hidden` を切り替え)。
- [x] **時代を条件達成型へ** — `eraName(day)` を廃止し `era.ts` (新規) の昇格条件へ完全移行した。
  - 胞子期→拡散期 (拠点1つ以上接続)、拡散期→変形体期 (拠点2つ以上 + 総質量1.5kg以上)、変形体期→成熟期 (全ネットワーク統合 + 探索率50%以上)。
  - HUD: 円形プログレスリング (`#era-ring`, `--era-progress` CSS変数) を実装。**未着手**: 「次の時代まで あと mm:ss」の線形予測は見送った (進捗率の表示のみ)。
  - 「大陸全体に栄養を届けよう」クエスト (上記) が実質的に「世界の目標」の役割を兼ねる。時代到達時に 🍄 (胞子石) を1度贈るよう `main.ts` に配線した (`lastEraName` の変化を検知)。
  - 時代判定は `growthStep` と同じ12tickごとに間引き (毎tickだと M8 で潰したはずの Union-Find コストが復活するため)。既存 e2e/実績は元々時代名に依存していなかったため追従修正は不要だった。
- [x] **ローカルタイムと昼夜** — `daytime.ts` (新規)。1日 (`TICKS_PER_DAY`=40 tick) を24時間にマップした `localTimeFor()` (`hh:mm:ss`、個体ビュー横 `#local-time` に表示) と、`nightFactorFor()` (余弦カーブの夜の深さ [0,1]) を実装。**スコープ簡略化**: `brightness` フィールドへの実際のグローバル変調 (sim の成長応答に効かせる) は、GridEnvironment の brightness が空間分布を持つフィールドで毎tick全体書き換えが高コストなため見送り、`render.ts` のトーン係数のみに留めた (視覚的な昼夜演出、洞窟ステージは無効)。
- [ ] **ミニマップの地形描画** — 見送った。大陸ステージの水域は本編キャンバスには描画されるが、ミニマップの背景には焼いていない (優先度: モバイルUI刷新 (M15) の方が体験全体への影響が大きいと判断)。
- [ ] **ズームスライダー UI** — 見送った (ホイール/ピンチで機能は満たしている。UI 差異のみ)。
- [ ] **速度 UI の4段ボタン化** — 見送った (スライダー+⏸+⏩で機能は同等。UI 差異のみ)。
- [ ] **4通貨目 (大陸探索専用)** — 見送った。3通貨のままで大陸ステージの経済ループ (拠点コスト/報酬) は成立するため優先度を下げた。
- **依存**: M9 (`DayRecord` → 時代予測、未使用のまま終わった)、M11 (時代報酬、配線済み)。
- **受け入れ基準の実績**: 大陸ステージで拠点30・実際のネットワーク統合が動く (`e2e/continent.spec.ts`)。時代リングが条件で進む (残り時間予測は未着手)。昼夜で画面のトーンが呼吸する。実効速度は計測上 ×6 を維持 (拠点数を増やしても`WORLD`/`FIELD`据え置きのため既存ベンチ値のまま)。

### M15 — モバイル縦 UI 刷新とオンボーディング 🟡 部分完了

> モックアップ①は**スマホ縦持ちのゲーム画面**。現状の「カードを縦に積むだけ」(style.css の `@media 900px`) を、ゲームとして持てる UI に作り替える。

- [x] **縦画面レイアウト** — 「カードを縦に積むだけ」を廃し、ハンバーガー ☰ + ドロワー + 下部ツールバーの3層構成へ作り替えた。
  - 上部ミニ HUD: 通貨 (M11) / DAY / 時代 (M14) はそのまま常時表示のヘッダに残し、`#menu-toggle` (ハンバーガー、900px以下でのみ表示) を追加。
  - 下部ツールバー: `#mobile-toolbar` (新規) にツール9種 (M10で6→9種に拡張済み分すべて) を横一列 (横スクロール可) で常時表示。実体は右パネルの「環境を整える」カードと同じ `button.tool[data-tool=...]` の複製で、`main.ts`/`ui.ts` の `document.querySelectorAll('button.tool')` ベースの配線がそのまま両方に効くため JS 側の変更は不要だった。「観察をはじめる ▶」大ボタン (M9 の `#day-loop-bar`) はツールバー直上に固定表示するCSSを追加 (要素自体は既存のものを再利用、複製していない)。
  - パネル類 (クエスト/図鑑/系統樹/アルバム/設定 等): `aside.panel.left`/`aside.panel.right` を900px以下で全画面ドロワー化し、`body.drawer-open` クラスで開閉する。**スコープ簡略化**: モックアップの「ボトムシート」ではなく全画面オーバーレイにした (スライドアニメーションも見送り、`display:none/flex` の即時切り替え — `[hidden]` 相当の実装を transform ではなく display で行うことで、以前 M9 で踏んだ「author CSS の display が UA の `[hidden]{display:none}` を上書きする」バグと、position:fixed の containing block が transform で変わってしまう問題の両方を回避)。デスクトップは現行の3カラムのまま無改修 (`@media (max-width:900px)` の中だけで完結)。
- [x] **オンボーディング** — `onboarding.ts` (新規、純粋関数 + `localStorage` 薄いラッパー) + `main.ts` の DOM 配線。初回起動時のみ3ステップのコーチマーク (「環境を整える」→「委ねる」→「変化を観察する」) を `#onboarding` (既存の `.modal-overlay`/`.modal.card` を再利用) で表示。スキップ可、`morpho.onboarded.v1` で1回だけ。**スコープ簡略化**: 各ステップを実操作 (エサを1つ置く 等) と連動させる案は、操作の検出がゲーム状態と密結合になり複雑化するため見送り、「次へ」ボタンで進める3枚のコーチマークに簡略化した。
- [x] **モバイル e2e** — `e2e/mobile.spec.ts` に縦ビューポート (390×844) の `test.describe` を追加し、オンボーディング完了 → デイループ1周 → ハンバーガーから図鑑を開く、までを通した。`e2e/onboarding.spec.ts` (新規) でオンボーディング自体の3ステップ進行/スキップ/既読永続化も検証。既存の e2e (9ファイル、31箇所の `page.goto('/')`) は `addInitScript` で `morpho.onboarded.v1` を事前に立てることでオンボーディングの割り込みを避け、全て無改修で通ることを確認した。
- [ ] **仕上げ確認** — 見送った。実機 (iOS Safari / Android Chrome) での safe-area・ドロワーとピンチ操作の競合・PWA スタンドアロン起動・描画 3ms/frame 以内の確認は、この開発環境 (ヘッドレス Chromium の Playwright のみ) では実施できない。ユーザー側での実機QAが必要。
- **依存**: M9〜M11 (HUD に載せる要素)。M12〜M14 とは独立に着手できた (実際に他マイルストーンへの手戻りなし)。
- **受け入れ基準の実績**: スマホ縦持ち (390×844) でオンボーディング→デイループ→ドロワーの一連の操作が片手相当の操作数で通ることを e2e で確認。実機での「片手で回る」感触の最終確認は仕上げ確認の項目として持ち越し。

## アーキテクチャ方針

- **sim はステートレスに保つ** — UI 側は SimState のスナップショットを読むだけ。書き込みは `Environment.placeX()` 経由に限定する。M10 の温度/毒素も同じ流儀 (`placeHeat`/`placeToxin`) で追加し、Undo は環境フィールドの差分逆適用であって sim の時間巻き戻しではない。
- **描画はラッパー** — `@morpho/web` は sim を import するだけで、sim 側に描画依存を持ち込まない (Node でも引き続き使える)。
- **時間スケールは UI に切り出す** — sim は「1 tick」しか知らない。日数・時代・デイループ (M9)・ローカルタイム (M14) はすべて web 側の派生量。
- **個性 / 図鑑 / 子孫 / 通貨 / デイリーは web 側で持つ** — sim は遺伝子を1個体ぶん受け取って動くだけ。永続化は localStorage (画像のみ IndexedDB)。スキーマを変えるときは必ず `vN → vN+1` のマイグレーションを書く (M13 の図鑑/系統樹が該当)。
- **ゲームロジックは純粋関数 + vitest** — `quests.ts` / `challenges.ts` / `tick-scheduler.ts` と同様に、M9 以降の新ロジック (day-loop / wallet / dailies / trait-labels / era / catalogue) も DOM と Worker から切り離した純粋モジュールとしてテストする。
- **リポジトリルートは pnpm workspaces** — `@morpho/web` は `@morpho/sim` をビルド成果物ではなく `../sim/src` を直接 import している。install は必ずリポジトリルートで行うこと。
- **tick は Worker、描画はメインスレッド** — `Game` の実体は `sim-worker.ts` の中で動く。メインスレッドは `game-proxy.ts` を通じて postMessage で命令を送り、最新スナップショットを受け取って描画するだけ。M9 の `runUntilTick`、M10 の `undoStroke` もこのプロトコルの命令として追加する。
- **性能は計測してから** — sim に触る変更 (M10) と規模を変える変更 (M14) は、着手前後で `pnpm --filter @morpho/sim run bench` を回し、`?debug` の perf HUD で実効速度を確認する。

## 動かし方

```sh
pnpm install      # リポジトリルートで一度だけ (workspaces が両パッケージの依存を解決する)
cd packages/web
pnpm dev
# → http://localhost:5173 で遊べる
```
