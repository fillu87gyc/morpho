# モルフォ — Web ゲーム化ロードマップ

> 「制御する」のではなく、「条件を整え、振る舞いを引き出す」。
> モックアップで描いた粘菌育成体験へ、既存の `@morpho/sim` を段階的に拡張する計画。

## ビジョン (2026-07-09 再定義)

実プレイ検証 第5回を受けて、目指すゲーム像を次のように言語化し直した。
以降のマイルストーンはすべてこの5行に奉仕する。

1. **果てのない世界** — Minecraft / Factorio のように、定常状態を持たず時間さえ与えれば
   無限に広がっていく。皿を埋めて終わりではなく、「原野」が本編。
2. **ズームインすると個性と地場** — 極狭い範囲では個体の個性と局所環境の効果が支配的で、
   一株ずつ違う振る舞いが見える。
3. **ズームアウトすると大局の伸び** — 俯瞰すれば「どんどん伸びていく」ことが絵として見える。
   俯瞰の高さは世界の成長とともに上がっていく。
4. **採餌の旅** — 視界に餌がなくても餌を探して這い回る。母体から近い組織は消費が緩やか、
   遠征している組織は早く消耗する (距離のコスト勾配)。だから遠くの豊かな餌場は挑戦になる。
5. **クリッカー型の介入カーブ** — 序盤は極小の世界で手動介入が濃い。後半は個性と環境が
   支配的になり手動の一手が相対的に無意味になる — プレイヤーは見守るか、
   「環境を買う」ような大局への高価な介入だけを行う。

## 現在地

- **`@morpho/sim`** — 粘菌のローカル則 (グラフ成長 / Biomass膜 / Activity場 / 温度・毒素・水域) は完成済み。
  M25 でチャンク化 (`ChunkedGridEnvironment` / `ChunkedActivityField` / `ChunkedBiomassField`) と
  forager reclaim (再採餌) が入り、半無限ワールドを駆動できる。決定論テスト + ベンチで守られている。
- **`@morpho/web`** — M0〜M27 完了。モックアップの遊び方 (デイループ、環境ツール9種、3通貨経済、
  個体の同定、図鑑と系統樹、大陸と時代、モバイルUI、PWA、Pages デプロイ)、モックアップの絵作り
  (テクスチャ/スプライト/湖岸線/有機的な脈)、新ステージ「原野」(半無限ワールド) まで出荷済み。
  各マイルストーンの要約は下の表、詳細は git 履歴と各ファイル冒頭コメント。
- **実プレイ検証 第5回 (2026-07-09)** — 「原野」をブラウザで6分強 (Day 16 相当) 観察 + sim 直接駆動で
  Day 100 相当まで実測し、上のビジョンとの乖離を洗い出した (下の「ビジョンとの乖離」節)。
  **結論: 半無限ワールドの土台 (M25) は動いているが、「広がりが見える」「広がり続けられる」
  「介入カーブがある」の3点が全て欠けており、現状の原野は
  『100×100 の窓の中で網がひたすら稠密化していくのを眺めるゲーム』になっている。**
  証跡は `docs/playtest-2026-07-09-infinite/`。対応は M28〜M32 に再定義した。

## 完了済みマイルストーン (M0〜M27) — 要約

> 詳細な設計メモは各実装ファイル冒頭のコメントと git 履歴 (各マイルストーンのコミット/PR)、
> 絵作りの経緯は `docs/art-direction.md`。ここでは「何がどこにあるか」だけを引けるようにする。

| M | 一行要約 | 主なコード |
| --- | --- | --- |
| **M0 土台** | Vite + TS の `packages/web`。Canvas 描画、再生/停止/速度、ツール5種、HUD、CI | `main.ts` `render.ts` |
| **M1 観察の快適化** | 差分再描画 60fps、sim を Worker へ、タイムライン、ヒートマップ、ズーム/パン | `field-diff.ts` `sim-worker.ts` `camera.ts` |
| **M1.5 土地** | バイオーム5種を手続き生成 + 地形テクスチャ (単色ベース) | `game.ts` |
| **M2 個性** | seed 決定的な Genome、6軸個性 + 5タイプ、図鑑、個体ビュー | `genome.ts` `individuality.ts` |
| **M3 環境の物語** | ステージ5種 (皿/洞窟/砂漠/都市跡/湿地)、環境の自然減衰 | `stages.ts` `environment.ts` |
| **M4 ゆるい目標** | メインクエスト・デイリーチャレンジ・実績・ベスト記録 | `quests.ts` `challenges.ts` |
| **M5 子孫** | 種の採取 → 変異付き継承、過酷な環境ほど変異幅大 | `genome.ts` `lineage.ts` |
| **M6 World View** | 複数コロニー、ミニマップ、Union-Find の統合判定 | `colony-networks.ts` `minimap.ts` |
| **M7 仕上げ** | 環境音、アルバム (IndexedDB)、PWA、タッチ、Pages 自動デプロイ | `ambient.ts` `album.ts` |
| **M8 速度** | tick 最適化、時間予算スケジューラ、perf HUD (`?debug`)、ベンチ | `tick-scheduler.ts` `perf-hud.ts` |
| **M9 デイループ** | 「仕込む→委ねる→受け取る」の1日フェーズ、結果パネル | `day-loop.ts` `day-report.ts` |
| **M10 環境システム** | 温度・毒素フィールド、耐性遺伝子、ツール4種追加 + Undo | `environment.ts` `undo.ts` |
| **M11 経済/デイリー** | 3通貨 Wallet (詰み防止付き)、ツールコスト、デイリー3件/日 | `wallet.ts` `dailies.ts` |
| **M12 個体の物語** | 通し番号+リネーム、★、特性チップ、カメラ追従 | `identity.ts` `trait-labels.ts` |
| **M13 収集/記録** | 図鑑37枠カタログ、系統樹の分岐ツリー、実績12種 | `catalogue.ts` `lineage.ts` |
| **M14 大陸と時代** | 大陸ステージ (水域+拠点)、条件達成型の時代、昼夜トーン | `era.ts` `daytime.ts` |
| **M15 モバイルUI** | ドロワー+下部ツールバー (≤900px)、コーチマーク | `onboarding.ts` `style.css` |
| **M15.5 実プレイ緊急修正** | 時代の day ゲート、クエスト引き締め、モバイル 0×0 キャンバス修正 | `era.ts` `main.ts` |
| **M15.7 時間スケール** | 壁時計ペーシング、`TICKS_PER_DAY` 40→240、×1=2.4分/日、`?dayms=` フック | `time-scale.ts` `sim-worker.ts` |
| **M16 世界の実感** | 時代 ETA「あと mm:ss」、ミニマップ地形焼き込み、速度4段、ズームスライダー | `era.ts` `minimap.ts` |
| **M16.5 ビジュアルパス** | 配色刷新 (黒潰れ解消・金色の2パス発光・リム発光・水の深浅)、輝度 e2e | `render.ts` `docs/art-direction.md` |
| **M17 個体の物語 完** | WorldEvent 構造化 (時刻+エリア注視)、突然変異イベント、特徴文、📊グラフ | `world-events.ts` `mutation-events.ts` |
| **M18 収集・記録ハブ** | 図鑑/マップ/メモのタブ、探索レポート、時代の年表、系統樹サムネ | `notes.ts` `report.ts` `era-history.ts` |
| **M19 総仕上げ** | デイループ既定化2択、演出3件、実機QA表 (`docs/QA.md`)、4通貨目の見送り判定 | `main.ts` `docs/QA.md` |
| **M20 アセット** | 発注ブリーフ (`docs/asset-briefs.md`)、タイル/スプライト実物、`AssetManager` (無ければベクタへフォールバック) | `assets.ts` |
| **M21 地形の質感** | タイルテクスチャ敷き込み、岩の露頭スプライト、接地影、木漏れ日 — ズームに耐える地面 | `render.ts` `terrain-blobs.ts` |
| **M22 水場の再設計** | marching squares の湖岸線、水面テクスチャ+漂流、浅瀬/湿った砂の岸帯、ミニマップ追従 | `coastline.ts` |
| **M23 ネットワーク有機化** | Catmull-Rom の曲線脈+先細り、星形ハブ、成長前線の扇、ジオメトリキャッシュ | `vein-curves.ts` |
| **M24 生態感** | キノコ/苔/朽木の決定的散布 (食べ尽くすと消える)、エサのオーブ化 | `ecology-scatter.ts` |
| **M25 果てのない世界 (土台)** | sim チャンク化、forager reclaim (枯れた sink を前線チップへ戻す)、窓アダプタ、新ステージ「原野」 | `chunked-*.ts` `chunk-window.ts` `growth.ts` |
| **M26 (旧)** | 俯瞰上昇の下敷き関数のみ (dead code)。実体は M28/M29 へ再定義して吸収 | `camera.ts` の `fitBBoxSpan` 等 |
| **M27 有界ステージの終盤** | 栄養の再生サイクル (season 脈動)、時代停滞時の残条件明示、期限切れチャレンジ表示 | `nutrient-regen.ts` `era.ts` |

**意図的な見送り (再掲)**: 昼夜 brightness の sim への反映 (毎tick全面書換が高コスト)、4通貨目 (3通貨で経済成立を確認済み)。
**過去の検証証跡**: 第2回 `docs/playtest-2026-07-05/`、第3回 (絵作りの乖離) + 第4回 (Day 24 完全凍結の実測) `docs/playtest-2026-07-05-visuals/`、M20〜M24 の before/after `docs/playtest-2026-07-06-visuals/`。

## ビジョンとの乖離 (実測、2026-07-09 第5回検証)

> 手法: (a) ブラウザ実プレイ — `vite preview` + headless Chromium で皿と原野を操作し、
> 原野は ×24 で6分強 (Day 16) 観察、HUD を15秒おきにサンプリング。
> (b) sim 直接駆動 — 原野相当の構成 (チャンク食料 + reclaim=0.3) で Day 100 相当 (24,000 tick) を
> 実測。証跡: `docs/playtest-2026-07-09-infinite/` (スクリーンショット + サンプリング生データ)。

| # | ビジョン | 実測した現状 | 対応 |
| --- | --- | --- | --- |
| V1 | ズームアウトすると大局の伸びが見える | **俯瞰が存在しない**。`camera.ts` は `MIN_ZOOM=1` で、最小ズーム=100×100 の窓がちょうど画面いっぱい。ズームスライダーを最小にしても画面が1ピクセルも変わらない (`b-wildland-zoomout.png` = `b-wildland-end.png`)。世界がどれだけ広がっても絵として一切見えない | **M28** |
| V2 | 世界の成長が数字でも実感できる | **HUD の「総面積/総量」は窓の中しか数えていない** (`game.ts` の `computeWorld` は焼き出した 96×96 窓を集計、理論上限 10,000m²)。原野 Day 9 以降は 8,000〜9,000m² を上下するだけで、成長が数字にも見えない。全世界を数えているのは接続ネットワーク数 (エッジ本数) だけ | **M28** |
| V3 | 定常状態を持たず無限に広がる | 出荷構成のブラウザ実測では、拡大ではなく**稠密化**する: Day 16 でエッジ 6,908 本の迷路が窓を埋め尽くし (`b-wildland-end.png`)、面積は頭打ち。sim 直接駆動 (Day 100) では前線スパン 26→192 と広がり続けるが、ペースは約 +1.9/日と控えめで、しかも非単調 (Day 65→75 で 144→92 と大きく後退することがある) | **M29/M30** |
| V4 | 時間さえ与えれば広がり続けられる | **性能で時間が崩壊する**。×24 指定でも実効ペースは開始直後で約 2.1秒/日、Day 16 (エッジ約7,000) では約45〜60秒/日 — 公称 (160ms/日) の約300倍遅い。sim 直接駆動でも、生成済みチャンクが evict されず溜まり続け (68チャンク到達)、エッジ200本程度なのに 5.4ms/tick まで漸増。長時間セッションは現状成立しない | **M29** |
| V5 | 探索範囲に餌がなくても探し回る | forager reclaim (M25) で「枯れたら這い出す」は実現済み。ただし原野の地形は**全チャンクに必ず餌パッチが1つ**湧く一様な野原で、「探し回る」旅にならない (どこへ行っても餌がある)。バイオームも1種類 | **M30** |
| V6 | 母体から近いと消費が緩やか、遠いと早い | **存在しない**。`life.ts` の維持コストは完全にローカルで、母体からの距離という概念が sim にない | **M30** |
| V7 | 序盤は極小の世界で手動が濃い | 皿は Day 0 の時点で既に 6拠点・4,464m²・318リンクが繋がっており、チャレンジ3件中2件が開始直後に自動達成される。原野も開始1日で「全拠点接続 100%」など3件全て自動達成 (`b-wildland-start.png`)。**「極小から手ずから育てる」立ち上がりがどのステージにもない** | **M31** |
| V8 | 後半は見守るか「環境を買う」大局介入 | **見守りモードには収入が一切ない** (雫の日次収入は日次結果モーダル = デイループモード限定、`main.ts`)。原野を16日見守っても雫 117 のまま不変。後半に効く高価な大局介入ツールも存在しない (ツールは全て窓内のブラシのみ) | **M31** |
| V9 | 原野が本編として成立している | 目標系がすべて有界ステージ用のまま: メインクエスト「拠点をすべてつなぐ」が開始直後から100%、「大陸の85%を探索する」は 0% 固定、時代は拡散期で停止 (「もう1拠点に到達」は原野では構造的に満たせない)。時代 ETA 表示が15秒ごとに「あと1:20 ↔ あと17:47 ↔ 条件:…」と乱高下。Undo/消去ツール無効、図鑑に原野個体が収録されない (いずれも M25 の意図的スコープ外のまま) | **M32** |

### 検証で見つけた小さな違和感 (上記以外)

- [ ] **チャンクの明るい正方形パッチ** — 原野の地面に半透明の四角いタイルがはっきり見える
  (`b-wildland-start.png` 背景)。木漏れ日 (`drawDappledLight`) かチャンク焼き出しの継ぎ目が
  正方形単位で出ているとみられる。M28 の俯瞰描画と合わせて調査 (小、`render.ts`)。
- [ ] **時代 ETA の乱高下** — `estimateEraEta()` が進捗の揺れをそのまま外挿するため、
  15秒間隔のサンプリングで毎回別の値 (1分↔22分↔条件表示) になる。移動平均などの平滑化が要る
  (小、`era.ts`)。V9 の一部として M32 で対応。

## 次のマイルストーン (M28〜M32) — ビジョンへ寄せる

> 位置づけ: **原野を本編に育てる**。有界6ステージ (皿〜大陸) は完成品として凍結し
> (回帰ゼロを保つ)、以降の開発は原則すべて原野に投資する。
> M28 (見える) → M29 (続く) → M30 (探す) → M31 (介入カーブ) → M32 (本編化) の順。
> M28 と M29 は補完関係 (俯瞰は描画の、休眠は sim の LOD) で、この2つが揃って
> はじめて「広がり続ける世界を眺める」が成立する。

### M28 — 俯瞰: 世界の広がりが「見える」ようにする

> V1/V2。ユーザー体験の核心で、最優先。モックアップ①の「WORLD VIEW ズームアウト: 大局を俯瞰」
> はこれ。M25 の窓方式 (カメラ無改修) は正解だったが、窓の外を見る手段を作らなかった。

> **進捗 (2026-07-09、完了)**: sim のチャンク要約 API → Worker の低頻度
> `worldOverview` メッセージ → カメラの動的最小ズーム → zoom < 1 の大局レイヤー →
> ワールドマップまで一気通貫で実装した。実プレイ実測 (`vite preview` + headless
> Chromium、×24 で100秒 = Day 6): **HUD 総面積 11,181 → 36,457m² と窓の理論上限
> (10,000m²) を超えて増え続け、最小ズームは世界の成長とともに 0.32 → 0.20 へ
> 下がり続け、ズームアウトすると窓 (100×100) の外に37チャンクぶんの世界が絵として
> 見える** (`docs/playtest-2026-07-09-infinite/after-m28-zoomout.png`、before は同
> ディレクトリの `b-wildland-zoomout.png` = 画面が1ピクセルも変わらなかった頃)。
> 俯瞰レイヤーの追加描画コストは実測 0.1〜0.5ms/frame (予算 3ms、`?debug` の
> perf HUD に内訳 `ov` 行を追加)。カメラ/ミニマップ/レンダラの変更はすべて
> 「原野のときだけ」の分岐で、既存6ステージは挙動・描画・決定論とも不変。
> e2e 51件 (新規2件含む) 全通過・回帰ゼロ。

- [x] **大局レイヤーの描画** — sim 側: `ChunkedGridEnvironment.summarizeChunks()`
  (栄養平均・障害物密度・水域有無) と `ChunkedScalarField.summarizeChunks()`/
  `summarizeWorld()` (バイオマス総和)。いずれも `peekChunk` ベースで副作用なし =
  何度呼んでも決定論を乱さない (`sim/test/chunk-summary.test.ts`)。web 側:
  `Game.worldOverview()` が地形とバイオマスの要約を束ね、Worker が1秒間隔
  (盤面が動いたときだけ) の `worldOverview` メッセージで届け、`world-overview.ts`
  が最新値を保持する。絵は `render.ts` の俯瞰経路 + `overview-layer.ts` (新規):
  訪問済みチャンクを nutrientAvg (地面の明度)・obstacleDensity (岩色)・hasWater
  (水色) で塗る粗タイル (色は純粋関数 `chunkTileColor`、`OverviewTileCache` が
  overview 更新時だけオフスクリーンへ焼き直す)、biomass 量に応じた金色の発光
  (1px/チャンクで焼き、貼るときの拡大補間で柔らかく滲ませる)、網の骨格
  (全エッジを直線のまま 1本の Path2D にまとめて stroke 1回 — M23 の曲線描画は
  俯瞰では使わない。パスはワールド座標で state 参照ごとにキャッシュ)。
  未訪問領域は暗いまま。**zoom=1 を跨ぐ違和感はハイブリッド + クロスフェードで
  消した**: zoom < 1 では窓領域 [0,100] だけをクリップして従来の詳細描画
  (M20〜M24 資産) を生かし、さらに zoom 1.0→0.7 の帯でタイル/骨格線を 0→1、
  曲線脈+ノードを 1→0 へフェードする。
- [x] **ズームの再設計** — `camera.ts` の `MIN_ZOOM=1` 固定を撤廃。純粋関数
  `wildlandMinZoom(bbox, worldSize)` が「訪問済み世界の bbox + 余白 1.3 倍が
  ちょうど収まる」最小ズームを返す (M26 の `fitBBoxSpan` をここで実戦投入)。
  世界が広がるほど下限が下がる = 俯瞰の高さが上がっていく。安全弁として span は
  2,000 (zoom 0.05) まで。bbox は `overviewLocalBBox()` (world-overview.ts) が
  チャンク集合から窓ローカル座標へ導出し、main.ts が毎フレーム
  `camera.setWildlandBounds()` へ供給する。zoom < 1 では `clampCenter()` が
  中央固定ではなく「bbox 内へのパン」を許す (窓の外の訪問済み領域を見に行ける。
  zoom >= 1 のクランプは原野でも従来のまま)。ズームスライダーの min 属性・
  ±ボタン・ホイール/ピンチ (`zoomAt`) すべてこの動的下限を尊重する。
  `setWildlandBounds` が呼ばれない既存6ステージは値域 [1,8]・中央固定とも
  完全に不変 (camera.test.ts に対照テストを追加)。
  - **座標系の落とし穴 (対処済み)**: チャンク番地は実座標で、窓ローカルへは
    「実座標 − 窓原点」で変換するが、overview に同梱の windowOrigin は集計
    時点の値なので窓の再センタリング直後に最大1秒古い。snapshot に現在の
    窓原点 (`FastSnapshot.windowOrigin`、原野のみ) を同梱し、描画は必ず
    そちらを使うことで、タイルと骨格線 (窓相対のノード座標) のずれを断った。
- [x] **HUD 統計の全世界化** (M28-A) — `computeWorld()` は原野では
  `chunkBio.summarizeWorld()` のチャンク横断集計へ切り替え (走査は 24tick に
  1回だけ数え直すキャッシュ付き)。「到達距離 (母体から最遠の組織まで)」
  「探索チャンク」をワールド情報パネルへ追加 (原野のみ表示、有界ステージは
  行ごと隠す)。既存6ステージの面積・総量は従来の窓集計のまま bit 一致
  (`test/wildland-world.test.ts`)。
- [x] **ワールドマップ** — `minimap.ts` の `drawWildland()`: 原野では「訪問済み
  世界の全体図 (大局レイヤーと同じ `chunkTileColor` + biomass の光) + 現在の
  窓を示す枠 + カメラ視野の枠」に切り替える。焼き直しは overview の tick が
  進んだときだけ。タップは `toWorldWildland()` で窓ローカル座標へ逆変換し、
  カメラをその位置へ俯瞰パン (zoom ≈ 0.33、動的下限でクランプ) する — 窓自体は
  sim 側の前線追従なので動かさず、窓の外は粗タイルのまま (詳細は見えない) が仕様。
- **受け入れ基準 → 達成を実測**: ズームアウトで開始時 (3×3 チャンク) より明確に
  広い世界 (37チャンク) が見える (`after-m28-zoom1.png` ⇄ `after-m28-zoomout.png`、
  ワールドマップのタップパンは `after-m28-worldmap-tap.png`)。HUD 総面積は
  10,000m² を超えて増え続ける (100秒で 36,457m²)。e2e: 「ズームスライダー最小で
  絵が変わる (checksum 比較 — M25 までは1ピクセルも変わらなかった)」「成長後の
  ズームアウトで訪問済みチャンクのタイルが窓の外側の座標に描かれ、未訪問の四隅は
  暗いまま (ピクセル検査)」を `e2e/wildland.spec.ts` へ追加し、既存49件 + 新規2件 =
  51件全通過。

### M29 — 止まらない時間: LOD/休眠で「広がり続けられる」ようにする

> V3/V4。旧 M26 の未着手分 (LOD と休眠・チャンクタイル化) をこの形で引き継ぐ。
> 実測済みのボトルネック: (a) エッジ約7,000本で実効ペースが公称の約300倍遅い、
> (b) 生成済みチャンクが evict されず diffuse コストが漸増、(c) エッジ1万本規模で
> tick 本体 30.5ms/tick (M25 実測)。原因は「全エッジ・全チャンクを毎tick等しく更新する」こと。

> **進捗 (2026-07-10、完了)**: M29-A (sim 側: 休眠 + evict、コミット 7b05700) →
> M29-B (原野への配線 + 稠密化抑制 + 実効ペース計測) の2段で実装した。
> 採った値と実測は下の各項目と実装メモを参照。ブラウザ実測 (`vite preview` +
> headless Chromium、×24 で 5.5 分 = Day 49、`docs/playtest-2026-07-09-infinite/`
> の browser-hud-samples-m29.txt / after-m29-*.png): **実効ペースは Day 2 →
> Day 49 まで 6.6〜10.8 日/分 (公称 ×24 = 10 日/分の 66〜108%) を維持し、
> tick 9.4〜22.0ms で頭打ち**。before (同ディレクトリの第5回実測) は Day 6 で
> tick 63.6ms・実効 ×3.7、Day 16 で 45〜60 秒/日 (≈1.2 日/分) だった —
> 実効ペースで約8倍、迷路化の解消込みで Day 49 まで公称近くを保つ。

- [x] **成熟領域の休眠 (sim)** — M29-A で実装 (`graph/dormancy.ts`)。前線 =
  「最新 born 上位4個の相異なる空間セル + Chebyshev margin 1」で、それ以外の
  セル内エッジは activity/fatigue/stress/radius 更新とフィールド deposit を
  スキップし、flux の通り道としてだけ生きる (凍結すると前線の sink が飢えて
  壊死するため)。判定は RNG 不使用で seed 決定的。起床は3経路 (前線接近の
  定期判定 / 成長の踏み込み wakeCellAt / 外部イベント wakeDormantArea — M29-B
  で `applyWildlandTool` に配線済み)。既存6ステージは dormancyCheckInterval=0
  (既定無効) で bit 一致 (sim 97テストの既存85は無修正で通過)。
- [x] **チャンクの evict** — M29-A で実装。awake セルから遠い実体チャンクを
  平均値1個へ圧縮解放し、再訪時に決定的に復元する。prune 後の無人跡地も回収。
  web 側の「探索チャンク」統計は evict で減らない累計 `touchedChunkCount()` へ
  切り替えた。実測 (出荷構成 Day 100): 実体チャンクは 14〜23 で頭打ち
  (evict 済み累計 106)、メモリが前線サイズ比例になった。
- [x] **稠密化の抑制 (バランス)** — 出荷構成相当 (applyGenome(PETRI_PARAMS,
  genome) + wildland overrides) の Game 直接駆動ハーネスで対照実験し
  (`docs/playtest-2026-07-09-infinite/sim-shipped-m29b.txt`)、原野の
  `paramOverrides` に **lateralBudBiomassThreshold 0.20→0.55 /
  lateralBudProbability 0.30→0.12** を採用。Day 40 対照 (同一 seed) で
  エッジ 8,729→1,529 (-82%) に対し span 677→613 (-9%) — 「エッジ数の増加 <
  span の増加」へ配分が反転した。gradientBias 強化案は却下 (勾配は局所の餌へ
  向かうベクトルなので強めるほど前線が餌に絡まり span 412 と逆効果)。
  穏やかな絞り (0.35/0.18) も却下 (span 同等でエッジ 1.8 倍)。
- [x] **性能の受け入れ基準を時間で切る** — `?debug` の perf HUD に
  「pace N.NN 日/分」(Worker 実測、worker-protocol.ts の PerfInfo) と
  「dormant N cells / evict N」を追加。sim ハーネス (出荷構成 Day 100):
  tick 9.1ms (Day 5) → 16.1ms (Day 100) で、×24 の予算 25ms/tick を
  Day 100 まで下回る = 実効ペースは満速のまま。ブラウザ実測は上の進捗欄。
  e2e に「×24 で60秒回して前半30秒と後半30秒の日進行比 > 0.5」を追加
  (実測 ~0.9-1.0、フレーク耐性でしきい値は保守的に)。
- **受け入れ基準**: 達成 — 実効ペース維持 (上記)、実体チャンク数は前線サイズ
  比例で頭打ち、sim 97 + web 362 テスト・e2e 52件 (51+新規1) 全通過。
- **実装メモ / 既知の限界**:
  - 休眠セル一辺は 24 = フィールドチャンク一辺 (48) のちょうど半分 (1チャンク
    = 2×2 セル)。48 (1:1) との対照では 24 の方が awake 領域を細かく絞れて
    Day 40 で 13ms/tick vs 31ms/tick (span 同等)。evict 判定はチャンク中心
    セル ±1 なので、セルがチャンクより細かくても awake に重なるチャンクを
    誤って解放しない。
  - **凍結ノード/エッジの蓄積**: 休眠エッジも flux (マルチソース BFS、O(V+E))
    には参加し続けるため、tick コストは総エッジ数に緩く比例して漸増する
    (Day 5→100 で 9→16ms)。×24 予算内だが、Day 200 級では再び効いてくる。
    M30 の距離 upkeep (遠征枝が枯れて戻る) で総数の頭打ちを作るのが本命。
  - **span の頭打ち**: 出荷構成ハーネスで span が Day 50 以降 660 で停滞する
    (touched チャンクは 82→129 と増え続ける = 前線は新しい土地を踏んでいる
    が、bbox を押し広げる方向の遠征が prune で戻される)。「もっと遠くへ」は
    M30 (餌の地理と距離勾配) の領分。
  - **窓と休眠域の重なり**: 窓アダプタ (`bakeChunkWindow`) は窓下の evict 済み
    チャンクを平均値で再実体化する (決定的)。実プレイの絵 (after-m29-mid/
    end-window.png) では脈は連続して描かれ破綻はないが、平均値チャンクの
    明るい正方形パッチは従来の「チャンクの明るい正方形パッチ」の違和感
    (下の検証メモ) をやや強調する。窓の再センタリング時に `wakeDormantArea`
    で窓領域を起こす案は、休眠の意味を薄めるため今回は見送り (絵の破綻が
    出たら再検討)。
  - ×24 時の HUD エッジ数は Day 49 で 1,521 本 (before: Day 16 で 6,908 本)。
    網は迷路ではなく「太い幹線 + 探索する細脈」の絵になった (スカスカに
    見えるかは M30 のバイオーム密度と合わせて再調整する)。

### M30 — 餌の地理: 「探し回る」価値のある世界にする

> V5/V6。採餌の仕組み (reclaim) はあるので、探す理由 (希少性と勾配) を世界側に作る。

> **進捗 (2026-07-10、完了)**: M30-A (sim 側: distanceUpkeep の hop 距離モード、
> コミット d928d5c) → M30-B (このコミット: 距離定義を 'origin' = スタート地点からの
> ユークリッド距離へ拡張 + バイオーム5種 + 原野配線 + 子孫接続 + 俯瞰の毒色)。
> 実プレイ検証は下の実装メモと `docs/playtest-2026-07-09-infinite/` (after-m30-*.png、
> sim-m30-*.txt)。

- [x] **バイオーム** — `biomes.ts` (web、純粋関数 + vitest) の `biomeAt(cx, cy, worldSeed)`:
  チャンク座標の決定的な値ノイズ (整数格子ハッシュの双線形補間、周期
  `BIOME_NOISE_SCALE=3` チャンク) を肥沃度/険しさ/水気の3軸で引き、5種へ分類 —
  **豊かな森** (餌2パッチ・湿潤、面積比 ~45%)、**痩せた荒地** (餌は30%のチャンクに
  小さな飛び石1つのみ・乾燥、~30%。横断の旅の主役)、**岩場** (障害物3〜5・餌少、
  ~10%)、**毒の窪地** (毒素2染み + 餌豊か = リスクリワード、~5%)、**水辺** (水域 +
  湿潤、~10%)。開始点の周囲 3×3 チャンクはノイズに依らず「母体の森」に固定
  (初手が荒地で即詰みになる seed の救済)。地形は `wildlandChunkTerrain` (stages.ts)
  が生成し、sim 側は `ChunkedGridEnvironment.generateTerrain` に toxin/moisture/
  water パッチを追加対応 (既存の呼び出しは省略可で M25 互換)。原野のみ
  `toxinDecayPerTick 0.0015→0.0004` (毒の窪地が数日で無害化しないように)。
- [x] **距離のコスト勾配 (sim)** — 2段階で実装。
  **M30-A (hops)**: `params.distanceUpkeep` (既定 0=無効) / `distanceUpdateInterval`
  (既定 60)。母体からの距離 h のエッジは fatigue 増分 ×(1+K·h)・回復 ÷(1+K·h)。
  距離は flux のマルチソース BFS に便乗して低頻度記録 (二重計算ゼロ・RNG 不使用)。
  既知の限界: hop 距離は forager reclaim で切り離された孤立前線に載らない。
  **M30-B (origin)**: `params.distanceMode: 'hops' | 'origin'` (既定 'hops' = M30-A
  互換)。'origin' は原点 (`seedSource` が `state.origins` へ確定記録、複数なら最寄り)
  からのユークリッド距離 (ワールド単位) を全ノードに記録 — 孤立前線にも効く
  (ハーネス実測でノードの距離カバレッジ 96〜100%、hops は連結コアのみ)。
  原野の出荷値は **K=0.0015/unit, mode='origin'** (hop 版推奨 0.005/hop ÷ growthStep
  3.6 で再スケール)。
- [x] **子孫の出番** — `wildMutationBoost(distance, biome)` (biomes.ts):
  原野で採種すると、母体から最も遠い前線ノードの到達距離 (500 unit で飽和、+50%)
  × バイオーム係数 (森/水辺 1.0、岩場 1.15、荒地 1.3、毒 1.35、上限 2.0) を
  `LineageEntry.mutationBoost` に記録し、その種を植えるとき `mutationScaleFor(stage)`
  に乗じる (`Game.reset(seed, stage, parentGenome, parentMutationBoost)`、Worker
  経由で配線)。既存の採種 UI は不変・有界6ステージの採種は boost 無記録 = 従来通り。
- **受け入れ基準**: 達成 —
  - 「バイオーム境界で前線が一旦止まり、reclaim で横断する」: 出荷構成ハーネス
    (seed 1234、`docs/playtest-2026-07-09-infinite/sim-m30-origin-k0.0015.txt`) で
    reach が 250.6 で Day 25〜45 停滞 (前線は岩場/荒地) → Day 50 に 332 へ跳ねて
    毒/森へ抜ける、Day 100 に荒地を再横断中 — 「伸びて→枯れて→別方向へ」の
    リズムが数値と前線バイオーム分布 (fb) に出ている。span は Day 100 で 693
    (K=0 バイオームのみ: 720、M29-B の一様地形: 660 頭打ち)。
  - 距離 upkeep の fatigue 実測差: 母体近傍 0.25 vs 前線遠端 0.32〜0.36 (+30〜40%)。
    対照 K=0.003 は勾配 3倍 (0.13/0.41) だが reach が Day 50 以降 543 で凍結 =
    強すぎて遠征が伸びない → 0.0015 を採用。
  - 既存6ステージ: 全パラメタ既定 0/'hops' で bit 一致 (sim 111 テスト全通過、
    distance-upkeep.test.ts の明示既定値テストで固定)。
- **実装メモ / 既知の限界**:
  - 俯瞰/ミニマップのバイオーム色: チャンク要約に `toxinAvg` を追加し
    (`summarizeChunks`、M28-A テスト追従)、`chunkTileColor` が毒の紫を混ぜる。
    森=明るい苔 / 荒地=暗い苔 / 岩場=無彩色 / 水辺=青 / 毒=紫で、既存4軸
    (nutrientAvg/obstacleDensity/hasWater + toxinAvg) にバイオームが自然に映る。
    窓内は環境フィールド (乾燥・毒・水域・湿潤) が既存の絵 (M10 毒オーバーレイ、
    M22 水場、M24 生態小物) に自動で反映される。M20 タイル資産のバイオーム別
    割り当ては見送り (色調の差で判別可、必要なら M32 で再訪)。
  - バイオームで「全チャンク必ず餌1パッチ」が崩れたため、span の絶対値は M29-B
    比でやや控えめ (Day 40: 465 vs 613)。代わりに成長が地理に律動する (痩せ地で
    減速→豊かな地帯で加速)。完全停止はしない (Day 100 まで単調増加を確認)。
  - 荒地の「飛び石」(30% チャンクに小パッチ) は詰み防止の要。0% にすると連続
    した荒地帯で前線が壊死しうる (未検証のまま 30% を採用、体感調整は M31/M32)。
  - K=0.0015 でも tick コストは M29-B と同等 (Day 100 で 16.5ms/tick、×24 予算内)。
    距離キャッシュは 60 tick ごとの全ノード走査 O(V) で、BFS 便乗より単純。
  - 採種 boost の「個体の位置」は最遠前線ノードで代表させた (コロニー全体が
    1個体という現行モデルでの近似)。株単位の採種座標を持つなら M32 で再定義。
  - **実プレイ (×24、5.5分、headless Chromium、`browser-hud-samples-m30.txt`)**:
    Day 47 到達、pace 6.6〜11.4 日/分 (M29-B の 6.6〜10.8 と同等、劣化なし)、
    console エラー 0。俯瞰 (after-m30-zoomout.png) で森の明るい苔 / 荒地の暗い
    タイル / 岩場の無彩色 / 毒の窪地の紫が見分けられる。到達距離は 60→302 と
    段階的に伸び、Day 22〜47 は 302 で停滞しつつ総面積/リンクは増え続けた
    (= 前線が地帯境界で足踏みしながら後背が肥える「地理のリズム」。ハーネス
    では同様の停滞が Day 50 に 332→483 へ抜けることを確認済み)。
  - 荒地の停滞は 20〜25 日続くことがある。プレイヤー体感で長すぎるようなら、
    飛び石の頻度 (30%) か barrens の面積比 (FERTILITY_CUT) で調整する。

### M31 — 介入カーブ: 序盤は手で、後半は環境を買う

> V7/V8。クリッカー系の「手動 → 自動化/大局化」の弧を、モルフォの文法
> (直接命令はできない、環境を整えるだけ) のまま作る。

> **進捗 (2026-07-11、完了)**: 極小スタート (発芽ラッチ) → 見守り収入 →
> 大局介入 (マクロツール4種、俯瞰専用) → UI 整合の順で実装。sim ローカル則は
> 無改修 (`Environment.placeX()` 呼び出しのみ)。実プレイ実測 (`vite preview` +
> headless Chromium): 無介入では Day 4 相当までゆっくり (edges 191)、餌を
> 置いた別セッションは同程度の日数で edges 155・面積 4,796m² まで到達し
> 進化の記録に「発芽 — 網の成長が勢いを増した」が Day 0 に記録される。
> 見守り放置 (×24, 3分) で 🪙120→1,062・🍃20→74 (Day 0→152、無操作)。俯瞰で
> マクロツール4種 (雨季/肥沃な帯/地熱↑/地熱↓) を購入・発動でき、効果範囲の
> 円/帯と残り日数が大局レイヤーに描かれる。証跡は
> `docs/playtest-2026-07-09-infinite/` (after-m31-*.png、
> sim-m31-seedling-contrast.txt、browser-watch-income-m31.txt)。
> sim 111 + web 400 テスト (新規 macro-tools/watch-income/wildland-m31 の
> 3ファイル)・e2e 56件 (53+新規3) 全通過・回帰ゼロ。

- [x] **極小スタート** — 原野の開始を「胞子1個 (source 1 + 枝2本) + 最初の
  餌パッチ1つ」にした (`stages.ts`/`game.ts`)。既存6ステージ (皿は source 3
  ×枝6本など) は完全に不変。母体の森 3×3 チャンクの残り8枚も「40%で小さな
  飛び石1つ」まで薄くした (荒地の飛び石と同じ詰み防止の考え方 — 見守り
  放置でも数日かけて自力で発芽できる)。
  - **発芽ラッチ (新規の仕組み)**: 素朴に成長パラメタを下げるだけでは
    「youth 駆動の自走拡大」(growth.ts の候補が新芽ボーナスを持つ) が抑え
    切れず、餌の有無に関わらずほぼ同じ速さで伸びてしまうことをハーネス
    対照実験で確認した。そこで sim パラメタではなく「胞子は発芽前の厳しい
    パラメタで生まれ、しきい値を超えたら出荷パラメタへ戻る」という
    **ラッチ** を web 側 (`Game`) に導入: 発芽前は
    `growthProbability 0.85→0.4 / growthActivityThreshold 0.20→0.35 /
    wNutrient 0.3→0.9 (餌への感受性を強化) / maxDegree 5→3`。sink (食料に
    根を張った終端) を半径6でクラスタリングして2クラスタ以上 (=「ふたつ目
    の餌場」) に届いたら発芽し、出荷パラメタへ戻すと同時に母体から新芽
    4本を吹かせる (`seedSource` を発芽時にもう一度呼ぶ)。新芽が無いと
    「全先端が餌に根を張って (sink化して) 伸びなくなる」満腹凍結が起きる
    ことも対照実験で確認済み — 発芽後も「成長できる先端が1つも無い」状態を
    日次で検出したら母体から2本を追加で吹かせる保険 (`ensureWildlandBud`、
    網が育ったら (edges≥300) 判定ごとスキップ) を入れた。判定はすべて
    state のみの純粋な派生 (rng 不使用) で決定論を乱さない。
  - **対照実験の数字** (`sim-m31-seedling-contrast.txt`、3 seed × Day 1〜3、
    無介入 vs 胞子近くに餌2つ+水1つを手で置く):
    seed 1234 Day1 edges 17→39・Day3 50→111 / seed 42 Day1 9→31・Day3
    19→64 / seed 7 Day1 10→40・Day3 30→56 — 全 seed で「餌を置く」が
    Day1〜3 とも明確に上回る。無介入でも完全停止はしない (見守り放置の
    救済である飛び石が効いている) が、伸びは控えめ。
- [x] **見守り収入** (`watch-income.ts`、純粋関数 + vitest) — 原野の見守り
  モードに3種の収入を追加: **日次基本給** 🪙6/日 (デイループの `10+
  growthBonus` より控えめ、見守りモード中の日境界のみ)、**新チャンク到達**
  🪙2/枚 (`exploredChunks` の増分)、**新バイオーム発見** 🍃10/種
  (`worldOverview.chunks` を走査、5種中の未発見種)。**二重付与の防ぎ方**:
  日次基本給は「見守りモード中に日境界を跨いだとき」だけ払う
  (`main.ts` の `!dayLoopMode` 判定) — デイループの日次収入 (`showDayResult`) は
  「デイループモード中に日を消化したとき」だけ払われるため、1つの日境界は
  どちらか一方のモードでしか跨げず重複しない。新チャンク/新バイオームは
  初回観測をベースライン化 (セッション開始時の3×3チャンクと母体の森に
  払わない) してから増分だけ払う。reason 文字列は日本語
  (「Day n を見守った」「新しい土地に到達」「新しいバイオーム「◯◯」を
  発見」)。実測: ×24・3分の見守り放置 (無操作) で 🪙120→1,062・🍃20→74
  (Day 0→152、`browser-watch-income-m31.txt`) — M31 前は見守りで何日回して
  も残高が1も増えなかった (ROADMAP.md V8)。
- [x] **大局介入 (環境を買う)** (`macro-tools.ts` が純粋な仕様、`Game.
  applyMacro()`/`maintainMacroEffects()` が sim への配線) — ズームアウト
  (原野で zoom<1 の俯瞰) 中だけ使えるマクロツール4種 (雨季/肥沃な帯/
  地熱↑/地熱↓)。効果は `ChunkedGridEnvironment.placeX()` へ実座標で直接
  書くだけで sim のローカル則は無改修。「一定期間」は web 側
  (`ActiveMacroEffect[]`) が適用の記録を持ち、`tick()` が発動 tick 基準の
  一定間隔 (`shouldReapplyMacro`) で薄く再適用して自然減衰に抗する — sim に
  期限の概念は持ち込まない。仕様:
  | ツール | 価格 | 効果半径/長さ | 期間 | 再適用間隔 | 初回量/維持量 |
  | --- | --- | --- | --- | --- | --- |
  | 雨季を呼ぶ | 🪙20 | 半径120 (窓100の1.2倍、数チャンク) | 5日 | 60tick | 湿度+0.35/+0.08 |
  | 肥沃な帯 | 🪙60 | 半幅12・全長192 (餌パッチ9個、チャンク4枚ぶん) | 6日 | 1日 | 栄養0.8 (>foodReachThreshold 0.55 = 根を張れる)/+0.15 |
  | 地熱↑ | 🍃30 | 半径120 | 5日 | 60tick | 温度+0.12/+0.05 |
  | 地熱↓ | 🍃30 | 半径120 | 5日 | 60tick | 温度−0.12/−0.05 |
  価格は窓内ブラシ (🪙1〜3・🍃2〜3) の20〜30倍。最安の「雨季」🪙20 は
  wallet.ts の詰み防止下限 (`SIZUKU_FLOOR=20`) と同額 — 残高が尽きても自動
  回復だけで必ずまた1つ買える。適用は `wakeDormantArea` (M29) を必ず経由
  するので休眠領域にも効く。既存6ステージでは `applyMacro` は no-op
  (`stage.infinite` ガード)、`macroEffects` ビューも `undefined`。
- [x] **介入の効果が読める** — `render.ts` の `drawMacroEffects()` が俯瞰
  レイヤーへ、円 (雨季/地熱) または帯 (肥沃な帯、ドラッグ方向へ点線+塗り)
  の半透明オーバーレイ + 「ツール名 あとN日」ラベルを描く。実プレイ確認
  (`after-m31-macro-effects.png`): 「雨季を呼ぶ」中心のラベルと「肥沃な帯」
  の東方向の帯 (緑の塗り+点線輪郭) が同時に見え、「最近の出来事」にも
  「大局介入「◯◯」を発動した」が記録される。
- [x] **UI の整合** — 俯瞰 (zoom<1、`overviewToolsOn` フラグ) では
  `body.overview-tools` を付け CSS で窓内ブラシを隠しマクロツールのみ表示
  (誤爆防止、`style.css`)。窓 (zoom≥1) では従来ブラシのみ。マクロツールは
  press→release のドラッグで発動 (連続適用しない = 数十倍価格の誤連打
  防止)、モバイルの下部ツールバーにも同じボタン群を複製済みで既存のタッチ
  経路 (タップ猶予/ピンチ判定) に乗る。キャンバス下のヒント行が状況に応じて
  切り替わる: 通常「クリックで配置 / …」→ 原野の窓「ズームアウトすると
  大局の道具が使える」→ 俯瞰「大局の道具: クリックで発動 / 「肥沃な帯」は
  ドラッグで方向指定」。
- **受け入れ基準 → 達成を実測**: 新規セッションの Day 0〜3 は無介入だと
  伸びが控えめで、餌を置くと3 seed 全てで明確に上回る (上の対照実験)。
  見守りモードで放置すると通貨が増える (実プレイ実測 + e2e「見守りモードで
  放置すると通貨 (🪙) が増える」)。大局ツール4種が原野の俯瞰で機能し
  (e2e「俯瞰でマクロツールへ切り替わり、購入・適用で残高が減って効果が
  見える」)、既存6ステージには出ない (`test/wildland-m31.test.ts` の
  no-op テスト)。
- **実装メモ / 既知の限界**:
  - 手撒きの餌 (窓内ブラシ) は amount 0.7 のまま不変 (foodReachThreshold
    0.55 を超える = 根を張れる「食事」)。地形の初期餌パッチと同じ量なので、
    プレイヤーが置いた餌も発芽の引き金になれる。
  - `wildland-dormancy.test.ts` の既存テスト1件が発芽ラッチによる序盤成長の
    鈍化で tick 1200 (Day 5) では休眠/evict がまだ十分育っていないことがあり
    (dormantCells が一時的に0へ落ち込む瞬間に当たっていた)、tick 1800
    (Day 7.5) へ調整して追従した (seed 1234 で dormantCells 19・
    evictedChunks 4 と余裕を持って通る)。
  - ブラウザ実測の「無介入 vs 餌を置く」screenshot 対 (`after-m31-noop-
    day.png` / `after-m31-fed-day.png`) は seed を固定していない (2つの別
    ブラウザコンテキストがそれぞれ既定のランダム seed で起動する) ため、
    厳密な対照ではなく「実プレイの絵として発芽と成長が機能している」ことの
    確認に留める。seed を揃えた厳密な対照は sim ハーネス
    (`sim-m31-seedling-contrast.txt`) 側の責務として分離した。
  - 見守り収入の新バイオーム発見 🍃10 は全5種 (母体の森=ベースライン込み)
    なので生涯最大 🍃40。長時間セッションでの主収入源は日次基本給と新チャンク
    到達 (どちらも上限なく伸び続ける)。

### M32 — 原野を本編に: 目標系と記録系の全面接続

> V9。遊びの器 (クエスト/時代/チャレンジ/図鑑/記録) を原野仕様にし、
> 有界6ステージを「チュートリアル & チャレンジ集」として位置づけ直す。

- [ ] **原野の時代** — 拠点数ベースの既存条件をやめ、無限世界の節目で刻む:
  到達距離・探索チャンク数・発見バイオーム数・世代数など。「時代が進むたびに世界の
  スケール感が一段上がる」(モックアップ①の「時代: 胞子期 → 変形体期」リングの原野版)。
  時代 ETA の乱高下 (上の違和感2つ目) もここで平滑化する。
- [ ] **原野のクエストとチャレンジ** — 「全拠点接続 100%/探索 0% 固定」を撤去し、
  原野専用のメインクエスト (例:「母体から 500 先へ到達しよう」「3つのバイオームに根を張ろう」) と
  期限付きでない反復チャレンジ (例:「今日中に新チャンク5つ」) に置き換える。
- [ ] **記録系の接続** — 図鑑/系統樹/実績/探索レポートに原野個体を収録する
  (`STAGE_ORDER` へ追加、図鑑は37枠を拡張)。Undo/消去ツールをチャンク系フィールドでも
  効くようにする (チャンク側 `recordBefore` 相当の差分記録)。
- [ ] **オンボーディングの動線** — 新規プレイヤーの推奨動線を「皿 (チュートリアル) → 原野 (本編)」
  として明示する (ステージ選択の並びと説明文、皿の成熟期到達で原野を勧めるコーチマーク)。
- **受け入れ基準**: 原野で時代が実際に進む (e2e)。開始直後に自動達成されるチャレンジが無い。
  図鑑に原野個体が収録される。既存6ステージの e2e 全通過。

## アーキテクチャ方針

- **原野が本編** (2026-07-09 更新) — 有界6ステージは完成品として凍結し、決定論テストと e2e で
  回帰ゼロを守る。新しい遊びはすべて原野に足す。sim の新パラメタは必ず「既定 0 = 無効」で入れ、
  既存ステージの bit 一致を保つ (M25 の `forageReclaimThreshold` 方式)。
- **sim はステートレスに保つ** — UI 側は SimState のスナップショットを読むだけ。書き込みは
  `Environment.placeX()` 経由に限定。M29 (休眠)・M30 (距離 upkeep) は sim のローカル則に手が入る —
  着手前に対照実験ハーネス (第5回検証の `sim-longrun` 方式) で before を採ってから変更する。
- **描画はラッパー** — `@morpho/web` は sim を import するだけで、sim 側に描画依存を持ち込まない。
  M28 の「チャンク要約」も sim 側は数値の集計だけを返し、絵は web 側で決める。
- **アセットは任意** — テクスチャ/スプライトが無くても手続き描画でゲームが成立するフォールバックを保つ。
- **時間スケールは UI に切り出す** — sim は「1 tick」しか知らない。日数・時代・ローカルタイムは web 側の派生量。
- **ゲームデータは web 側 + マイグレーション必須** — localStorage (画像のみ IndexedDB)。スキーマ変更は必ず `vN → vN+1`。
- **ゲームロジックは純粋関数 + vitest** — チャンク要約・距離勾配・収入計算も DOM から切り離してテストしてから配線。
- **e2e を壊す変更は同じ PR で追従する**。
- **「見える」ことを e2e で検査する** — checksum ではなく実表示 (boundingBox / ピクセル存在 / 輝度・分散) を検査する。
- **リポジトリルートは pnpm workspaces** — install は必ずリポジトリルートで。
- **性能は計測してから** — 描画は `?debug` の perf HUD で 3ms/frame 以内。sim は「実効 日/分」(M29) で
  長時間の劣化を検査する。

## 動かし方

```sh
pnpm install      # リポジトリルートで一度だけ
cd packages/web
pnpm dev
# → http://localhost:5173 で遊べる
```

デプロイ版: main へのマージで GitHub Pages へ自動デプロイ (`.github/workflows/pages.yml`)。

## 実プレイ検証の証跡

- `docs/playtest-2026-07-09-infinite/` — **第5回 (ビジョンとの乖離、M28〜M32 の before 基準)**。
  原野 Day 1 / Day 16 / ズームアウト最小 (Day 16 と同一画面 = 俯瞰不在の証跡) / 皿 Day 0、
  ブラウザ HUD の15秒おきサンプリング生データ、sim 直接駆動 Day 100 のサンプリング生データ。
- `docs/playtest-2026-07-06-visuals/` — M20〜M24 の after (絵作り)。
- `docs/playtest-2026-07-05-visuals/` — 第3回 (絵作りの乖離) + 第4回 (Day 24 完全凍結)。M20〜M27 の before 基準。
- `docs/playtest-2026-07-05/` — 第2回 (遊び方の検証)。
