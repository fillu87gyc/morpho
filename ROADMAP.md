# モルフォ — Web ゲーム化ロードマップ

> 「制御する」のではなく、「条件を整え、振る舞いを引き出す」。
> モックアップで描いた粘菌育成体験へ、既存の `@morpho/sim` を段階的に拡張する計画。

## 現在地

- **`@morpho/sim`** — 粘菌のローカル則 (グラフ成長 / Biomass膜 / Activity場 / 温度・毒素・水域を含む環境ツール) は完成済み。決定論テスト + ベンチ (`pnpm --filter @morpho/sim run bench`) で守られている。
- **`@morpho/web`** — M0〜M15.5 で「モックアップの遊び方」は一通り成立した: デイループ (仕込む→委ねる→受け取る)、環境ツール9種+Undo、3通貨経済とゆるいデイリー、個体の同定 (名前/★/特性チップ/追跡)、図鑑37枠と分岐する系統樹、大陸ステージと条件達成型の時代、モバイル縦UIとオンボーディング、PWA、GitHub Pages 自動デプロイ。**今すぐ遊べる状態にある。**
- **実プレイ検証 第2回 (2026-07-05)** — headless Chromium で新規プレイヤー導線 (オンボーディング → ツール6回配置 → 皿を Day 20 まで育成 → デイループ1周 → 大陸 Day 21 → モバイル縦)。コンソールエラー0。証跡スクリーンショットは `docs/playtest-2026-07-05/` に保存した。発見と処置:
  - ✅ **M15.5 のバランス再調整は機能している** — 皿で Day 20 ちょうどに変形体期 (目標帯 Day 20〜30)、メインクエストの探索も 94% で残る。「初日で全部終わる」状態は解消された。
  - 🔧 **モバイルでキャンバスが 0×0 になり何も見えない** (致命的リグレッション) — M15.5 の `fitCanvas()` 測定前ゼロ化が、行高がコンテンツ由来のモバイル縦レイアウトでは wrap の高さごと 0 に潰し、そのまま復元されない。**このセッションで修正済み** (高さが取れないときは幅を一辺にしてビューポート残り高さでクランプ + e2e に「キャンバスの実表示サイズ > 200px」検査を追加)。教訓: canvasChecksum≠0 (バッキングストアに描画がある) は「プレイヤーに見えている」を意味しない。
  - 📷 **日次結果モーダルのサムネイルが開いた瞬間だけ壊れ画像アイコン** — `renderThumbnail()` の resolve 前にモーダルが開くため src 未設定の一瞬が見える (2.5秒後には正常を確認)。プレースホルダ/フェードインで隠す → M19。
- **M15.7 完了 (2026-07-05)** — ⏱ 「1日」が実時間0.64秒しかない問題 (`TICK_INTERVAL_MS=16`×`TICKS_PER_DAY=40` → ×1で1日≈0.64秒) を、TICKS_PER_DAY を変えずに sim-worker.ts の tick 生成を実時間ベースの分数蓄積へ変更して解消 (×1で1日≈120秒を実測)。⚖ チャレンジ「最短でつなぐ」も Day2以内へ引き締め。詳細は M15.7 セクション参照。
- **M16.5 完了 (2026-07-05)** — 🎨 絵作りの最大の印象差 (ほぼ黒の虚無+カーキのブロブ+赤茶の線) を、地形ベース色の常時不透明化・ハッシュノイズ粒感・ネットワークの2パス発光・プラズマ膜のリム発光で解消。詳細・before/after 証跡は M16.5 セクションと `docs/art-direction.md` を参照。
- **M16 完了 (2026-07-05)** — 時代の残り時間予測 (`estimateEraEta`)・ミニマップ地形焼き込み・速度4段ボタン (⏸▶▶▶▶▶▶)・ズームスライダーを実装。詳細は M16 セクション参照。
- **残っているもの** — 差分は「機能の穴」ではなく **記録と演出の仕上げ**。実施順は **M17 → M18 → M19**。

## 完了済みマイルストーン (M0〜M15.5) — 要約

> 詳細な設計メモは各実装ファイル冒頭のコメントと git 履歴 (各マイルストーンのコミット/PR) に残っている。ここでは「何がどこにあるか」だけを引けるようにする。

| M | 一行要約 | 主なコード |
| --- | --- | --- |
| **M0 土台** | Vite + TS の `packages/web`。Canvas 描画、再生/一時停止/速度スライダー (×1〜24)、ツール5種、HUD、リセット、CI typecheck | `web/src/main.ts` `render.ts` |
| **M1 観察の快適化** | 差分再描画で 60fps、sim を Web Worker へ分離、成長タイムライン、環境ヒートマップ、ズーム/パン | `field-diff.ts` `sim-worker.ts` `game-proxy.ts` `timeline.ts` `camera.ts` |
| **M1.5 土地** | バイオーム5種をシードから手続き生成 + 地形テクスチャ | `web/src/game.ts` |
| **M2 個性** | seed から決定的な `Genome`、6軸個性 + 5タイプ判定、図鑑、個体ビュー | `sim/graph/genome.ts` `individuality.ts` `web/src/encyclopedia.ts` |
| **M3 環境の物語** | ステージ5種 (皿/洞窟/砂漠/都市跡/湿地)、環境の自然減衰 | `web/src/stages.ts` `sim/env/environment.ts` |
| **M4 ゆるい目標** | メインクエスト・デイリーチャレンジ・実績・ベスト記録 | `quests.ts` `challenges.ts` `achievements.ts` `scoreboard.ts` |
| **M5 子孫** | 種の採取 → 変異付き継承、過酷なステージほど変異幅が大きい | `sim/graph/genome.ts` `web/src/lineage.ts` |
| **M6 World View** | 複数コロニー、ミニマップ、Union-Find のネットワーク統合判定 | `colony-networks.ts` `minimap.ts` |
| **M7 仕上げ** | 環境音、アルバム (IndexedDB)、PWA、タッチ+ピンチ、GitHub Pages 自動デプロイ | `ambient.ts` `album.ts` `pinch.ts` `.github/workflows/pages.yml` |
| **M8 速度** | tick 0.5〜0.7ms への最適化、時間予算スケジューラ、早送りモード、perf HUD (`?debug`)、再現ベンチ | `sim/graph/*` `tick-scheduler.ts` `perf-hud.ts` |
| **M9 デイループ** | 「仕込む→委ねる→受け取る」の1日フェーズ。日境界で自動停止、結果パネルに3軸スコアの前日比Δ。既定は見守り、ヘッダのトグルで切替 | `day-loop.ts` `day-report.ts` `worker-protocol.ts` |
| **M10 環境システム** | sim に温度・毒素フィールド、Genome に耐性2遺伝子。加温/冷却/毒素/乾燥ツールと stroke 単位 Undo、環境バランスHUDの実測値化 | `sim/env/environment.ts` `sim/graph/life.ts` `web/src/undo.ts` |
| **M11 経済/デイリー** | 3通貨 (🪙🍃🍄) の Wallet (詰み防止の自動回復付き)、ツールコスト、配置カウント型デイリー3件/日、チャレンジ3種の常時一覧化 | `wallet.ts` `dailies.ts` `challenges.ts` |
| **M12 個体の物語** | 通し番号 + リネーム、★評価、特性チップ、育った環境チップ、カメラ追従、出来事イベント。**残: 突然変異演出・特徴文・統計グラフ・エリア注視 → M17** | `identity.ts` `trait-labels.ts` `sim/graph/growth.ts` |
| **M13 収集/記録** | 図鑑をタイプ×ステージ+特殊条件の37枠カタログへ、系統樹の分岐ツリー化、実績12種。**残: タブUI・探索レポート・系統樹サムネ → M18** | `catalogue.ts` `catalogue-thumbs.ts` `lineage.ts` |
| **M14 大陸と時代** | 大陸ステージ (水域+拠点の手続き生成+専用クエスト)、時代の条件達成型化、ローカルタイム+夜間トーン。**残: 残り時間予測・ミニマップ地形 → M16** | `era.ts` `daytime.ts` `stages.ts` |
| **M15 モバイルUI** | ハンバーガー☰+全画面ドロワー+下部ツールバー (900px以下)、初回3ステップのコーチマーク | `onboarding.ts` `web/index.html` `style.css` |
| **M15.5 実プレイ緊急修正** | 時代に day ゲート (拡散8/変形体20/成熟40)・クエスト/チャレンジ引き締め、モバイル2段ヘッダ+水平オーバーフロー e2e、モード切替の縦書き潰れ修正、見守りモードでも DayRecord 記録。**副作用: fitCanvas ゼロ化がモバイルでキャンバス 0×0 を生む → 2026-07-05 に修正・e2e 追加済み** | `era.ts` `quests.ts` `challenges.ts` `style.css` `main.ts` `mobile.spec.ts` |

**速度の現状**: スライダー上限 ×24 に対し通常モードで実効 ×4〜7、早送り (⏩) で ×10 前後。残るボトルネックは sim の tick コスト (エッジ数に比例) のみ。ベースラインは `sim/scripts/bench.ts` で再現できる。

## 残ギャップ一覧 (2026-07-05 第2回改訂)

モックアップ2枚との差分のうち **未完了のものだけ**。番号は旧棚卸し表のまま、P で始まる番号は実プレイ検証での追加発見。

| # | モックアップの要素 | 現状 | 行き先 |
| --- | --- | --- | --- |
| P8 | 日次結果の「今日の姿」サムネイル | モーダルが開いた瞬間は壊れ画像アイコン (生成前の src 未設定が見える) | **M19** |
| 18 | 時代HUD「次の時代まで あと 12:34」の残り時間予測 | 進捗リングのみ (`era.ts`) | **M16** |
| 36 | ミニマップに地形 (水域・バイオーム) を描く | コロニー点と視野矩形のみ (証跡: `continent-minimap.png`) | **M16** |
| 21 | 速度コントロール ⏸ ▶ ▶▶ ▶▶▶ の4段ボタン | スライダー + ⏸ + ⏩ | **M16** |
| 37 | ズームスライダー (−/+) UI | ホイール/ピンチのみ | **M16** |
| 27 | 進化の記録「個体が突然変異 — 光走性が上昇」「新しい形質を獲得」 | 時代の節目と迂回/胞子イベントのみ | **M17** |
| 28 | 最近の出来事に時刻 (「12:15 栄養を発見」) | イベント文言と Day 表示のみ | **M17** |
| 29 | 「このエリアを注視中」(視野内の出来事だけを見る) | 出来事が座標なしの文字列 | **M17** |
| 8 | 個体カードのタイプ別特徴文 | タイプ名チップのみ | **M17** |
| 33 | 個体ビューの 📊 統計グラフ | 撮影📷と♥のみ実装済み | **M17** |
| 7 | 収集・記録のタブ (図鑑 / マップ / メモ) | 別カードに散在、メモは無い | **M18** |
| 40 | 探索レポート | 未着手 | **M18** |
| 10 | 系統樹ノードのサムネイル | 分岐ツリーは済み、ノードは文字のみ | **M18** |
| — | 通貨増減の「+n」フロート演出 | 簡易フラッシュのみ | **M19** |
| — | ドロワーのスライドアニメーション / ボトムシート化 | display 即時切替 | **M19** |
| — | 実機QA (iOS Safari / Android Chrome の safe-area・ピンチ・PWA) | この開発環境では検証不能 | **M19** (手順書化) |
| 17 | 4通貨目 (大陸探索専用) | 3通貨で経済は成立 | **見送り継続** (M19 で最終判定) |
| — | 昼夜の brightness を sim の成長応答に効かせる | 夜間トーンのみ | **見送り継続** (毎tick全面書換が高コスト) |

## これからのマイルストーン (M17〜M19)

実施順は **M17 → M18 → M19** (M15.7・M16.5・M16 は完了済み)。並べ方の意図:

1. **M17** — 出来事の構造化という下地工事を含み、M18 の探索レポートがそれに依存する。
2. **M18** — 記録系の集大成で、M17 のグラフ部品を再利用する。
3. **M19** — 全機能が出揃ってから初めて意味を持つ最終バランス調整と実機QA・リリース。

各マイルストーンは従来どおり「純粋関数 + vitest → DOM 配線 → e2e」の順で作る。規模の目安: S=半日、M=1〜2日、L=3日〜。

### M15.7 — 時間スケールの再設計: 「1日」を観察に値する長さへ ✅ 完了 (2026-07-05)

> 実測: `TICK_INTERVAL_MS = 16` (sim-worker.ts) × `TICKS_PER_DAY = 40` (day-loop.ts) → ×1 で 1日 ≈ **0.64秒**。モックアップ①の観察画面は「育成中… 02:34」— 数分単位で委ねる体験を描いている。

**実装した設計 (検討した「TICKS_PER_DAY を増やす」案からの変更点):**
- **TICKS_PER_DAY は 40 のまま変更しなかった。** 変えると era/quests/challenges/wallet の「1日あたりの成長量」が変わり全部の再調整が要るが、日ゲート系 (era.ts の `*_MIN_DAY`) は「経過日数」で判定するため、tick数を増やしても生の成長条件はより早い日数で満たされるだけで day ゲートの拘束力は変わらない一方、% ベースのクエストやチャレンジの「Day N 以内」は影響を受ける — リスクの割に得るものが少ないと判断し見送った。
- 代わりに **sim-worker.ts の tick 生成を「実時間ベースの分数蓄積」に変更**: ×1 における1日の実時間長 (秒) を `secondsPerDayAtSpeed1` (既定 120秒) として定義し、フレーム経過時間 × (TICKS_PER_DAY/secondsPerDayAtSpeed1) を `demandAccumulator` に貯めて整数分だけ `TickScheduler` に渡す (端数のまま渡すと debt/estTickMs 計算が壊れるため、端数保持はここでの責務にした)。早送りモードの `FAST_FORWARD_RATIO` は不要になった (実時間ベースなのでループ間隔に依存しない)。
- 実測: ×1 で Day 1 到達に **120.9秒** (目標2分にほぼ一致)。副次効果として、旧来の「実効速度 ×4〜7 の CPU 律速」は事実上解消された (新しい目標 ticks/秒が ×24 でも 6.4/秒程度で、tick コスト 0.5〜0.7ms の予算に対して大きく余裕がある)。
- **インターポレーションは追加しなかった** — tick 間隔 (~3秒) でのチェックサム変化を目視確認し、初期段階 (ノード数が少ない) では「静かに育つ」印象で違和感がないと判断。ノードが多い後半で気になるようなら追加を検討 (未着手のフォローアップ)。
- **チャレンジ「最短でつなぐ」を Day 2 以内へ引き締めた** (旧 Day 6。受動プレイでの実測 Day 3 達成を下回るライン)。
- **e2e 時間短縮フック**: `e2e/fixtures.ts` を新設し、全 e2e スペックの import を `@playwright/test` からここへ切り替え。`addInitScript` で `morpho.e2eDayMs.v1` (既定 500ms/日) を localStorage に積み、`main.ts` が起動時に読んで Worker へ `setSecondsPerDay` を送る (Worker はページの localStorage を直接読めないため main.ts が中継)。手動デバッグ用に `?dayms=<ms>` クエリパラメータも用意。
- **受け入れ基準**: ✅ ×1 で 1日が実測120秒。✅ 既存 e2e 37本全通過 (時間短縮フック経由)。✅ typecheck/vitest 211件通過。

### M16.5 — ビジュアルパス: 「自然のフィールドに粘菌が這う」画へ ✅ 完了 (2026-07-05)

> **最大の印象差**。モックアップは「苔むした岩肌・木漏れ日の中を、金色に光る網が這う」— 現状は「ほぼ黒の虚無に、カーキの均一ブロブと赤茶の実線」。証跡 (before): `docs/playtest-2026-07-05/petri-day8-field.png` / `continent-day21-field.png`。証跡 (after): `petri-m165-after.png` / `continent-m165-after.png`。詳細は `docs/art-direction.md`。

- [x] **基準画の固定** — `docs/art-direction.md` に目標のルックと実装ノートを記載。
- [x] **地形の露出底上げ** — `STAGE_BG`/`STAGE_ROCK_COLOR` を大幅増光。地形の合成方式を「近黒背景 + 淡い差分」から「`TERRAIN_TONE` (shadow⇄sunlit) による常時不透明のベース色」へ変更。`env.brightness` を「日当たり」として視覚的に転用し、明暗を表現。
- [x] **地形テクスチャの強化** — `hashNoise(x,y)` によるセル単位の決定的な粒感 (苔・岩のまだら) を追加。乾湿による色味の寄せも合わせて調整。
- [x] **ネットワークの発光表現** — `drawEdges` を2パス化 (`globalCompositeOperation: 'lighter'` + `shadowBlur` のぼかしグロー → crisp な芯)。`TUBE_LIGHT/DARK` を金色系統へ変更。
- [x] **Biomass 膜の見た目** — `PLASMA_LOW/MID/HI` を金色 (#E8B84B系) へ、`PLASMA_RIM` で成長前線 (v の低い帯域) を山型カーブで一段明るく光らせるリム発光を追加。
- [x] **性能ガード** — `?debug` で draw **2.10ms** (予算3ms以内) を確認。`e2e/visual.spec.ts` (新規) に「黒潰れ再発検知」を追加 — 完全なピクセル差分スナップショットは seed が `Math.random()` 由来で決定的な基準画像を作れないため断念し、代わりに皿/大陸それぞれで平均輝度が閾値 (>35) を超えることを検査する。
- **受け入れ基準**: ✅ before/after のスクリーンショットで「金色に光る網が苔むした地面を這っている」に見える (目視確認済み)。✅ `?debug` で 3ms/frame 以内。✅ 既存 e2e 39本 (新規2本含む) 全通過。

### M16 — 世界の実感: 時代予測・ミニマップ地形・速度/ズームUI ✅ 完了 (2026-07-05)

> World View モックアップの HUD を完成させた。すべて web 側の演出・UI で、sim・Worker プロトコルには触っていない。

- [x] **時代の残り時間予測「あと mm:ss」** — `era.ts` に `EraSample`/`estimateEraEta()` を追加。EWMA (α=0.35) で進捗速度を推定し `(1-progress)/rate` を ETA (ms) として返す。速度がほぼ0・履歴不足・後退中は `null` (UI は「次の時代まで —」)。サンプリングは `ui.ts` の `render()` に relay (2.5秒に1点、直近20点のリングバッファ、時代切替でリセット)。`formatMMSS` は `main.ts` → `day-loop.ts` へ移設して共通化。test 6件追加。
- [x] **ミニマップ地形描画** — `minimap.ts` に `bakeTerrain(stageId, env)` を追加。ステージ切替時に一度だけオフスクリーンへバイオーム基調色 (M16.5 の `TERRAIN_TONE` と揃えた簡易パレット) + 水域 + 障害物を焼き、毎フレームは「地形キャッシュ → コロニー点 → 視野矩形」の3層合成。e2e (`continent.spec.ts`) に水域ピクセル存在検査を追加。
- [x] **速度コントロールの4段ボタン化** — `index.html`/`ui.ts`/`main.ts`。⏸ ▶ ▶▶ ▶▶▶ (▶▶▶ のみ ×24+早送りON も同時に設定)。`aria-pressed` で選択状態を示し `morpho.speedPreset.v1` に永続化。スライダー+⏩トグルは廃止。
  - **e2e 追従**: `catalogue/continent/lineage-tree.spec.ts` の `setSpeedSlider(24)` ヘルパーをボタンクリックへ置き換え、`smoke.spec.ts`/`day-loop.spec.ts` の `#pause-toggle`/`#fast-forward`/`#speed-slider` 参照をすべて新ボタンへ移行。
  - **踏んだ地雷**: `day-loop.spec.ts` の「見守りで数日過ごしてからデイループへ」テストが、▶▶▶ (早送りON) だと `frame()` の重い処理 (DayRecord記録を含む) の呼び出し頻度が 60fps→10fps に落ち、e2e の短縮日長 (500ms/日) と組み合わさって複数日が1フレームに収まってしまいテストが不安定になった。早送りを伴わない ▶▶ (×8) に変更して解消 (本番の通常速度では日長がずっと長いため起きない)。
- [x] **ズームスライダー UI** — `index.html` に「− [スライダー] +」+「ピンチでズーム」ヒントをキャンバス右下に追加。`Camera.setZoomCentered()` (新規) で絶対値ズームの入口を用意し、ホイール/ピンチでの変化も毎フレーム双方向に同期する。
- **受け入れ基準**: ✅ 大陸ステージで「時代リング + あと mm:ss + 地形入りミニマップ」が同時に見える。✅ 4段ボタンで消化速度を切り替えても既存 e2e 39本 (更新分含む) 全通過。✅ typecheck/vitest 217件通過。

### M17 — 個体の物語の完成: 出来事の構造化・突然変異・特徴文・統計グラフ

> モックアップ②の「進化の記録」「このエリアを注視中」「📊」を成立させる。**中核は `recentEvents: string[]` を構造化イベントへ置き換える下地工事**で、M18 の探索レポートもこれに乗る。

- [ ] **出来事の構造化** — `web/src/world-events.ts` (新規) + `game.ts` 改修 (M)
  - `WorldEvent { id, day, tick, kind, text, x?, y? }` を定義し、`game.ts` の `recentEvents: string[]` と `eventToText()` を置き換える。sim の `SimEvent` は発生ノードを持つので、emit 時に座標を埋める。
  - 文言辞書は `event-text.ts` (新規) へ切り出し。高頻度イベントの間引きは現行ロジックを踏襲。保持数は 6 → 30。
  - **表示は「12:15 栄養を発見」形式** — `WorldEvent.tick` から `daytime.ts` の `localTimeFor()` で時刻を導出 (残ギャップ #28)。
  - test: 間引き・リングバッファ・座標の有無。既存 `sim/test/events.test.ts` は無改修で通ること。
- [ ] **「このエリアを注視中」トグル** — `main.ts` / `ui.ts` (S、上の構造化に依存)
  - 出来事カードに 👁 トグル。ON のとき camera の視野矩形と `WorldEvent.x/y` の交差でフィルタ。座標なしイベントは常に通す。
  - e2e: 「ズームインして注視ON → 視野外イベントが消える」。
- [ ] **プレイ中の突然変異・形質獲得イベント** — `day-report.ts` + `trait-labels.ts` の派生で実装 (M)
  - **sim には手を入れない**。日境界で前日の `DayRecord.traits` と比較し、|Δ| > 0.08 の軸があれば「個体が突然変異 — 探索性が上昇」を emit (1日最大1件、最大Δの軸)。`traitChipsFor()` の新出現チップで「新しい形質を獲得」も同様に。
  - 決定的なので vitest で「Δが閾値未満なら無音」「複数軸なら最大の1軸」「チップ出現の検知」を固定。
- [ ] **タイプ別特徴文** — `trait-labels.ts` に `typeDescriptionFor(type): string` (S)
  - 5タイプに1文ずつ (例: 太くつなぐ型 →「太い幹をつくり、安定したネットワークを好む。」)。個体ビューと図鑑ツールチップに表示。スナップショットテスト。
- [ ] **統計グラフ 📊** — `web/src/chart.ts` (新規) + 個体ビューにボタン追加 (M)
  - 依存ライブラリなしの canvas 折れ線チャート。入力は `DayReport` の直近60日。系列は 3軸スコア + 質量 (右軸)。`buildChartLayout()` (純粋関数) と `drawChart()` に分離し、前者を vitest で固定。
  - **M18 の探索レポートの部品になる** — 汎用の系列チャートとして書く。
- **依存**: M9 (`DayRecord`)、M12 (特性チップ)。sim・Worker プロトコル無改修。
- **受け入れ基準**: 数日回すと「進化の記録」に突然変異/形質獲得が現れる。注視ONで足元の出来事だけが流れ、出来事に時刻が付く。個体ビューに特徴文と 📊 グラフ。既存 e2e 全通過。

### M18 — 収集・記録ハブ: タブUI・メモ・探索レポート・系統樹サムネ

> モックアップ①の「収集・記録 (図鑑/マップ/メモ)」タブと②の「探索レポート」。散らばった記録系カードを1つの「ふりかえる場所」に束ねる。

- [ ] **「収集・記録」タブ付きパネル** — `index.html` / `ui.ts` / `style.css` (M)
  - 図鑑 / マップ (ミニマップ拡大版 + M16 の地形) / メモ の3タブを1カードに統合。既存 DOM をタブコンテンツへ移設し、描画・更新ロジックは無改修。タブ選択は `morpho.recordTab.v1` に永続化。モバイルではドロワー内で同じタブ。
  - **e2e 追従**: 図鑑を開く導線がタブ経由に変わる。
- [ ] **メモ** — `web/src/notes.ts` (新規) (S)
  - Day 付き自由テキスト。追加/削除のみ。「今日の成長を貼る」で当日の `DayRecord` サマリを定型文挿入。localStorage `morpho.notes.v1`、直近100件。
- [ ] **時代の年表の記録** — `main.ts` の時代切替検知に1行 (S)
  - `morpho.eraHistory.v1` に `{ era, day }` を追記。**単独で先行マージしてよい** (記録は早く始めるほど価値がある。M15.7/M19 のバランス実測データにもなる)。
- [ ] **探索レポート** — `web/src/report.ts` (新規) + モーダル (M)
  - ① 総質量・総面積の推移チャート (M17 の `chart.ts` 再利用) ② 時代の年表 ③ 数値サマリ (拠点接続/探索率/発見種数/実績) ④「画像で保存」→ アルバムへ。
  - `buildReport(sources): ReportData` を純粋関数で vitest、canvas 合成は e2e スモークのみ。
- [ ] **系統樹ノードのサムネイル** — `lineage.ts` + `catalogue-thumbs.ts` (S)
  - 採種時に `renderThumbnail()` を撮り IndexedDB (store 分離) に保存。ノードに 40px サムネ、無いノードは文字のまま (マイグレーション不要)。
- **依存**: M16 (ミニマップ地形)、M17 (`chart.ts`、構造化イベント)。
- **受け入れ基準**: 1カードから図鑑/マップ/メモを行き来できる。探索レポートに推移グラフと年表が出て PNG がアルバムに残る。新規採種で系統樹にサムネ。`e2e/report.spec.ts` (新規) + 既存 e2e 全通過。

### M19 — 遊び心地の総仕上げ: セッション設計・演出・実機QA・リリース

> 機能が出揃ってから、「ゲームとして毎日さわって気持ちいいか」を1本のマイルストーンとして扱う。粗い時間・バランスは M15.5/M15.7 で先に直してあるので、ここでは**目標体験に対する精密な合わせ込み**を行う。

- [ ] **セッション設計 (バランス調整パス)** — 数値だけの変更 (M)
  - 目標体験: 「**1セッション = デイループ2〜3日ぶん = 実時間10〜15分**。初日のセッションで拡散期に届き、3〜4セッションで成熟期・初採種まで行ける」。M15.7 の日長と M15.5 の day ゲートがこの体験に沿うかを `eraHistory` (M18) の実データで検証して詰める。
  - 調整対象: 速度プリセットと日長の組合せ、`era.ts` の昇格閾値、`wallet.ts` の日次報酬とツールコスト (1日あたりツール8〜12回の残高感)、デイリー要求数。**"やらなくても大丈夫" の哲学 (詰み防止・未達ペナルティなし) は制約として維持。**
- [ ] **デイループを既定モードへ** — M9 で見送った決定の再挑戦 (S)
  - オンボーディング直後に「デイループ / 見守り」の2択モーダル。既存 e2e は `addInitScript` で見守りに固定して無改修で通す。
- [ ] **演出の残り3件** (S×3)
  - 日次結果サムネの生成待ちを隠す: 生成完了までプレースホルダ (ぼかしたキャンバス or スピナー) を出し、`onload` でフェードイン (P8。現状はモーダルが開いた瞬間に壊れ画像アイコンが見える)。
  - 通貨の「+n」フロート表示: 増減時に残高チップから `+12 🪙` が浮いて消える (`prefers-reduced-motion` では無効)。
  - ドロワー/結果パネルのスライドイン: アニメーション対象を内側のコンテンツ要素に限定し、オーバーレイ自体は display のまま (M15 の containing block 問題を回避)。
- [ ] **実機QAの手順書化と実施** — `docs/QA.md` (新規) (S + ユーザー実施)
  - 本当に実機でしか検証できない項目だけ: safe-area、ドロワーとピンチの競合、PWA ホーム追加とオフライン起動、3ms/frame、音声の自動再生制限。headless で検査可能なものは e2e に足すのを先に検討する (モバイルの 0×0 キャンバスも `boundingBox` 検査で CI 化済み — 「実機でしか分からない」に安易に逃がさない)。
  - 発見された問題はこのマイルストーン内で修正 (バッファ L 相当)。
- [ ] **4通貨目の最終判定** — 大陸をバランスパスで遊び込み、3通貨で緩急が足りるなら正式廃案。足りない場合のみ最小構成で追加 (判断基準: 大陸で🪙が常時カンストする/しない)。
- [ ] **リリース** — README に遊び方 (3ステップ + スクリーンショット) と Pages URL、`CHANGELOG.md` を起こして v1.0 タグ。
- **依存**: M15.7〜M18 全部。
- **受け入れ基準**: 新規プレイヤー (localStorage 空) が、オンボーディング → デイループ選択 → 初日で拡散期到達 → 「今日の成長」で🪙獲得、まで**誰の説明もなしに15分で到達**できる。実機QAチェックリストが全項目 ✅ または既知の制約として文書化。v1.0 が Pages で配信。

## アーキテクチャ方針

- **sim はステートレスに保つ** — UI 側は SimState のスナップショットを読むだけ。書き込みは `Environment.placeX()` 経由に限定する。M15.7〜M19 は **sim のローカル則・Worker プロトコルに手を入れない** (M15.7 の時間スケールは sim-worker のループ間隔と web 側定数のみ、M16.5 の絵作りは `render.ts` のみ、M17 の突然変異イベントも DayRecord からの web 側派生)。
- **描画はラッパー** — `@morpho/web` は sim を import するだけで、sim 側に描画依存を持ち込まない。
- **時間スケールは UI に切り出す** — sim は「1 tick」しか知らない。日数・時代・デイループ・ローカルタイム・時代ETA はすべて web 側の派生量。M15.7 でもこの原則は変えない (変えるのは「1 tick が実時間何msか」と「1日が何tickか」だけ)。
- **ゲームデータは web 側 + マイグレーション必須** — 永続化は localStorage (画像のみ IndexedDB)。スキーマを変えるときは必ず `vN → vN+1` のマイグレーションを書く。
- **ゲームロジックは純粋関数 + vitest** — 新ロジック (era ETA / world-events / mutation-events / chart layout / notes / report) は DOM と Worker から切り離した純粋モジュールとしてテストしてから配線する。
- **e2e を壊す変更は同じ PR で追従する** — M15.7 の日長、M16 の速度4段化、M18 のタブ統合は既存 e2e の前提を変える。導線を変えるコミットと e2e 修正は分離しない。
- **「見える」ことを e2e で検査する** — バッキングストアの checksum や要素の可視性だけでは「プレイヤーに見えている」を保証できない (モバイル 0×0 キャンバスの教訓)。表示サイズ (`boundingBox`)・水平オーバーフロー (`scrollWidth`)・ピクセル存在 (getImageData) のような**実表示の検査**を優先する。
- **リポジトリルートは pnpm workspaces** — install は必ずリポジトリルートで行うこと (`@morpho/web` は `../sim/src` を直接 import している)。
- **性能は計測してから** — 描画に触る変更は `?debug` の perf HUD で 3ms/frame 以内を確認する。sim ベンチは M16〜M19 では変化しないはず (変化したら設計違反のシグナル)。

## 動かし方

```sh
pnpm install      # リポジトリルートで一度だけ (workspaces が両パッケージの依存を解決する)
cd packages/web
pnpm dev
# → http://localhost:5173 で遊べる
```

デプロイ版: main へのマージで GitHub Pages へ自動デプロイされる (`.github/workflows/pages.yml`)。

## 実プレイ検証の証跡

- `docs/playtest-2026-07-05/` — 第2回実プレイ検証のスクリーンショット (絵作りの before 基準としても使う):
  - `petri-day8-field.png` / `petri-day8-zoomed.png` — 皿ステージの全景とズームイン。M16.5 の「現状」の根拠。
  - `continent-day21-field.png` / `continent-minimap.png` — 大陸ステージとミニマップ (地形なし)。
  - `desktop-day20-full.png` — デスクトップ全体 UI (Day 20 変形体期、バランス目標帯の証跡)。
  - `mobile-390-fixed.png` — モバイル 390px、キャンバス 0×0 修正後の表示。
