# モルフォ — Web ゲーム化ロードマップ

> 「制御する」のではなく、「条件を整え、振る舞いを引き出す」。
> モックアップで描いた粘菌育成体験へ、既存の `@morpho/sim` を段階的に拡張する計画。

## ビジョン (2026-07-09 再定義)

実プレイ検証 第5回を受けて、目指すゲーム像を次のように言語化し直した。
以降のマイルストーンはすべてこの5行に奉仕する。

1. **果てのない世界** — Minecraft / Factorio のように、定常状態を持たず時間さえ与えれば
   無限に広がっていく。皿を埋めて終わりではなく、「原野」が本編。
2. **ズームインすると個性と地場** — 極狭い範囲では個体の個性と局所環境の効果が支配的で、
   一株ずつ違う振る舞いが見える。
3. **ズームアウトすると大局の伸び** — 俯瞰すれば「どんどん伸びていく」ことが絵として見える。
   俯瞰の高さは世界の成長とともに上がっていく。
4. **採餌の旅** — 視界に餌がなくても餌を探して這い回る。母体から近い組織は消費が緩やか、
   遠征している組織は早く消耗する (距離のコスト勾配)。だから遠くの豊かな餌場は挑戦になる。
5. **クリッカー型の介入カーブ** — 序盤は極小の世界で手動介入が濃い。後半は個性と環境が
   支配的になり手動の一手が相対的に無意味になる — プレイヤーは見守るか、
   「環境を買う」ような大局への高価な介入だけを行う。

## 現在地

- **`@morpho/sim`** — 粘菌のローカル則 (グラフ成長 / Biomass膜 / Activity場 / 温度・毒素・水域) は完成済み。
  M25 でチャンク化 (`ChunkedGridEnvironment` / `ChunkedActivityField` / `ChunkedBiomassField`) と
  forager reclaim (再採餌) が入り、半無限ワールドを駆動できる。決定論テスト + ベンチで守られている。
- **`@morpho/web`** — M0〜M27 完了。モックアップの遊び方 (デイループ、環境ツール9種、3通貨経済、
  個体の同定、図鑑と系統樹、大陸と時代、モバイルUI、PWA、Pages デプロイ)、モックアップの絵作り
  (テクスチャ/スプライト/湖岸線/有機的な脈)、新ステージ「原野」(半無限ワールド) まで出荷済み。
  各マイルストーンの要約は下の表、詳細は git 履歴と各ファイル冒頭コメント。
- **実プレイ検証 第5回 (2026-07-09)** — 「原野」をブラウザで6分強 (Day 16 相当) 観察 + sim 直接駆動で
  Day 100 相当まで実測し、上のビジョンとの乖離を洗い出した (下の「ビジョンとの乖離」節)。
  **結論: 半無限ワールドの土台 (M25) は動いているが、「広がりが見える」「広がり続けられる」
  「介入カーブがある」の3点が全て欠けており、現状の原野は
  『100×100 の窓の中で網がひたすら稠密化していくのを眺めるゲーム』になっている。**
  証跡は `docs/playtest-2026-07-09-infinite/`。対応は M28〜M32 に再定義した。

## 完了済みマイルストーン (M0〜M27) — 要約

> 詳細な設計メモは各実装ファイル冒頭のコメントと git 履歴 (各マイルストーンのコミット/PR)、
> 絵作りの経緯は `docs/art-direction.md`。ここでは「何がどこにあるか」だけを引けるようにする。

| M | 一行要約 | 主なコード |
| --- | --- | --- |
| **M0 土台** | Vite + TS の `packages/web`。Canvas 描画、再生/停止/速度、ツール5種、HUD、CI | `main.ts` `render.ts` |
| **M1 観察の快適化** | 差分再描画 60fps、sim を Worker へ、タイムライン、ヒートマップ、ズーム/パン | `field-diff.ts` `sim-worker.ts` `camera.ts` |
| **M1.5 土地** | バイオーム5種を手続き生成 + 地形テクスチャ (単色ベース) | `game.ts` |
| **M2 個性** | seed 決定的な Genome、6軸個性 + 5タイプ、図鑑、個体ビュー | `genome.ts` `individuality.ts` |
| **M3 環境の物語** | ステージ5種 (皿/洞窟/砂漠/都市跡/湿地)、環境の自然減衰 | `stages.ts` `environment.ts` |
| **M4 ゆるい目標** | メインクエスト・デイリーチャレンジ・実績・ベスト記録 | `quests.ts` `challenges.ts` |
| **M5 子孫** | 種の採取 → 変異付き継承、過酷な環境ほど変異幅大 | `genome.ts` `lineage.ts` |
| **M6 World View** | 複数コロニー、ミニマップ、Union-Find の統合判定 | `colony-networks.ts` `minimap.ts` |
| **M7 仕上げ** | 環境音、アルバム (IndexedDB)、PWA、タッチ、Pages 自動デプロイ | `ambient.ts` `album.ts` |
| **M8 速度** | tick 最適化、時間予算スケジューラ、perf HUD (`?debug`)、ベンチ | `tick-scheduler.ts` `perf-hud.ts` |
| **M9 デイループ** | 「仕込む→委ねる→受け取る」の1日フェーズ、結果パネル | `day-loop.ts` `day-report.ts` |
| **M10 環境システム** | 温度・毒素フィールド、耐性遺伝子、ツール4種追加 + Undo | `environment.ts` `undo.ts` |
| **M11 経済/デイリー** | 3通貨 Wallet (詰み防止付き)、ツールコスト、デイリー3件/日 | `wallet.ts` `dailies.ts` |
| **M12 個体の物語** | 通し番号+リネーム、★、特性チップ、カメラ追従 | `identity.ts` `trait-labels.ts` |
| **M13 収集/記録** | 図鑑37枠カタログ、系統樹の分岐ツリー、実績12種 | `catalogue.ts` `lineage.ts` |
| **M14 大陸と時代** | 大陸ステージ (水域+拠点)、条件達成型の時代、昼夜トーン | `era.ts` `daytime.ts` |
| **M15 モバイルUI** | ドロワー+下部ツールバー (≤900px)、コーチマーク | `onboarding.ts` `style.css` |
| **M15.5 実プレイ緊急修正** | 時代の day ゲート、クエスト引き締め、モバイル 0×0 キャンバス修正 | `era.ts` `main.ts` |
| **M15.7 時間スケール** | 壁時計ペーシング、`TICKS_PER_DAY` 40→240、×1=2.4分/日、`?dayms=` フック | `time-scale.ts` `sim-worker.ts` |
| **M16 世界の実感** | 時代 ETA「あと mm:ss」、ミニマップ地形焼き込み、速度4段、ズームスライダー | `era.ts` `minimap.ts` |
| **M16.5 ビジュアルパス** | 配色刷新 (黒潰れ解消・金色の2パス発光・リム発光・水の深浅)、輝度 e2e | `render.ts` `docs/art-direction.md` |
| **M17 個体の物語 完** | WorldEvent 構造化 (時刻+エリア注視)、突然変異イベント、特徴文、📊グラフ | `world-events.ts` `mutation-events.ts` |
| **M18 収集・記録ハブ** | 図鑑/マップ/メモのタブ、探索レポート、時代の年表、系統樹サムネ | `notes.ts` `report.ts` `era-history.ts` |
| **M19 総仕上げ** | デイループ既定化2択、演出3件、実機QA表 (`docs/QA.md`)、4通貨目の見送り判定 | `main.ts` `docs/QA.md` |
| **M20 アセット** | 発注ブリーフ (`docs/asset-briefs.md`)、タイル/スプライト実物、`AssetManager` (無ければベクタへフォールバック) | `assets.ts` |
| **M21 地形の質感** | タイルテクスチャ敷き込み、岩の露頭スプライト、接地影、木漏れ日 — ズームに耐える地面 | `render.ts` `terrain-blobs.ts` |
| **M22 水場の再設計** | marching squares の湖岸線、水面テクスチャ+漂流、浅瀬/湿った砂の岸帯、ミニマップ追従 | `coastline.ts` |
| **M23 ネットワーク有機化** | Catmull-Rom の曲線脈+先細り、星形ハブ、成長前線の扇、ジオメトリキャッシュ | `vein-curves.ts` |
| **M24 生態感** | キノコ/苔/朽木の決定的散布 (食べ尽くすと消える)、エサのオーブ化 | `ecology-scatter.ts` |
| **M25 果てのない世界 (土台)** | sim チャンク化、forager reclaim (枯れた sink を前線チップへ戻す)、窓アダプタ、新ステージ「原野」 | `chunked-*.ts` `chunk-window.ts` `growth.ts` |
| **M26 (旧)** | 俯瞰上昇の下敷き関数のみ (dead code)。実体は M28/M29 へ再定義して吸収 | `camera.ts` の `fitBBoxSpan` 等 |
| **M27 有界ステージの終盤** | 栄養の再生サイクル (season 脈動)、時代停滞時の残条件明示、期限切れチャレンジ表示 | `nutrient-regen.ts` `era.ts` |

**意図的な見送り (再掲)**: 昼夜 brightness の sim への反映 (毎tick全面書換が高コスト)、4通貨目 (3通貨で経済成立を確認済み)。
**過去の検証証跡**: 第2回 `docs/playtest-2026-07-05/`、第3回 (絵作りの乖離) + 第4回 (Day 24 完全凍結の実測) `docs/playtest-2026-07-05-visuals/`、M20〜M24 の before/after `docs/playtest-2026-07-06-visuals/`。

## ビジョンとの乖離 (実測、2026-07-09 第5回検証)

> 手法: (a) ブラウザ実プレイ — `vite preview` + headless Chromium で皿と原野を操作し、
> 原野は ×24 で6分強 (Day 16) 観察、HUD を15秒おきにサンプリング。
> (b) sim 直接駆動 — 原野相当の構成 (チャンク食料 + reclaim=0.3) で Day 100 相当 (24,000 tick) を
> 実測。証跡: `docs/playtest-2026-07-09-infinite/` (スクリーンショット + サンプリング生データ)。

| # | ビジョン | 実測した現状 | 対応 |
| --- | --- | --- | --- |
| V1 | ズームアウトすると大局の伸びが見える | **俯瞰が存在しない**。`camera.ts` は `MIN_ZOOM=1` で、最小ズーム=100×100 の窓がちょうど画面いっぱい。ズームスライダーを最小にしても画面が1ピクセルも変わらない (`b-wildland-zoomout.png` = `b-wildland-end.png`)。世界がどれだけ広がっても絵として一切見えない | **M28** |
| V2 | 世界の成長が数字でも実感できる | **HUD の「総面積/総量」は窓の中しか数えていない** (`game.ts` の `computeWorld` は焼き出した 96×96 窓を集計、理論上限 10,000m²)。原野 Day 9 以降は 8,000〜9,000m² を上下するだけで、成長が数字にも見えない。全世界を数えているのは接続ネットワーク数 (エッジ本数) だけ | **M28** |
| V3 | 定常状態を持たず無限に広がる | 出荷構成のブラウザ実測では、拡大ではなく**稠密化**する: Day 16 でエッジ 6,908 本の迷路が窓を埋め尽くし (`b-wildland-end.png`)、面積は頭打ち。sim 直接駆動 (Day 100) では前線スパン 26→192 と広がり続けるが、ペースは約 +1.9/日と控えめで、しかも非単調 (Day 65→75 で 144→92 と大きく後退することがある) | **M29/M30** |
| V4 | 時間さえ与えれば広がり続けられる | **性能で時間が崩壊する**。×24 指定でも実効ペースは開始直後で約 2.1秒/日、Day 16 (エッジ約7,000) では約45〜60秒/日 — 公称 (160ms/日) の約300倍遅い。sim 直接駆動でも、生成済みチャンクが evict されず溜まり続け (68チャンク到達)、エッジ200本程度なのに 5.4ms/tick まで漸増。長時間セッションは現状成立しない | **M29** |
| V5 | 探索範囲に餌がなくても探し回る | forager reclaim (M25) で「枯れたら這い出す」は実現済み。ただし原野の地形は**全チャンクに必ず餌パッチが1つ**湧く一様な野原で、「探し回る」旅にならない (どこへ行っても餌がある)。バイオームも1種類 | **M30** |
| V6 | 母体から近いと消費が緩やか、遠いと早い | **存在しない**。`life.ts` の維持コストは完全にローカルで、母体からの距離という概念が sim にない | **M30** |
| V7 | 序盤は極小の世界で手動が濃い | 皿は Day 0 の時点で既に 6拠点・4,464m²・318リンクが繋がっており、チャレンジ3件中2件が開始直後に自動達成される。原野も開始1日で「全拠点接続 100%」など3件全て自動達成 (`b-wildland-start.png`)。**「極小から手ずから育てる」立ち上がりがどのステージにもない** | **M31** |
| V8 | 後半は見守るか「環境を買う」大局介入 | **見守りモードには収入が一切ない** (雫の日次収入は日次結果モーダル = デイループモード限定、`main.ts`)。原野を16日見守っても雫 117 のまま不変。後半に効く高価な大局介入ツールも存在しない (ツールは全て窓内のブラシのみ) | **M31** |
| V9 | 原野が本編として成立している | 目標系がすべて有界ステージ用のまま: メインクエスト「拠点をすべてつなぐ」が開始直後から100%、「大陸の85%を探索する」は 0% 固定、時代は拡散期で停止 (「もう1拠点に到達」は原野では構造的に満たせない)。時代 ETA 表示が15秒ごとに「あと1:20 ↔ あと17:47 ↔ 条件:…」と乱高下。Undo/消去ツール無効、図鑑に原野個体が収録されない (いずれも M25 の意図的スコープ外のまま) | **M32** |

### 検証で見つけた小さな違和感 (上記以外)

- [ ] **チャンクの明るい正方形パッチ** — 原野の地面に半透明の四角いタイルがはっきり見える
  (`b-wildland-start.png` 背景)。木漏れ日 (`drawDappledLight`) かチャンク焼き出しの継ぎ目が
  正方形単位で出ているとみられる。M28 の俯瞰描画と合わせて調査 (小、`render.ts`)。
- [ ] **時代 ETA の乱高下** — `estimateEraEta()` が進捗の揺れをそのまま外挿するため、
  15秒間隔のサンプリングで毎回別の値 (1分↔22分↔条件表示) になる。移動平均などの平滑化が要る
  (小、`era.ts`)。V9 の一部として M32 で対応。

## 次のマイルストーン (M28〜M32) — ビジョンへ寄せる

> 位置づけ: **原野を本編に育てる**。有界6ステージ (皿〜大陸) は完成品として凍結し
> (回帰ゼロを保つ)、以降の開発は原則すべて原野に投資する。
> M28 (見える) → M29 (続く) → M30 (探す) → M31 (介入カーブ) → M32 (本編化) の順。
> M28 と M29 は補完関係 (俯瞰は描画の、休眠は sim の LOD) で、この2つが揃って
> はじめて「広がり続ける世界を眺める」が成立する。

### M28 — 俯瞰: 世界の広がりが「見える」ようにする

> V1/V2。ユーザー体験の核心で、最優先。モックアップ①の「WORLD VIEW ズームアウト: 大局を俯瞰」
> はこれ。M25 の窓方式 (カメラ無改修) は正解だったが、窓の外を見る手段を作らなかった。

- [ ] **大局レイヤーの描画** — ズームアウトが `MIN_ZOOM` に達したらさらに引けるようにし、
  窓の外は「チャンク単位の粗い描画」(訪問済みチャンクの地形色 + バイオマス濃度 + 網の骨格線)
  に切り替える。詳細描画 (M20〜M24 の資産) は窓の中だけで生きればよい。
  sim 側に「チャンクごとの要約 (バイオマス量・ノード数・地形種)」を返す軽い集計 API を足し、
  Worker プロトコルへ低頻度 (数百ms間隔) の `worldOverview` メッセージを追加する。
- [ ] **ズームの再設計** — `camera.ts` の `MIN_ZOOM=1` 固定を撤廃し、原野では
  「現在の世界 bbox + 余白がちょうど収まる」まで引けるようにする (M26 で作った
  `fitBBoxSpan`/`bboxCenter`/`approachSpan` を今度こそ使う)。世界が広がるほど
  引ける高さが上がる = 「俯瞰の高さがどんどん高くなっていく」。
  既存6ステージのズーム値域は不変。
- [ ] **HUD 統計の全世界化** — `computeWorld()` を原野ではチャンク側の集計に切り替え、
  総面積・総量が窓ではなく世界全体を数えるようにする。「到達距離 (母体から最遠の組織まで)」
  「探索チャンク数」を新指標としてワールド情報パネルへ足す — 伸び続ける数字を必ず1つ以上
  常時見せる。
- [ ] **ワールドマップ** — ミニマップを原野では「窓の位置を示す枠 + 訪問済み世界の全体図」
  にする (大局レイヤーと同じチャンク要約を焼く)。タップで窓をそこへ移動できれば、
  「探索済みの土地に戻る」(現状不可能) も同時に解決する。
- **受け入れ基準**: 原野で30分放置 → ズームアウトすると開始時より明確に広い世界が見える
  (スクリーンショット比較)。HUD 総面積が 10,000m² を超えて増え続ける。e2e:
  ズームアウト時のキャンバスに窓 (100×100) の外のコンテンツが描かれることをピクセル検査。
  既存6ステージの e2e 全通過。

### M29 — 止まらない時間: LOD/休眠で「広がり続けられる」ようにする

> V3/V4。旧 M26 の未着手分 (LOD と休眠・チャンクタイル化) をこの形で引き継ぐ。
> 実測済みのボトルネック: (a) エッジ約7,000本で実効ペースが公称の約300倍遅い、
> (b) 生成済みチャンクが evict されず diffuse コストが漸増、(c) エッジ1万本規模で
> tick 本体 30.5ms/tick (M25 実測)。原因は「全エッジ・全チャンクを毎tick等しく更新する」こと。

- [ ] **成熟領域の休眠 (sim)** — 前線から遠く状態変化が小さくなった領域 (チャンク単位) を
  「休眠」させる: 休眠チャンク内のエッジは activity/fatigue/radius 更新とフィールド書き込みを
  スキップし、輸送 (flux) の通り道としてだけ生きる。前線が近づく・プレイヤーが介入する・
  餌が再生するなどのイベントで起こす。決定論を保つ (休眠判定も seed 決定的に)。
  ⚠️ sim の tick ループ本体に手が入る、このロードマップ最大の sim 変更。
  既存6ステージは休眠無効 (閾値 0) で bit 一致を守る。
- [ ] **チャンクの evict** — 休眠チャンクのフィールドを要約値 (平均栄養など) に圧縮して
  実体を解放し、再訪時に要約から復元する。`generatedChunkCount()` がセッション時間に
  比例して増え続ける現状 (Day 100 で 68 チャンク・5.4ms/tick、上限なし) を止める。
- [ ] **稠密化の抑制 (バランス)** — ブラウザ実測で「窓の中で迷路化してエッジ7,000本」に
  なったのは、出荷パラメタ (genome + PETRI_PARAMS 由来) では lateral bud / loop 生成が
  前線の前進より速いため。原野の `paramOverrides` を「外へ伸びる > 中を埋める」へ調整し、
  同じ60秒観察でエッジ数より span が伸びる配分にする (sim 直接駆動ハーネスで対照実験)。
- [ ] **性能の受け入れ基準を時間で切る** — `?debug` の perf HUD に「実効 日/分」を足し、
  ×24 指定時の実効ペースが Day 0 と Day 100 で ±20% に収まることを sim ハーネス +
  ブラウザ実測の両方で確認する。
- **受け入れ基準**: 原野を Day 100 相当まで回しても実効ペースが落ちない (上記)。
  メモリ (生成済みチャンク数) が前線サイズに比例し、総経過時間に比例しない。
  既存6ステージの決定論テスト bit 一致・e2e 全通過。

### M30 — 餌の地理: 「探し回る」価値のある世界にする

> V5/V6。採餌の仕組み (reclaim) はあるので、探す理由 (希少性と勾配) を世界側に作る。

- [ ] **バイオーム** — チャンクノイズで地帯を分ける: 豊かな森 (餌多・湿潤)、痩せた荒地
  (餌ほぼ無し)、岩場 (障害物多)、水辺 (湿潤・水域)、毒の窪地 (毒素・餌豊か = リスクリワード) など
  4〜6種。「餌のない地帯を横断して次の豊かな地帯へ届くか」という旅を発生させる。
  M20 のタイル資産をバイオームごとに割り当てて絵でも見分けられるようにする。
- [ ] **距離のコスト勾配 (sim)** — `params.distanceUpkeep` (新規、既定 0=無効) を足し、
  母体 (source) からのグラフ距離が遠いエッジほど fatigue の回復が遅く/消耗が速くなる
  ローカル則を入れる。「母体の近くは消費が緩やか、遠征中は早く消化される」を実現し、
  無制限な一方向暴走を抑える自然なブレーキにもなる (V3 の非単調な後退も、
  「伸びすぎた遠征枝が枯れて戻る」という読める物語になる)。
  グラフ距離は厳密最短路ではなく source からの BFS 近似を低頻度で更新して償却する。
- [ ] **子孫の出番** — 遠征で枯れる/毒地帯で変異が増える、と M5 の継承システムを接続する:
  遠くのバイオームで採種した子孫ほど変異幅が大きい等、「遠くへ行く理由」を個性側にも作る。
- **受け入れ基準**: sim ハーネスで「バイオーム境界で前線が一旦止まり、reclaim で横断する」
  シナリオを対照実験で確認。距離 upkeep 有効時、母体近傍と前線のエッジ fatigue に
  実測差が出る。既存6ステージは全パラメタ既定 0 で bit 一致。

### M31 — 介入カーブ: 序盤は手で、後半は環境を買う

> V7/V8。クリッカー系の「手動 → 自動化/大局化」の弧を、モルフォの文法
> (直接命令はできない、環境を整えるだけ) のまま作る。

- [ ] **極小スタート** — 原野の開始を「胞子1個 + 最初の餌パッチ1つ」からにする。
  最初の数日はプレイヤーが餌や水を置いてやらないと育たない (置けば確実に応える) —
  手動介入が濃く、意味が大きい時間帯。既存の皿は変えない (皿はチュートリアルとして現状が適切)。
- [ ] **見守り収入** — 見守りモードにも収入を入れる: 新チャンク到達・新バイオーム発見・
  時代到達などの「世界の成長」に比例して雫/若葉が入る (デイループの日次収入と整合させ、
  どちらのモードでも経済が回るようにする)。
- [ ] **大局介入 (環境を買う)** — ズームアウト (M28 の大局レイヤー) 中だけ使える高価な
  マクロツールを3種ほど: 「雨季を呼ぶ (広域の湿度を一定期間底上げ)」「肥沃な帯 (指定方向に
  栄養の回廊を敷く)」「地熱 (広域の温度を変える)」。価格は窓内ブラシの数十倍にし、
  後半の主要な意思決定にする。ブラシ系ツールは後半も使えるが、世界が大きくなるほど
  相対的な影響が小さくなる (= 自然に「手動が無意味になっていく」)。
- [ ] **介入の効果が読める** — 大局介入の効果範囲と残り期間を大局レイヤーに可視化する
  (「買った環境」が働いているのが見える = クリッカーの建物に相当する満足感)。
- **受け入れ基準**: 新規セッションの Day 0〜3 は無介入だと成長がほぼ止まり、餌を置くと
  明確に応答する (sim ハーネス対照実験)。見守りモードで30分放置して通貨が増えている (e2e)。
  大局ツール3種が原野で機能し、既存6ステージには出ない。

### M32 — 原野を本編に: 目標系と記録系の全面接続

> V9。遊びの器 (クエスト/時代/チャレンジ/図鑑/記録) を原野仕様にし、
> 有界6ステージを「チュートリアル & チャレンジ集」として位置づけ直す。

- [ ] **原野の時代** — 拠点数ベースの既存条件をやめ、無限世界の節目で刻む:
  到達距離・探索チャンク数・発見バイオーム数・世代数など。「時代が進むたびに世界の
  スケール感が一段上がる」(モックアップ①の「時代: 胞子期 → 変形体期」リングの原野版)。
  時代 ETA の乱高下 (上の違和感2つ目) もここで平滑化する。
- [ ] **原野のクエストとチャレンジ** — 「全拠点接続 100%/探索 0% 固定」を撤去し、
  原野専用のメインクエスト (例:「母体から 500 先へ到達しよう」「3つのバイオームに根を張ろう」) と
  期限付きでない反復チャレンジ (例:「今日中に新チャンク5つ」) に置き換える。
- [ ] **記録系の接続** — 図鑑/系統樹/実績/探索レポートに原野個体を収録する
  (`STAGE_ORDER` へ追加、図鑑は37枠を拡張)。Undo/消去ツールをチャンク系フィールドでも
  効くようにする (チャンク側 `recordBefore` 相当の差分記録)。
- [ ] **オンボーディングの動線** — 新規プレイヤーの推奨動線を「皿 (チュートリアル) → 原野 (本編)」
  として明示する (ステージ選択の並びと説明文、皿の成熟期到達で原野を勧めるコーチマーク)。
- **受け入れ基準**: 原野で時代が実際に進む (e2e)。開始直後に自動達成されるチャレンジが無い。
  図鑑に原野個体が収録される。既存6ステージの e2e 全通過。

## アーキテクチャ方針

- **原野が本編** (2026-07-09 更新) — 有界6ステージは完成品として凍結し、決定論テストと e2e で
  回帰ゼロを守る。新しい遊びはすべて原野に足す。sim の新パラメタは必ず「既定 0 = 無効」で入れ、
  既存ステージの bit 一致を保つ (M25 の `forageReclaimThreshold` 方式)。
- **sim はステートレスに保つ** — UI 側は SimState のスナップショットを読むだけ。書き込みは
  `Environment.placeX()` 経由に限定。M29 (休眠)・M30 (距離 upkeep) は sim のローカル則に手が入る —
  着手前に対照実験ハーネス (第5回検証の `sim-longrun` 方式) で before を採ってから変更する。
- **描画はラッパー** — `@morpho/web` は sim を import するだけで、sim 側に描画依存を持ち込まない。
  M28 の「チャンク要約」も sim 側は数値の集計だけを返し、絵は web 側で決める。
- **アセットは任意** — テクスチャ/スプライトが無くても手続き描画でゲームが成立するフォールバックを保つ。
- **時間スケールは UI に切り出す** — sim は「1 tick」しか知らない。日数・時代・ローカルタイムは web 側の派生量。
- **ゲームデータは web 側 + マイグレーション必須** — localStorage (画像のみ IndexedDB)。スキーマ変更は必ず `vN → vN+1`。
- **ゲームロジックは純粋関数 + vitest** — チャンク要約・距離勾配・収入計算も DOM から切り離してテストしてから配線。
- **e2e を壊す変更は同じ PR で追従する**。
- **「見える」ことを e2e で検査する** — checksum ではなく実表示 (boundingBox / ピクセル存在 / 輝度・分散) を検査する。
- **リポジトリルートは pnpm workspaces** — install は必ずリポジトリルートで。
- **性能は計測してから** — 描画は `?debug` の perf HUD で 3ms/frame 以内。sim は「実効 日/分」(M29) で
  長時間の劣化を検査する。

## 動かし方

```sh
pnpm install      # リポジトリルートで一度だけ
cd packages/web
pnpm dev
# → http://localhost:5173 で遊べる
```

デプロイ版: main へのマージで GitHub Pages へ自動デプロイ (`.github/workflows/pages.yml`)。

## 実プレイ検証の証跡

- `docs/playtest-2026-07-09-infinite/` — **第5回 (ビジョンとの乖離、M28〜M32 の before 基準)**。
  原野 Day 1 / Day 16 / ズームアウト最小 (Day 16 と同一画面 = 俯瞰不在の証跡) / 皿 Day 0、
  ブラウザ HUD の15秒おきサンプリング生データ、sim 直接駆動 Day 100 のサンプリング生データ。
- `docs/playtest-2026-07-06-visuals/` — M20〜M24 の after (絵作り)。
- `docs/playtest-2026-07-05-visuals/` — 第3回 (絵作りの乖離) + 第4回 (Day 24 完全凍結)。M20〜M27 の before 基準。
- `docs/playtest-2026-07-05/` — 第2回 (遊び方の検証)。
