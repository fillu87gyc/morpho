# モルフォ — Web ゲーム化ロードマップ

> 「制御する」のではなく、「条件を整え、振る舞いを引き出す」。
> モックアップで描いた粘菌育成体験へ、既存の `@morpho/sim` を段階的に拡張する計画。

## 現在地

- **`@morpho/sim`** — 粘菌のローカル則 (グラフ成長 / Biomass膜 / Activity場 / 温度・毒素・水域を含む環境ツール) は完成済み。決定論テスト + ベンチ (`pnpm --filter @morpho/sim run bench`) で守られている。
- **`@morpho/web`** — M0〜M15.5 で「モックアップの遊び方」は一通り成立した: デイループ (仕込む→委ねる→受け取る)、環境ツール9種+Undo、3通貨経済とゆるいデイリー、個体の同定 (名前/★/特性チップ/追跡)、図鑑37枠と分岐する系統樹、大陸ステージと条件達成型の時代、モバイル縦UIとオンボーディング、PWA、GitHub Pages 自動デプロイ。**今すぐ遊べる状態にある。**
- **実プレイ検証 第2回 (2026-07-05)** — headless Chromium で新規プレイヤー導線 (オンボーディング → ツール6回配置 → 皿を Day 20 まで育成 → デイループ1周 → 大陸 Day 21 → モバイル縦)。コンソールエラー0。証跡スクリーンショットは `docs/playtest-2026-07-05/` に保存した。発見と処置:
  - ✅ **M15.5 のバランス再調整は機能している** — 皿で Day 20 ちょうどに変形体期 (目標帯 Day 20〜30)、メインクエストの探索も 94% で残る。「初日で全部終わる」状態は解消された。
  - 🔧 **モバイルでキャンバスが 0×0 になり何も見えない** (致命的リグレッション) — M15.5 の `fitCanvas()` 測定前ゼロ化が、行高がコンテンツ由来のモバイル縦レイアウトでは wrap の高さごと 0 に潰し、そのまま復元されない。**このセッションで修正済み** (高さが取れないときは幅を一辺にしてビューポート残り高さでクランプ + e2e に「キャンバスの実表示サイズ > 200px」検査を追加)。教訓: canvasChecksum≠0 (バッキングストアに描画がある) は「プレイヤーに見えている」を意味しない。
  - 📷 **日次結果モーダルのサムネイルが開いた瞬間だけ壊れ画像アイコン** — `renderThumbnail()` の resolve 前にモーダルが開くため src 未設定の一瞬が見える (2.5秒後には正常を確認)。M19 で修正済み。
- **M15.7 完了 (2026-07-05)** — ⏱ 「1日」が実時間0.64秒しかない問題 (`TICK_INTERVAL_MS=16`×`TICKS_PER_DAY=40` → ×1で1日≈0.64秒) を、sim-worker.ts に壁時計時間ベースのペーシングを実装して解消。`TICKS_PER_DAY` を6倍 (40→240) にし、既定の日長を `time-scale.ts` の `DEFAULT_DAY_MS=144,000` (×1で2.4分/日) に設定。`?dayms=`/`localStorage: morpho.dayMs.v1` で日長を上書きできる開発/e2e フックを追加。⚖ チャレンジ「最短でつなぐ」も実測 (皿は無操作でも Day 0 のうちに全接続) に合わせて「Day 0のうちに」へ換算。詳細は M15.7 セクション参照。
- **M16.5 完了 (2026-07-05)** — 🎨 絵作りの最大の印象差 (ほぼ黒の虚無+カーキのブロブ+赤茶の線) を、地形ベース色の常時不透明化・ハッシュノイズ粒感・ネットワークの2パス発光・プラズマ膜のリム発光で解消。詳細・before/after 証跡は M16.5 セクションと `docs/art-direction.md` を参照。
- **M16 完了 (2026-07-05)** — 時代の残り時間予測 (`estimateEraEta`)・ミニマップ地形焼き込み・速度4段ボタン (⏸▶▶▶▶▶▶)・ズームスライダーを実装。詳細は M16 セクション参照。
- **M17 完了 (2026-07-05)** — 出来事を `WorldEvent` へ構造化 (時刻表示+エリア注視)、突然変異/形質獲得イベント、タイプ別特徴文、統計グラフ📊を実装。詳細は M17 セクション参照。
- **M18 完了 (2026-07-05)** — 「収集・記録」タブ (図鑑/マップ/メモ)、メモ、時代の年表、探索レポート、系統樹サムネイルを実装。詳細は M18 セクション参照。
- **M19 完了 (2026-07-05)** — デイループ既定化の2択オンボーディング、演出3件 (サムネプレースホルダ・通貨フロート・ドロワースライドイン)、実機QAチェックリスト、4通貨目の見送り判定、README/CHANGELOG。詳細は M19 セクション参照。
- **現在地: M0〜M19 すべて完了。** `ROADMAP.md` に残る作業項目はない。今後の変更は `CHANGELOG.md` で追跡する (「アーキテクチャ方針」節はコードに変更を加える際の指針として引き続き有効)。

## 完了済みマイルストーン (M0〜M15.5) — 要約

> 詳細な設計メモは各実装ファイル冒頭のコメントと git 履歴 (各マイルストーンのコミット/PR) に残っている。ここでは「何がどこにあるか」だけを引けるようにする。

| M | 一行要約 | 主なコード |
| --- | --- | --- |
| **M0 土台** | Vite + TS の `packages/web`。Canvas 描画、再生/一時停止/速度スライダー (×1〜24)、ツール5種、HUD、リセット、CI typecheck | `web/src/main.ts` `render.ts` |
| **M1 観察の快適化** | 差分再描画で 60fps、sim を Web Worker へ分離、成長タイムライン、環境ヒートマップ、ズーム/パン | `field-diff.ts` `sim-worker.ts` `game-proxy.ts` `timeline.ts` `camera.ts` |
| **M1.5 土地** | バイオーム5種をシードから手続き生成 + 地形テクスチャ | `web/src/game.ts` |
| **M2 個性** | seed から決定的な `Genome`、6軸個性 + 5タイプ判定、図鑑、個体ビュー | `sim/graph/genome.ts` `individuality.ts` `web/src/encyclopedia.ts` |
| **M3 環境の物語** | ステージ5種 (皿/洞窟/砂漠/都市跡/湿地)、環境の自然減衰 | `web/src/stages.ts` `sim/env/environment.ts` |
| **M4 ゆるい目標** | メインクエスト・デイリーチャレンジ・実績・ベスト記録 | `quests.ts` `challenges.ts` `achievements.ts` `scoreboard.ts` |
| **M5 子孫** | 種の採取 → 変異付き継承、過酷なステージほど変異幅が大きい | `sim/graph/genome.ts` `web/src/lineage.ts` |
| **M6 World View** | 複数コロニー、ミニマップ、Union-Find のネットワーク統合判定 | `colony-networks.ts` `minimap.ts` |
| **M7 仕上げ** | 環境音、アルバム (IndexedDB)、PWA、タッチ+ピンチ、GitHub Pages 自動デプロイ | `ambient.ts` `album.ts` `pinch.ts` `.github/workflows/pages.yml` |
| **M8 速度** | tick 0.5〜0.7ms への最適化、時間予算スケジューラ、早送りモード、perf HUD (`?debug`)、再現ベンチ | `sim/graph/*` `tick-scheduler.ts` `perf-hud.ts` |
| **M9 デイループ** | 「仕込む→委ねる→受け取る」の1日フェーズ。日境界で自動停止、結果パネルに3軸スコアの前日比Δ。既定は見守り、ヘッダのトグルで切替 | `day-loop.ts` `day-report.ts` `worker-protocol.ts` |
| **M10 環境システム** | sim に温度・毒素フィールド、Genome に耐性2遺伝子。加温/冷却/毒素/乾燥ツールと stroke 単位 Undo、環境バランスHUDの実測値化 | `sim/env/environment.ts` `sim/graph/life.ts` `web/src/undo.ts` |
| **M11 経済/デイリー** | 3通貨 (🪙🍃🍄) の Wallet (詰み防止の自動回復付き)、ツールコスト、配置カウント型デイリー3件/日、チャレンジ3種の常時一覧化 | `wallet.ts` `dailies.ts` `challenges.ts` |
| **M12 個体の物語** | 通し番号 + リネーム、★評価、特性チップ、育った環境チップ、カメラ追従、出来事イベント。**残: 突然変異演出・特徴文・統計グラフ・エリア注視 → M17** | `identity.ts` `trait-labels.ts` `sim/graph/growth.ts` |
| **M13 収集/記録** | 図鑑をタイプ×ステージ+特殊条件の37枠カタログへ、系統樹の分岐ツリー化、実績12種。**残: タブUI・探索レポート・系統樹サムネ → M18** | `catalogue.ts` `catalogue-thumbs.ts` `lineage.ts` |
| **M14 大陸と時代** | 大陸ステージ (水域+拠点の手続き生成+専用クエスト)、時代の条件達成型化、ローカルタイム+夜間トーン。**残: 残り時間予測・ミニマップ地形 → M16** | `era.ts` `daytime.ts` `stages.ts` |
| **M15 モバイルUI** | ハンバーガー☰+全画面ドロワー+下部ツールバー (900px以下)、初回3ステップのコーチマーク | `onboarding.ts` `web/index.html` `style.css` |
| **M15.5 実プレイ緊急修正** | 時代に day ゲート (拡散8/変形体20/成熟40)・クエスト/チャレンジ引き締め、モバイル2段ヘッダ+水平オーバーフロー e2e、モード切替の縦書き潰れ修正、見守りモードでも DayRecord 記録。**副作用: fitCanvas ゼロ化がモバイルでキャンバス 0×0 を生む → 2026-07-05 に修正・e2e 追加済み** | `era.ts` `quests.ts` `challenges.ts` `style.css` `main.ts` `mobile.spec.ts` |
| **M15.7 時間スケール再設計** | sim-worker.ts に壁時計時間ベースのペーシング (要求 tick 数 = 経過実時間 / (dayMs÷TICKS_PER_DAY) × speed) を実装し、早送り用のループ間隔比率換算を撤廃。`TICKS_PER_DAY` を 40→240 (6倍) に、既定の日長を `DEFAULT_DAY_MS=144,000` (×1で2.4分/日) に設定。`?dayms=` / `localStorage: morpho.dayMs.v1` で日長を上書きできる開発/e2e フック (`time-scale.ts`) を追加し、既存 e2e は旧 tick レート相当 (3840ms/日) に固定して全通過を維持。実測で「チャレンジ『最短でつなぐ』は皿なら無操作でも Day 0 のうちに全接続してしまう」ことが判明し (P7)、旧 240 tick 相当の絶対ラインを保ったまま表記を「Day 6 以内」→「Day 0 のうちに」へ換算。era/quests/wallet/dailies は day 数・実時間ベースのままで無改修 (day ゲートの相対的な厳しさは変わらないことを確認済み) | `time-scale.ts` `sim-worker.ts` `day-loop.ts` `game-proxy.ts` `worker-protocol.ts` `challenges.ts` |
| **M16.5 ビジュアルパス** | 配色を刷新: STAGE_BG を昼想定で底上げ (旧 `#0b0d0c` 付近→苔の緑/暖色系)、地形に `hashNoise()` ベースの粒ノイズ (苔/岩のまだら/水面の揺らぎ)、Biomass 膜は成長前線をリム発光させ確立した核はアルファを絞って地形を透かす (旧: 中心ほど不透明の単調増加を反転)、ネットワークは2パス発光 (`lighter` 合成の glow + 芯)、水域は深浅グラデーション+縁の明るいライン、夜間トーンは暗紺の不透明上塗りから弱い半透明の青の重ねへ (`'color'` 合成は彩度を完全置換して真っ青になる失敗を screenshot 検証で発見し不採用)。性能: dirty な全セルを通る粒ノイズは「他レイヤーが何も該当しない素の地面」のときだけ計算するガードを追加し、glow は総エッジ数が `GLOW_MAX_EDGES` を超えると自動で省略。`docs/art-direction.md` に色票と検証結果をまとめ、`e2e/visual.spec.ts` (画面隅の輝度検査、旧配色で確実に失敗することを確認済み) を追加 | `render.ts` `docs/art-direction.md` |

**速度の現状**: スライダー上限 ×24 に対し通常モードで実効 ×4〜7、早送り (⏩) で ×10 前後。残るボトルネックは sim の tick コスト (エッジ数に比例) のみ。ベースラインは `sim/scripts/bench.ts` で再現できる。

## 残ギャップ一覧 (2026-07-05、全項目解消)

モックアップ2枚との差分は M16〜M19 で全て解消した。唯一、意図的に見送ったままの項目:

| # | モックアップの要素 | 現状 | 判断 |
| --- | --- | --- | --- |
| — | 昼夜の brightness を sim の成長応答に効かせる | 夜間トーン (視覚のみ) | **見送り継続** — 毎tick全面書換が高コストなため。sim 無改修の方針とも整合しない |
| — | 4通貨目 (大陸探索専用) | 3通貨で経済は成立 | **M19 で正式に見送り判定** — 大陸プレイで🪙の偏りは観測されなかった |

## マイルストーン一覧 (M15.7〜M19、すべて完了)

各マイルストーンは「純粋関数 + vitest → DOM 配線 → e2e」の順で作った。

### M15.7 — 時間スケールの再設計: 「1日」を観察に値する長さへ ✅ 完了 (2026-07-05)

> 実測: `TICK_INTERVAL_MS = 16` (sim-worker.ts) × `TICKS_PER_DAY = 40` (day-loop.ts) → ×1 で 1日 ≈ **0.64秒**。モックアップ①の観察画面は「育成中… 02:34」— 数分単位で委ねる体験を描いている。

**実装した設計:**
- **`TICKS_PER_DAY` を 40→240 (6倍) に変更**。era/quests/challenges/wallet の day ゲート判定は「経過日数」ベースなので拘束力自体は変わらないが、% ベースのチャレンジ (「Day N 以内」) は換算が必要になる (下記)。
- **sim-worker.ts の tick 生成を「壁時計時間ベースのペーシング」に変更**: `time-scale.ts` に「×1 における1日の実時間長 (ms)」を `DEFAULT_DAY_MS=144,000` (2.4分) として切り出し、経過実時間 ÷ (dayMs/TICKS_PER_DAY) × speed で要求 tick 数を `paceDebt` に貯めて整数分だけ `TickScheduler` に渡す。早送りモードのループ間隔比率換算は不要になった (実時間ベースなのでループ間隔に依存しない)。
- **e2e/開発用の日長上書きフック**: `?dayms=` クエリパラメータ、または `localStorage: morpho.dayMs.v1` で上書きできる (`time-scale.ts` の `resolveDayMs()`、優先順位は URL > storage > 既定)。各 e2e スペックが `addInitScript` で個別に値を設定する (共通フィクスチャは導入せず、テストごとに必要な時間感覚を明示)。
- **チャレンジ「最短でつなぐ」を「Day 0 のうちに」へ換算** — 実測で皿ステージは無操作でも tick 70〜194 (新 240tick 換算で Day 0 の範囲内) で全拠点接続してしまうことが判明したため、旧 240tick 相当の絶対ラインを保ったまま表記を「Day 6 以内」から換算した。
- **受け入れ基準**: ✅ ×1 で 1日が実測120秒台。✅ 既存 e2e 全通過 (日長上書きフック経由)。✅ typecheck/vitest 通過。

### M16.5 — ビジュアルパス: 「自然のフィールドに粘菌が這う」画へ ✅ 完了 (2026-07-05)

> **最大の印象差**。モックアップは「苔むした岩肌・木漏れ日の中を、金色に光る網が這う」— 現状は「ほぼ黒の虚無に、カーキの均一ブロブと赤茶の実線」。証跡 (before): `docs/playtest-2026-07-05/petri-day8-field.png` / `continent-day21-field.png`。証跡 (after): `petri-m165-after.png` / `continent-m165-after.png`。詳細は `docs/art-direction.md`。

- [x] **基準画の固定** — `docs/art-direction.md` に目標のルックと実装ノートを記載。
- [x] **地形の露出底上げ** — `STAGE_BG`/`STAGE_ROCK_COLOR` を大幅増光。地形の合成方式を「近黒背景 + 淡い差分」から「`TERRAIN_TONE` (shadow⇄sunlit) による常時不透明のベース色」へ変更。`env.brightness` を「日当たり」として視覚的に転用し、明暗を表現。
- [x] **地形テクスチャの強化** — `hashNoise(x,y)` によるセル単位の決定的な粒感 (苔・岩のまだら) を追加。乾湿による色味の寄せも合わせて調整。
- [x] **ネットワークの発光表現** — `drawEdges` を2パス化 (`globalCompositeOperation: 'lighter'` + `shadowBlur` のぼかしグロー → crisp な芯)。`TUBE_LIGHT/DARK` を金色系統へ変更。
- [x] **Biomass 膜の見た目** — `PLASMA_LOW/MID/HI` を金色 (#E8B84B系) へ、`PLASMA_RIM` で成長前線 (v の低い帯域) を山型カーブで一段明るく光らせるリム発光を追加。
- [x] **性能ガード** — `?debug` で draw **2.10ms** (予算3ms以内) を確認。`e2e/visual.spec.ts` (新規) に「黒潰れ再発検知」を追加 — 完全なピクセル差分スナップショットは seed が `Math.random()` 由来で決定的な基準画像を作れないため断念し、代わりに皿/大陸それぞれで平均輝度が閾値 (>35) を超えることを検査する。
- **受け入れ基準**: ✅ before/after のスクリーンショットで「金色に光る網が苔むした地面を這っている」に見える (目視確認済み)。✅ `?debug` で 3ms/frame 以内。✅ 既存 e2e 39本 (新規2本含む) 全通過。

### M16 — 世界の実感: 時代予測・ミニマップ地形・速度/ズームUI ✅ 完了 (2026-07-05)

> World View モックアップの HUD を完成させた。すべて web 側の演出・UI で、sim・Worker プロトコルには触っていない。

- [x] **時代の残り時間予測「あと mm:ss」** — `era.ts` に `EraSample`/`estimateEraEta()` を追加。EWMA (α=0.35) で進捗速度を推定し `(1-progress)/rate` を ETA (ms) として返す。速度がほぼ0・履歴不足・後退中は `null` (UI は「次の時代まで —」)。サンプリングは `ui.ts` の `render()` に relay (2.5秒に1点、直近20点のリングバッファ、時代切替でリセット)。`formatMMSS` は `main.ts` → `day-loop.ts` へ移設して共通化。test 6件追加。
- [x] **ミニマップ地形描画** — `minimap.ts` に `bakeTerrain(stageId, env)` を追加。ステージ切替時に一度だけオフスクリーンへバイオーム基調色 (M16.5 の `TERRAIN_TONE` と揃えた簡易パレット) + 水域 + 障害物を焼き、毎フレームは「地形キャッシュ → コロニー点 → 視野矩形」の3層合成。e2e (`continent.spec.ts`) に水域ピクセル存在検査を追加。
- [x] **速度コントロールの4段ボタン化** — `index.html`/`ui.ts`/`main.ts`。⏸ ▶ ▶▶ ▶▶▶ (▶▶▶ のみ ×24+早送りON も同時に設定)。`aria-pressed` で選択状態を示し `morpho.speedPreset.v1` に永続化。スライダー+⏩トグルは廃止。
  - **e2e 追従**: `catalogue/continent/lineage-tree.spec.ts` の `setSpeedSlider(24)` ヘルパーをボタンクリックへ置き換え、`smoke.spec.ts`/`day-loop.spec.ts` の `#pause-toggle`/`#fast-forward`/`#speed-slider` 参照をすべて新ボタンへ移行。
  - **踏んだ地雷**: `day-loop.spec.ts` の「見守りで数日過ごしてからデイループへ」テストが、▶▶▶ (早送りON) だと `frame()` の重い処理 (DayRecord記録を含む) の呼び出し頻度が 60fps→10fps に落ち、e2e の短縮日長 (500ms/日) と組み合わさって複数日が1フレームに収まってしまいテストが不安定になった。早送りを伴わない ▶▶ (×8) に変更して解消 (本番の通常速度では日長がずっと長いため起きない)。
- [x] **ズームスライダー UI** — `index.html` に「− [スライダー] +」+「ピンチでズーム」ヒントをキャンバス右下に追加。`Camera.setZoomCentered()` (新規) で絶対値ズームの入口を用意し、ホイール/ピンチでの変化も毎フレーム双方向に同期する。
- **受け入れ基準**: ✅ 大陸ステージで「時代リング + あと mm:ss + 地形入りミニマップ」が同時に見える。✅ 4段ボタンで消化速度を切り替えても既存 e2e 39本 (更新分含む) 全通過。✅ typecheck/vitest 217件通過。

### M17 — 個体の物語の完成: 出来事の構造化・突然変異・特徴文・統計グラフ ✅ 完了 (2026-07-05)

> モックアップ②の「進化の記録」「このエリアを注視中」「📊」を成立させた。

- [x] **出来事の構造化** — `web/src/world-events.ts` (新規)。`WorldEvent { id, day, tick, kind, text, x?, y? }` + `WorldEventLog` (30件の回転バッファ、`id` 単調増加)。`game.ts` の `recentEvents: string[]` と `eventToText()` を置き換えた。
  - **設計変更**: ロードマップ原案は「進化の記録の翻訳先を出来事ログに変える」だったが、実装では「進化の記録 (evoLog/eraLog) は時代の節目+M17の突然変異/形質獲得の"節目"専用」「最近の出来事 (worldEventLog) は道具の設置+sim の細かい出来事 (太い幹が育った等) を統合した"時々刻々"専用」に役割を分けた — モックアップの2枚のスクリーンショットがそれぞれ別の粒度の情報を持っていたことに合わせた。
  - 表示は「12:15 栄養を発見」形式 (`daytime.ts` の `localTimeFor()` から時刻を導出、残ギャップ #28 解消)。event-text.ts への辞書切り出しは行わず game.ts 内に留めた (診断: 分離の便益より複雑化コストが上回ると判断)。
  - test: `test/world-events.test.ts` (8件)。既存 `sim/test/events.test.ts` 無改修で通過。
- [x] **「このエリアを注視中」トグル** — `main.ts` / `ui.ts`。出来事カードに 👁 トグル。ON のとき `camera.view()` と `WorldEvent.x/y` の交差 (`filterByArea()`) でフィルタ。座標なしイベントは常に通す。
  - e2e: `identity.spec.ts` に「ズームインして注視ON → 視野外イベントが消え、OFFで戻る」を追加。**踏んだ地雷**: e2e の短縮日長 (500ms/日) だと sim イベントの奔流で30件バッファがすぐ埋まり、テスト対象のエサイベントが押し出される — テスト内で `#speed-btn-pause` を使いsimを止めてから検証するよう変更。
- [x] **プレイ中の突然変異・形質獲得イベント** — `web/src/mutation-events.ts` (新規)。**sim 無改修**。日境界 (`main.ts` の `checkMutationEvents()`) で前日の `DayRecord.traits` と当日を比較し |Δ|>0.08 の最大軸を「個体が突然変異 — 探索性が上昇」として、`traitChipsFor()` の新出現チップを「新しい形質を獲得 — ◯◯」として、main.ts 側のローカル evoLog (`localEvoLog`) に積む。`ui.render()` が `game.evolution()` (era 節目) とマージして表示する。test: `test/mutation-events.test.ts` (7件)。
- [x] **タイプ別特徴文** — `trait-labels.ts` に `typeDescriptionFor(id): string` (5タイプ分)。個体ビューのタイプ表示直下に表示。test: `test/trait-labels.test.ts` に1件追加。
- [x] **統計グラフ 📊** — `web/src/chart.ts` (新規)。`buildChartLayout()` (純粋関数、vitest 7件) + `drawChart()`。個体ビューの 🎯追跡 の隣に 📊 ボタン → モーダルで直近60日の3軸スコア+質量 (右軸) を折れ線表示。データ2日未満は「まだ記録が足りません」。
- **受け入れ基準**: ✅ 数日回すと「進化の記録」に突然変異/形質獲得が現れる。✅ 注視ONで足元の出来事だけが流れる。✅ 出来事に時刻が付く。✅ 個体ビューに特徴文と 📊 グラフ。✅ vitest 240件 + e2e 40本、全通過。

### M18 — 収集・記録ハブ: タブUI・メモ・探索レポート・系統樹サムネ ✅ 完了 (2026-07-05)

> モックアップ①の「収集・記録 (図鑑/マップ/メモ)」タブと②の「探索レポート」を実装した。

- [x] **「収集・記録」タブ付きパネル** — `index.html` / `ui.ts` / `style.css`。図鑑 (右パネル) / マップ (旧・左パネルの「ワールドビュー」カード全体、ミニマップ+統合ネットワーク数+「離れたコロニーをひとつに」クエスト) / メモ の3タブを1カードへ統合。既存 DOM (`#ency` / `#minimap` 等) の ID・描画ロジックは無改修、`ui.ts` の配置だけを変えた。タブ選択は `morpho.recordTab.v1` に永続化。
  - **e2e 追従**: `smoke.spec.ts` の「ミニマップをクリック」テストが `#record-tab-map` を開いてから操作するよう変更。
- [x] **メモ** — `web/src/notes.ts` (新規)。Day付き自由テキスト、追加/削除のみ、「今日の成長を貼る」で `game.snapshot()` から定型文を挿入。`morpho.notes.v1`、直近100件。test 7件。
- [x] **時代の年表の記録** — `web/src/era-history.ts` (新規)。`morpho.eraHistory.v1` に `{ era, day }` を追記 (同じ時代への重複記録は冪等にスキップ)。test 4件。
- [x] **探索レポート** — `web/src/report.ts` (新規) + `#report-modal`。総質量・総面積の推移 (`chart.ts` 再利用) + 時代の年表 + 数値サマリ (拠点接続/探索率/発見種数/実績) + 「画像で保存」(canvas 合成 → アルバム)。`buildReport()`/`eraHistoryLines()` を純粋関数で vitest 12件、`e2e/report.spec.ts` (新規) でスモーク。
- [x] **系統樹ノードのサムネイル** — `web/src/lineage-thumbs.ts` (新規)。採種時に `renderThumbnail()` を撮り IndexedDB へ保存 (40px サムネ、無いノードは文字のまま)。
  - **設計変更**: ロードマップ原案は「catalogue-thumbs.ts と同じ IndexedDB で store を分ける」だったが、2クラスが同じ DB のバージョン管理を独立に持つ競合を避けるため専用 DB (`morpho-lineage-thumbs`) にした。
  - **踏んだ地雷**: `renderThumbnail→fetch→IndexedDB保存` は harvest() 本体 (同期、`lineage.version++`) より確実に遅れて完了する非同期処理のため、`ui.ts` の再描画ガードが `lineage.version` だけを見ていると「サムネイルが後から届いてもそのフレームではもう再描画しない」まま出ないバグになった。`LineageThumbs` に独自の `version` を持たせ、`ui.ts` 側で両方の合成キーを再描画判定に使うよう修正 (実プレイ検証で発覚、e2e にも同事象の待機付きアサーションを追加)。
- **受け入れ基準**: ✅ 1カードから図鑑/マップ/メモを行き来できる。✅ 探索レポートに推移グラフと年表が出て PNG がアルバムに残る。✅ 新規採種で系統樹にサムネ (非同期完了後に反映)。✅ vitest 256件 + e2e 42本、全通過。

### M19 — 遊び心地の総仕上げ: セッション設計・演出・実機QA・リリース ✅ 完了 (2026-07-05)

> 機能が出揃ってから、「ゲームとして毎日さわって気持ちいいか」を1本のマイルストーンとして扱った。

- [x] **セッション設計 (バランス調整パス)** — 実測ベースの検証。速度プリセット ▶▶ (×8) で headless 実測すると Day 8 (拡散期) 到達に約130秒 (2分強)、目標の「10〜15分セッション」に対して十分早く届く。一方、既定選択の ▶ (×1、120秒/日) のままだと Day 8 到達に約16分かかり、初回セッションの想定をやや超える。
  - **判断**: `era.ts` の day ゲート・`wallet.ts` の日次報酬 (M11 設計のまま) はいずれも実測上の破綻がなく、数値の再調整は行わなかった。速度プリセットの既定値を ▶ (×1) → ▶▶ (×8) に変える案は検討したが、多くの e2e が「既定速度のまま数秒で日が進む」ことに暗黙に依存しており (`smoke.spec.ts` 等)、この変更単体で広範囲の再検証が要るためこのセッションでは見送った。**フォローアップ**: オンボーディングまたは初回ヒントで「▶▶ がおすすめ」を案内する導線が今後の候補。
  - "やらなくても大丈夫" の哲学 (詰み防止・未達ペナルティなし) は維持したまま、数値変更は行っていない。
- [x] **デイループを既定モードへ** — オンボーディング直後に「デイループで遊ぶ / 見守りで眺める」の2択モーダル (`#day-loop-choice`) を追加。`morpho.dayLoopMode.v1` が未設定 (＝真の初回) のときだけ表示し、既存ユーザー (既に値がある) には出さず現在の選択を維持する。既存 e2e は `morpho.onboarded.v1` を事前設定してオンボーディング自体をスキップしており (=このモーダルも発火しない) 無改修で通った。オンボーディングを実際に完了させる2本 (`onboarding.spec.ts`/`mobile.spec.ts`) だけ選択操作を追加。
- [x] **演出の残り3件**
  - 日次結果サムネのプレースホルダ化 (P8 解消): `renderThumbnail()` 完了前に `drThumb` の `src` 属性を外して `.dr-thumb` の背景色に戻し、新しい画像が届いてから旧 URL を revoke する順序に変更 (旧実装は「死んだ blob URL を指したままの一瞬」が壊れ画像アイコンとして見えていた)。
  - 通貨の「+n」フロート表示: 残高チップから `+12`/`-5` が浮いて消える (`prefers-reduced-motion` では生成しない)。
  - ドロワーのスライドイン: `.panel.left/.right` (position:fixed のオーバーレイ) 自体は M15.5 の教訓どおり display 切替のまま、内側に新設した `.drawer-content` (デスクトップでは `display:contents` でレイアウトに関与しない) だけを transform でスライドさせる。
- [x] **実機QAの手順書化** — `docs/QA.md` (新規)。iOS/Android の safe-area・PWA・オフライン起動・ピンチ・自動再生制限など、実機でしか検証できない項目のみ列挙。**実施** (実機での手動チェック) はこの開発環境では行えないため、チェックリストとして文書化した状態で引き継ぐ。
- [x] **4通貨目の最終判定** — **正式に見送り** とし、残ギャップ一覧から外した。M15.7〜M19 を通じて大陸ステージを繰り返し操作した範囲では 🪙 が常時カンストする/枯渇するような偏りは観察されず、3通貨で経済は成立していると判断した (大陸専用の緩急が要るという具体的な兆候は見つからなかった)。
- [x] **リリース** — `README.md` (新規、遊び方3ステップ+スクリーンショット+Pages URL)、`CHANGELOG.md` (新規、v1.0 エントリ) を作成。
- **受け入れ基準**: ✅ 新規プレイヤーがオンボーディング → デイループ/見守り選択 → プレイ開始まで到達 (実測: ▶▶ 選択時は Day8/拡散期到達が約2分、既定 ▶ では約16分 — 上記の通り既知の制約として記録)。✅ 実機QAチェックリストを文書化 (実施は未着手として明記)。✅ v1.0 が README/CHANGELOG とともにリリース可能な状態。✅ vitest 256件 + e2e 44本、全通過。

## アーキテクチャ方針

- **sim はステートレスに保つ** — UI 側は SimState のスナップショットを読むだけ。書き込みは `Environment.placeX()` 経由に限定する。M15.7〜M19 は **sim のローカル則・Worker プロトコルに手を入れない** (M15.7 の時間スケールは sim-worker のループ間隔と web 側定数のみ、M16.5 の絵作りは `render.ts` のみ、M17 の突然変異イベントも DayRecord からの web 側派生)。
- **描画はラッパー** — `@morpho/web` は sim を import するだけで、sim 側に描画依存を持ち込まない。
- **時間スケールは UI に切り出す** — sim は「1 tick」しか知らない。日数・時代・デイループ・ローカルタイム・時代ETA はすべて web 側の派生量。M15.7 でもこの原則は変えない (変えるのは「1 tick が実時間何msか」と「1日が何tickか」だけ)。
- **ゲームデータは web 側 + マイグレーション必須** — 永続化は localStorage (画像のみ IndexedDB)。スキーマを変えるときは必ず `vN → vN+1` のマイグレーションを書く。
- **ゲームロジックは純粋関数 + vitest** — 新ロジック (era ETA / world-events / mutation-events / chart layout / notes / report) は DOM と Worker から切り離した純粋モジュールとしてテストしてから配線する。
- **e2e を壊す変更は同じ PR で追従する** — M15.7 の日長、M16 の速度4段化、M18 のタブ統合は既存 e2e の前提を変える。導線を変えるコミットと e2e 修正は分離しない。
- **「見える」ことを e2e で検査する** — バッキングストアの checksum や要素の可視性だけでは「プレイヤーに見えている」を保証できない (モバイル 0×0 キャンバスの教訓)。表示サイズ (`boundingBox`)・水平オーバーフロー (`scrollWidth`)・ピクセル存在 (getImageData) のような**実表示の検査**を優先する。
- **リポジトリルートは pnpm workspaces** — install は必ずリポジトリルートで行うこと (`@morpho/web` は `../sim/src` を直接 import している)。
- **性能は計測してから** — 描画に触る変更は `?debug` の perf HUD で 3ms/frame 以内を確認する。sim ベンチは M16〜M19 では変化しないはず (変化したら設計違反のシグナル)。

## 動かし方

```sh
pnpm install      # リポジトリルートで一度だけ (workspaces が両パッケージの依存を解決する)
cd packages/web
pnpm dev
# → http://localhost:5173 で遊べる
```

デプロイ版: main へのマージで GitHub Pages へ自動デプロイされる (`.github/workflows/pages.yml`)。

## 実プレイ検証の証跡

- `docs/playtest-2026-07-05/` — 第2回実プレイ検証のスクリーンショット (絵作りの before 基準としても使う):
  - `petri-day8-field.png` / `petri-day8-zoomed.png` — 皿ステージの全景とズームイン。M16.5 の「現状」の根拠。
  - `continent-day21-field.png` / `continent-minimap.png` — 大陸ステージとミニマップ (地形なし)。
  - `desktop-day20-full.png` — デスクトップ全体 UI (Day 20 変形体期、バランス目標帯の証跡)。
  - `mobile-390-fixed.png` — モバイル 390px、キャンバス 0×0 修正後の表示。
