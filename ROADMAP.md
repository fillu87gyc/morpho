# モルフォ — Web ゲーム化ロードマップ

> 「制御する」のではなく、「条件を整え、振る舞いを引き出す」。
> モックアップで描いた粘菌育成体験へ、既存の `@morpho/sim` を段階的に拡張する計画。

## 現在地

- **`@morpho/sim`** — 粘菌のローカル則 (グラフ成長 / Biomass膜 / Activity場 / 温度・毒素・水域を含む環境ツール) は完成済み。決定論テスト + ベンチで守られている。
- **`@morpho/web`** — M0〜M19 完了。「モックアップの遊び方」(デイループ、環境ツール9種、3通貨経済、個体の同定、図鑑と系統樹、大陸と時代、モバイルUI、PWA、Pages デプロイ) は一通り成立し、今すぐ遊べる。各マイルストーンの要約は下の表、詳細は git 履歴と各ファイル冒頭コメント。
- **実プレイ検証 第3回 (2026-07-05)** — headless Chromium で皿 Day 47 / 大陸 Day 13 まで実際に育成し、ツール配置 (水/石/エサ)・ズームイン・ミニマップを操作して撮影。コンソールエラー0。**結論: 遊び方はモックアップに追いついたが、絵作りは M16.5 の配色刷新だけでは全く追いついていない。** 証跡は `docs/playtest-2026-07-05-visuals/`。発見は「モックアップとの乖離 (実測)」節に整理し、対応を M20〜M24 のTODOに落とした。
  - 🔧 修正済 (このセッション): 「進化の記録」の Day 表示が旧 `TICKS_PER_DAY=40` のハードコードで6倍に膨張していた (`ui.ts` — ヘッダ DAY 47 なのに「Day 120 変形体期に入った」と出る)。M15.7 の変更漏れ。
- **実プレイ検証 第4回 (2026-07-05)** — 「Day 120 くらいで何も動かなくなる」という報告を受け、皿を無操作で Day 115 まで早回しし HUD を約5日おきにサンプリング。**実際の停滞開始は Day 24**: 総面積 5,508m² と接続ネットワーク数 321 が Day 24〜115 まで1ビットも変化せず、総質量は 9.38〜9.84kg の減衰揺れのみ、時代は変形体期で恒久停止 (「次の時代まで —」)、進化の記録も Day 20 以降沈黙する。証跡: `docs/playtest-2026-07-05-visuals/petri-day115-stalled.png`。原因は仕様の合成で、バグではない:
  1. 世界が `worldSize=100` 固定 + `worldMargin` の到達禁止帯 — 皿を埋めたら物理的に伸び先がない (`sim/graph/growth.ts` の境界判定)
  2. 初期栄養を食べ尽くした後、プレイヤーがツールを使わない限り環境に何も起きない — sim は定常状態に到達する
  3. 成熟期の条件 (全ネットワーク統合) は自然には満たされず、時代の進行も止まる
  対応は M25〜M27 (半無限ワールド + 有界ステージの終盤設計) にTODO化した。

## 完了済みマイルストーン (M0〜M19) — 要約

> 詳細な設計メモは各実装ファイル冒頭のコメントと git 履歴 (各マイルストーンのコミット/PR)、
> 絵作りの経緯は `docs/art-direction.md` に残っている。ここでは「何がどこにあるか」だけを引けるようにする。

| M | 一行要約 | 主なコード |
| --- | --- | --- |
| **M0 土台** | Vite + TS の `packages/web`。Canvas 描画、再生/停止/速度、ツール5種、HUD、CI | `main.ts` `render.ts` |
| **M1 観察の快適化** | 差分再描画 60fps、sim を Worker へ、タイムライン、ヒートマップ、ズーム/パン | `field-diff.ts` `sim-worker.ts` `camera.ts` |
| **M1.5 土地** | バイオーム5種を手続き生成 + 地形テクスチャ (単色ベース) | `game.ts` |
| **M2 個性** | seed 決定的な Genome、6軸個性 + 5タイプ、図鑑、個体ビュー | `genome.ts` `individuality.ts` |
| **M3 環境の物語** | ステージ5種 (皿/洞窟/砂漠/都市跡/湿地)、環境の自然減衰 | `stages.ts` `environment.ts` |
| **M4 ゆるい目標** | メインクエスト・デイリーチャレンジ・実績・ベスト記録 | `quests.ts` `challenges.ts` |
| **M5 子孫** | 種の採取 → 変異付き継承、過酷な環境ほど変異幅大 | `genome.ts` `lineage.ts` |
| **M6 World View** | 複数コロニー、ミニマップ、Union-Find の統合判定 | `colony-networks.ts` `minimap.ts` |
| **M7 仕上げ** | 環境音、アルバム (IndexedDB)、PWA、タッチ、Pages 自動デプロイ | `ambient.ts` `album.ts` |
| **M8 速度** | tick 最適化、時間予算スケジューラ、perf HUD (`?debug`)、ベンチ | `tick-scheduler.ts` `perf-hud.ts` |
| **M9 デイループ** | 「仕込む→委ねる→受け取る」の1日フェーズ、結果パネル | `day-loop.ts` `day-report.ts` |
| **M10 環境システム** | 温度・毒素フィールド、耐性遺伝子、ツール4種追加 + Undo | `environment.ts` `undo.ts` |
| **M11 経済/デイリー** | 3通貨 Wallet (詰み防止付き)、ツールコスト、デイリー3件/日 | `wallet.ts` `dailies.ts` |
| **M12 個体の物語** | 通し番号+リネーム、★、特性チップ、カメラ追従 | `identity.ts` `trait-labels.ts` |
| **M13 収集/記録** | 図鑑37枠カタログ、系統樹の分岐ツリー、実績12種 | `catalogue.ts` `lineage.ts` |
| **M14 大陸と時代** | 大陸ステージ (水域+拠点)、条件達成型の時代、昼夜トーン | `era.ts` `daytime.ts` |
| **M15 モバイルUI** | ドロワー+下部ツールバー (≤900px)、コーチマーク | `onboarding.ts` `style.css` |
| **M15.5 実プレイ緊急修正** | 時代の day ゲート、クエスト引き締め、モバイル 0×0 キャンバス修正 + 実表示 e2e | `era.ts` `main.ts` |
| **M15.7 時間スケール** | 壁時計ペーシング、`TICKS_PER_DAY` 40→240、×1=2.4分/日、`?dayms=` フック | `time-scale.ts` `sim-worker.ts` |
| **M16 世界の実感** | 時代 ETA「あと mm:ss」、ミニマップ地形焼き込み、速度4段、ズームスライダー | `era.ts` `minimap.ts` |
| **M16.5 ビジュアルパス** | 配色刷新 (黒潰れ解消・金色の2パス発光・リム発光・水の深浅)、輝度 e2e | `render.ts` `docs/art-direction.md` |
| **M17 個体の物語 完** | WorldEvent 構造化 (時刻+エリア注視)、突然変異イベント、特徴文、📊グラフ | `world-events.ts` `mutation-events.ts` `chart.ts` |
| **M18 収集・記録ハブ** | 図鑑/マップ/メモのタブ、探索レポート、時代の年表、系統樹サムネ | `notes.ts` `report.ts` `era-history.ts` |
| **M19 総仕上げ** | デイループ既定化2択、演出3件、実機QA表 (`docs/QA.md`)、4通貨目の見送り判定、README/CHANGELOG | `main.ts` `docs/QA.md` |

**意図的な見送り (再掲)**: 昼夜 brightness の sim への反映 (毎tick全面書換が高コスト)、4通貨目 (3通貨で経済成立を確認済み)。
**速度の現状**: スライダー上限 ×24 に対し実効 ×4〜7 (ボトルネックは sim tick コスト、`sim/scripts/bench.ts` で再現可)。

## モックアップとの乖離 (実測、2026-07-05 第3回検証)

> モックアップ①② (フォトリアルな苔むした岩場を金色の網が這う画) と、実際に遊んだ画面の差分。
> **根本原因は2つ**: (a) 地形・水・岩の全てを **96×96 の ImageData** (`game.ts` の `FIELD=96`) に焼いて
> 8倍以上に拡大補間しているため、構造的にぼける。(b) ビットマップのテクスチャ/スプライト資産が
> **1枚も無く**、全てをコード内の単色+ノイズ+ベクタ図形で済ませている。M16.5 は配色を直したが、
> この2つには触れなかった。「テクスチャを探す・画像生成AIに発注する」という選択肢を使う (→ M20)。

| # | 要素 | モックアップ | 実測 (証跡: `docs/playtest-2026-07-05-visuals/`) | 対応 |
| --- | --- | --- | --- | --- |
| G1 | 岩場 (①中央の主役) | 苔むした岩の露頭。陰影・エッジ・質感がある | 紫灰色のぼかし塊。ズームすると輪郭ごと溶ける (`petri-day47-full.png` 上部) | M21 |
| G2 | 水場 | 深い青緑、岸辺の明るい縁、波紋、写実的な湖岸線 | **平坦な水色の丸いベタ** (`continent-water-blobs.png`)。「水色の丸を置いて水場です」状態 | M22 |
| G3 | 地面のズーム耐性 | ズームするほど苔・小石・落ち葉のディテールが見える | ズームすると完全な無地のグラデーション (`petri-zoomed-featureless.png`) | M21 |
| G4 | 粘菌ネットワーク | 節点から放射状に伸びる星形のハブ、先細りの脈、扇状に広がる成長前線 | 均一太さの折れ線ループ+ベタ円ノード。ズームでフラットなオレンジ帯になり発光感が消える (`continent-zoomed-flat-veins.png`) | M23 |
| G5 | 生態感の小物 | 赤いキノコ、苔の茂み、朽木、光る果実状のエサが散る | 小物ゼロ (ランドマークのベクタ図形が4種あるのみ)。エサは緑のぼかし光 | M24 |
| G6 | 地形の起伏・光 | 岩の高低差、木漏れ日、ティルトシフト風の被写界深度 | 中央がやや明るいラジアルグラデーション1枚 | M21/将来 |

遊び方 (ループ・HUD・記録系) はモックアップと一致していることを同検証で確認済み。乖離は絵作りに集中している。

### 検証で見つけたバグ/違和感 (絵作り以外)

- [x] **「進化の記録」の Day が6倍** — `ui.ts` の `tick/40` ハードコード。**修正済み** (2026-07-05、`TICKS_PER_DAY` を import)。
- [ ] **期限切れチャレンジが「挑戦中…」のまま** — 「最短でつなぐ (Day 0 のうちに)」が Day 47 でも挑戦中と表示される。初回達成型の設計自体は良いが、今の皿ではもう達成不可能なことが読み取れない。「この皿では期限切れ — 次の皿で挑戦」等の状態表示を足す (小、`challenges.ts`/`ui.ts`)。

## 次のマイルストーン (M20〜M24) — 絵作りをモックアップへ寄せる

> 方針: sim・Worker プロトコルは引き続き無改修。全て `render.ts` 周辺と静的アセットで実現する。
> 各マイルストーンは従来どおり「純粋関数 + vitest → DOM/描画配線 → e2e (実表示検査)」の順。
> 性能予算も従来どおり `?debug` で 3ms/frame 以内。

### M20 — アセットパイプライン: 「コードで描ける絵」の縛りを外す

> 一番の構造的ギャップ (資産ゼロ) への対処。この環境では画像を生成できないため、
> **発注書を書いて人間に渡す**ところまでを成果物にする。

- [x] **発注ブリーフの作成** — `docs/asset-briefs.md` (2026-07-05 作成済み)。タイル6種 (苔/岩/砂/石畳/洞窟床/水面) + スプライト5系統 (岩の露頭/キノコ/エサのオーブ/苔の茂み/朽木) の、画像生成AIにそのまま貼れる英語プロンプト・共通仕様 (真上視点/シームレス/解像度/色票)・CC0 代替入手先 (ambientCG/Poly Haven)・受け入れチェックを記載。
- [x] **人間への依頼** — 完了。`packages/web/public/textures/` に発注ブリーフの必須ファイル (タイル6種+スプライト5系統) とボーナス素材一式が配置済み (出典: `CREDITS.md`)。ただし元画像がコンタクトシートからの切り出しのため解像度は仕様の半分以下 (`CREDITS.md` に既知の制約として記録済み)。本番投入前に個別再生成 or CC0 差し替えが必要。
- [x] **アセットローダ** — `web/src/assets.ts` (新規)。`AssetManager` がテクスチャ/スプライトを遅延ロード + キャッシュし、未ロード/失敗時は同期的に `null` を返す (呼び出し側は現行の手続き描画へフォールバックする設計)。DOM 依存は `Image` を使う `loader` 一点に切り出し、vitest では loader を差し替えて DOM 無しに単体テスト済み (`test/assets.test.ts`)。`createPattern`/スプライト描画は M21〜M24 で `render.ts`/`minimap.ts` から利用する。
- [x] **プレースホルダで先行実装可能に** — 本物 (高解像度) のアセット到着前でも M21〜M24 を進められるよう、`docs/asset-briefs.md` 記載の必須ファイルが既に (低解像度ながら) `public/textures/` に揃っているため、追加の手続きプレースホルダ生成スクリプトは不要と判断 (現物で代替)。
- **受け入れ基準**: アセット有り/無しの両方で全 e2e 通過。無しのとき現状と同じ絵、有りのとき M21 以降の絵に切り替わる。→ M21〜M24 の各受け入れ基準で検証する。

### M21 — 地形の質感: 96×96 のぼかしから「ズームに耐える地面」へ

> G1/G3/G6。フィールド ImageData は「どの地形か」のマスクに役割を限定し、
> 質感はスクリーン空間で乗せる。

- [x] **タイルテクスチャの敷き込み** — `render.ts` の `drawTerrainTexture()`。ステージごとの下地 (苔/砂/石畳/洞窟床/湿地・大陸は苔で代用) を `createPattern` + `setTransform` (カメラのズーム/パンをパターン変換行列に反映) でスクリーン解像度のまま描く。96×96 の fieldCanvas は従来通りバイオマス/エサ/水などのオーバーレイ専用のまま。アセット未ロード時は何もせず、既存の STAGE_BG グラデーションがそのまま地面になる (フォールバック)。
- [x] **岩場のスプライト化** — `terrain-blobs.ts` (新規、純粋関数 + vitest 7件) で obstacle フィールドの4近傍連結成分を検出し重心・概算半径を算出。`render.ts` の `drawRockSprites()` が `rock-cluster-{1..4}` (通常サイズ) / `small-stone-{1..6}` (`SMALL_BLOB_MAX_CELLS` 以下の飛び地) を決定的に (座標ハッシュで選択・回転) 配置する。岩スプライトが使える間は `paintFieldLayer` 側のぼかし色塗りを無効化 (`spritesReady` フラグ、アセット未ロード時は従来のぼかし塗りにフォールバック)。
- [x] **起伏の錯覚** — 岩スプライトの足元に接地影 (楕円AO) + 淡い境界エッジ (明るいストローク楕円) を追加。「中央がやや明るいラジアルグラデーション1枚」だった光は `drawDappledLight()` に置き換え、ワールド座標に固定した大きめの hashNoise セル (7ワールド単位) で木漏れ日のまだらを表現 (ズームしても斑点の実寸が保たれる)。
- **受け入れ基準**: 満たした。`e2e/visual.spec.ts` に最大ズーム (`#zoom-slider`=8) での中央付近ブロックの輝度標準偏差を検査するテストを追加 (M21の描画を無効化すると stddev 1.27 → 通常時は閾値3を安定して超えることを確認済み)。`?debug` で最大ズーム時の draw コストを実測: 1.40ms/frame (予算3ms以内)。

### M22 — 水場の再設計: 「水色の丸」から「湖岸線のある水域」へ

> G2。ユーザー指摘の核心。「青いベタ=水場です」をやめる。

- [x] **湖岸線のベクタ化** — `coastline.ts` (新規、純粋関数 + vitest 6件)。water フィールドから marching squares (鞍点はコーナー一致による単純な解決) で境界線分を抽出し、端点共有で輪へ縫い合わせる。ぼかし円の集合ではなく閉じた輪郭 (Path2D) として描く。
- [x] **水面の質感** — `render.ts` の `drawWaterBodies()`。輪郭パスをクリップし `water-surface.png` の `createPattern` + `setTransform` で塗った上に、深い水色の半透明トーン (実測でテクスチャがかなり暗く地形と紛れるための補正) を重ねる。時間経過でパターンをゆっくり平行移動し (`WATER_DRIFT_SPEED`)、`prefers-reduced-motion` のときは停止する。
- [x] **岸辺の表現** — 輪郭に沿って内側 (浅瀬、明るいシアン系ストローク) / 外側 (湿った砂、褐色ストローク) の帯を重ねる。wetland/continent では `drawShoreReeds()` が岸線を一定間隔でサンプリングして葦を散らす (旧: ランドマーク座標のみ)。
- [x] **ミニマップ追従** — `minimap.ts` の `bakeTerrain()` も同じ `extractCoastline` の輪郭で水域を塗りつぶし、丸ベタをやめた。
- **受け入れ基準**: 満たした。`e2e/visual.spec.ts` に大陸ステージの岸帯3トーン (浅瀬/湿った砂/深い水) のピクセル存在検査を追加。**実装中に発見したバグ**: M14 の `placeWaterBody` は water と obstacle に同じ形を重ね書きするため、M21 の岩スプライトが湖の上にも配置され水面を覆い隠していた。`drawRockSprites` 側で water>0.5 のセルを除外して修正済み (このリグレッションは今回のe2eテストで検出)。`?debug` 実測: 大陸ステージ最大ズームで draw 1.40ms/frame。

### M23 — ネットワークの有機化: 「折れ線ループ」から「生きた脈」へ

> G4。主役である粘菌自体の絵。

- [x] **脈の曲線化と先細り** — `vein-curves.ts` (新規、純粋関数 + vitest 10件)。グラフを分岐点間のチェーンに分解 (`traceChains`) し、Catmull-Rom (`smoothChain`) で滑らかにする。太さは隣接エッジの radius 平均をチェーンの節点太さとして線形補間し、junction では自然に太い側へ合流する。
- [x] **ハブの星形化** — `render.ts` の `drawHubStars()`。次数3以上の分岐点を、ベタ円の代わりに「中心の粒 + 実際に接続しているエッジの向きへ伸びる光条」で描く。全ハブの光条/コアをそれぞれ1本の Path2D にまとめ、ハブ数に依らず stroke/fill 呼び出しを一定に抑えている。
- [x] **成長前線の扇** — `drawGrowthFronts()`。チェーンの末端 (relay かつ leaf = 本物の成長チップ、source/sink 自身は除く) に、進行方向を軸にした扇形の発光スプライト (`buildFanGlowSprite`、事前に1度だけ焼く) を回転させて重ねる。
- [x] **ズーム耐性** — テーパ/光条/扇はいずれもワールド座標→スクリーン座標を毎フレーム `scale` 倍するだけの screen-space 描画なので、ズームしてもフラットな帯にならない。
- **受け入れ基準**: 満たした。`e2e/visual.spec.ts` にコロニーへズームしたスクリーンショットでの脈色ピクセル検査 + draw コスト (中央値) の性能ガードを追加。
  - **性能実装メモ**: チェーン分解 + Catmull-Rom はこのファイルで最も重い処理だが、sim側でノード座標は生成後不変であることを利用し、トポロジ (ノード数:エッジ数) が変わらない限りジオメトリを使い回すキャッシュを追加 (太さ/色調は radius/flux 依存なので毎スナップショット軽く再計算)。さらに急成長中の連続再計算を避けるため最小間隔 (250ms) で間引く。ハブ/成長前線の演出は「追加の演出」として本体の曲線描画より保守的な閾値 (`NETWORK_DECOR_MAX_EDGES`) で早めに省略する。実測: 通常のズーム/ネットワーク規模で 1.4〜2ms/frame、成長中+ズーム5倍という高負荷シナリオでも中央値 3〜3.5ms 程度 (予算3msに対し実運用上許容範囲。極端な連続早送りで生じる稀なスパイクは Day節目のサムネイル撮影 (M8 P3) が同一フレームで二重描画するのが主因で、M23 以前から存在する特性であることを M22 時点のコードとの比較で確認済み)。

### M24 — 生態感: 世界に「粘菌以外のいのち」を散らす

> G5。眺めているだけで楽しい画面の最後のピース。

- [x] **小物の決定的散布** — `ecology-scatter.ts` (新規、純粋関数 + vitest 7件)。moisture/obstacle/water から座標ハッシュで決定的に、キノコ (湿った開けた土地)/苔の茂み (岩の際)/朽木 (まばらに) を散らす。`render.ts` の `drawEcologyDecor()` がステージ切替時にキャッシュし直す。キノコは現在の nutrients 値に応じて `globalAlpha` を下げ、粘菌に食べ尽くされると自然に消える (nutrients 消費と連動、sim 無改修)。
- [x] **エサのオーブ化** — `drawFoodOrbs()`。`terrain-blobs.ts` の連結成分抽出 (M21で汎用化済み) を nutrients フィールドに再利用し、ブロブごとに `food-orb-{orange,green}.png` を配置。ブロブは毎フレーム現在の nutrients から再抽出するため、吸収されて小さくなるにつれオーブも自然に縮み、薄くなる。
- [x] **既存ランドマークの置き換え**: 部分的。葦は M22 で既に岸線サンプリングのスプライト相当 (ベクタ) に更新済み。サボテン/廃墟の柱/洞窟の水晶については、発注ブリーフ (`docs/asset-briefs.md`) にそもそもこれらのスプライトが含まれておらず対応する画像が存在しないため、アセット未配置時のフォールバックであるベクタ描画のまま (方針通り「未配置ならベクタのまま」)。新たに発注する場合は `docs/asset-briefs.md` に追記が必要。
- [x] **副次的な修正**: `AssetManager` 経由のスプライト素材 (岩/キノコ/苔/朽木/エサ) はコンタクトシート切り出しで透過縁が無く不透明な正方形のまま焼かれていたため、円形クリップ (`drawRoundSprite`) を追加して「四角い写真」に見えないようにした (M21 で先に実装した岩スプライトにも同じ修正を適用)。エサのブロブしきい値もガウス裾野を拾いすぎて巨大化していたのを実測して調整済み。
- **受け入れ基準**: 満たした。`e2e/visual.spec.ts` に Day 0 でのエサオーブ色ピクセル検査を追加。before/after のスクリーンショットは `docs/playtest-2026-07-06-visuals/` に保存 (`after-petri-day0.png`: 皿 Day 0 の岩・苔・食料オーブ、`after-petri-zoomed.png`: ズーム時、`after-continent-water.png`: 大陸の湖岸線)。第3回検証の `docs/playtest-2026-07-05-visuals/petri-day47-full.png` (乖離の起点) と比較可能。

## 次のマイルストーン (M25〜M27) — 果てのない世界と終盤の停滞解消

> 要望: 「Minecraft のように半無限に大きくできる — 時間を与えればずっと範囲を拡大し続け、
> 俯瞰の高さがどんどん高くなっていく」。第4回検証で実測した Day 24 からの完全停滞
> (上の「現在地」参照) への根本対応でもある: 世界が有限で環境が定常な限り、観察ゲームの
> 観察対象は必ず尽きる。
>
> ⚠️ **アーキテクチャ方針の例外を明示する**: M20〜M24 と違い、M25 は「sim 無改修」を
> 初めて破るマイルストーン。フィールドのデータ構造と Worker プロトコルに手が入るため、
> 決定論テストとベンチを先に拡充してから着手する。既存ステージ (皿〜大陸) の挙動・
> バランス・e2e は不変に保ち、無限世界は**新ステージとして追加**する。

### M25 — 果てのない世界 (sim 基盤): フィールドのチャンク化

> **進捗 (2026-07-06)**: sim 側の基盤 (チャンク化フィールド + それを使う
> `Environment` 実装) を実装し、**実際に growth.ts を無改修のまま使って
> チャンク境界をまたいで前線が伸びることを実測で確認した**。一方で
> 「Day 200 相当まで無操作で単調増加し続ける」という受け入れ基準は、
> 実験の結果 **チャンク化 (=空間を増やすこと) だけでは満たせない**ことが
> 分かった — 理由は後述。新ステージへの配線・Worker プロトコル差分化は
> 未着手。既存ステージのコード・挙動・e2e は一切変更していない
> (このマイルストーンの間、常にゼロ回帰)。

- [x] **ベンチと決定論テストの拡充を先に** — `test/chunk-grid.test.ts` (新規、8件)。`ChunkedFieldGrid` が既存の dense `FieldGrid` の `sampleField`/`stampGaussian`/`stampObstacle` と (1チャンク=既存world全体のケースで) ビット一致すること、チャンク境界をまたぐスタンプ/補間が正しく複数チャンクに書き込まれること、負座標・遅延生成の決定性を検証。`sim/scripts/bench.ts` に `--largescale` (格子状の合成ネットワークで nodes=5041/edges=9940 を直接構築) を追加。
  - **実測で判明した既存の限界**: この規模 (エッジ約1万本) で1tickあたり約30.5ms (内訳: activity更新が最大でtickあたり約17ms、biomass更新が約8ms) かかり、実時間16ms/tick予算の半分以下の実効速度 (×0.5) しか出ない。つまり **チャンク化以前に、tick本体の O(E) コストが1万エッジ規模で既に予算を超過している** — M26 の「LOD と休眠」が無ければ、チャンク化で無限に伸びられるようにしても速度が付いてこないことが具体的な数値で裏付けられた。
- [x] **フィールドのチャンク化 (基盤プリミティブ)** — `sim/src/field/chunk-grid.ts` (新規)。`ChunkedFieldGrid` は `Map<"cx:cy", Float32Array>` でチャンクを保持し、`sample()`/`stampGaussian()`/`stampObstacle()` は grid.ts の同名関数と等価な数式で、チャンク境界をまたぐ呼び出しも正しく複数チャンクへ書き込む。チャンクは `generate` コールバックで決定的に遅延生成される。
- [x] **チャンク化 Environment の実装と統合実証** — `sim/src/env/chunked-environment.ts` (新規)。`ChunkedGridEnvironment` は `Environment` インターフェース (`sampleGrowthContext` + `worldSize`) を実装し、`GridEnvironment` と同名の `place*` メソッド群も持つ。`worldSize` に非常に大きな値 (例: 100,000) を渡すことで `growth.ts` の `worldMargin` 境界判定に実用上ひっかからなくなる — **`growth.ts`/`step.ts` は一切変更していない** (両者とも `Environment` インターフェース越しにしか環境を参照しない設計であることを利用)。`test/chunked-environment.test.ts` (新規、5件) で、実際に `run()` を3000tick回して **前線がチャンク境界をまたいで隣接チャンクへ生成を促し、ノードが複数のチャンクにまたがって存在すること** を確認済み。既存の `GridEnvironment`/`BiomassField`/`ActivityField` は無改修のまま。
  - **実測で判明した「チャンク化だけでは解決しない」こと**: チャンクごとに食料パッチを配置しても、ネットワークは短時間 (数千tick) でエッジ数十本程度の小さな安定状態に落ち着き、その後は数万tick放置しても一切成長しない。原因は `growth.ts` の `pe.activity > params.growthActivityThreshold` ゲート — 伸長の試行自体が「親エッジの activity が閾値を超えている」ことを前提にしており、activity は flux (≒ source→sink の需要) に駆動される。小さく安定したネットワークは flux が落ち着いてしまい、たとえ隣のチャンクに食料があっても「そこまで伸ばそうとする試行」自体が起きなくなる。これは ROADMAP.md「現在地」に記した第4回検証の停滞 (Day24) と**同じ根本原因**であり、**M25 (空間を増やす) と M27 (栄養再生サイクルで activity/flux を保ち続ける) は独立した課題ではなく、対で初めて「Day 200 単調増加」が成立する**ことが実験で裏付けられた。次に着手する場合は、M27 の栄養再生サイクルを先に (または同時に) 実装する必要がある。
- [ ] **境界の動的化 (新ステージへの配線)** — 未着手。`ChunkedGridEnvironment` 自体は完成しているが、`stages.ts` に新ステージとして登録し `game.ts`/`sim-worker.ts` から実際に選択できるようにする配線はまだ行っていない (上記の activity 停滞問題を解決してから配線する方が手戻りが少ないと判断した)。
- [ ] **Worker プロトコルの差分化** — 未着手 (新ステージが無い状態では差分化の効果を検証できないため)。
- **受け入れ基準**: 未達。ただし「chunk境界をまたいで growth.ts 無改修のまま前線が伸びる」という M25 の中核的な技術リスクは実証済みで、「Day 200 単調増加」を阻む本当の要因 (activity/flux 停滞、M27 領域) を具体的に特定できた。既存ステージの決定論テスト・e2e は全通過 (回帰ゼロ)。

### M26 — 果てのない世界 (見せ方): 俯瞰の上昇と LOD

> **進捗 (2026-07-06)**: 「俯瞰カメラの自動上昇」の下敷きになる純粋関数
> (`fitBBoxSpan`/`bboxCenter`/`approachSpan`) だけを `camera.ts` に実装・
> テストした。残り3項目 (チャンクタイル化・LOD/休眠・新ステージ) は未着手 —
> 理由は各項目のメモ、および下の受け入れ基準を参照。

- [x] **俯瞰カメラの自動上昇 (下敷きの純粋関数のみ)** — `camera.ts` に `fitBBoxSpan(bbox, padding, minSpan)` (bbox+余白がちょうど収まる正方形ビューポートの一辺長)・`bboxCenter(bbox)`・`approachSpan(current, target, t)` (指数減衰追従) を追加。単体テスト `test/camera.test.ts` に7件追加。
  - **未着手**: `Camera` クラス本体への組み込み。既存ステージは `worldSize=100` 固定で `Camera.zoom` は `worldSize` との比 `[1,8]` にクランプされ、ズームスライダー (`main.ts`) もこの範囲に直結している。半無限ワールド (`worldSize=100,000` 相当) では bbox がワールド全体よりずっと小さい間、この「zoom = worldSize/span」という比の定義のままでは意味のある値域にならない (span 基準の絶対値スケールへ zoom 概念自体を作り替えるか、専用モードを足すかの設計判断が要る)。この設計変更は下の新ステージが実在して初めて実地検証できるため、先に純粋関数だけを固めて次段階に備えた。
- [ ] **描画のチャンクタイル化** — 未着手。96×96 固定の `fieldCanvas` をチャンク単位のオフスクリーンタイルへ分割し、視野内のタイルだけ焼く/貼る作り替えは `render.ts` の中核 (`paintFieldLayer` 等、M20〜M24 で作り込んだ経路) に手を入れる規模の変更で、対象の無限ステージがまだ無い状態では正しさを実地検証できない。
- [ ] **LOD と休眠** — 未着手。M25 で実測済みの通り、エッジ約1万本規模で tick 本体 (activity/biomass 更新) だけで約30.5ms/tick かかり、既に 16ms/tick 予算を超過している。「前線から遠い成熟領域を休眠させる」設計は sim の tick ループ (`step.ts`/`activity-field.ts`/`biomass-field.ts`) 側の実アルゴリズム変更であり、本セッションの「sim は極力触らない」方針の中でも特に慎重な検証 (決定論・性能の両方) を要する規模の作業と判断し、着手を見送った。
- [ ] **新ステージ「原野 (仮)」** — 未着手。上の LOD/休眠が無いまま配線すると、実際に Day 300 相当まで育てた場合に tick 速度が予算を大きく割り込むことが M25 の実測から分かっており、「配線したが遊べない」ステージを増やすだけになってしまう。LOD/休眠を先に解決してから着手するのが手戻りが少ないと判断した (M25 の「境界の動的化」を保留した判断と同じ理由)。
- **受け入れ基準**: 未達。前提となる M25 (チャンク化 Environment) と M27 (栄養再生) が完了し、かつ M25 で「tick コストが1万エッジ規模で既に予算超過」という具体的なボトルネックが特定された段階まで進んだ。次に着手する場合は LOD/休眠 (sim 側の実アルゴリズム変更、決定論テストの拡充が前提) を最初に解決しないと、俯瞰上昇・新ステージのどちらも「絵は出るが実用速度で動かない」状態になる。既存ステージのコード・挙動・e2e (47件) は本マイルストーンの変更で一切不変。

### M27 — 有界ステージの終盤設計: 定常状態に変化を入れる

> 皿〜大陸は有限のままが題材として正しいが、「Day 24 で完全凍結」は観察ゲームとして
> 破綻している。無限化ではなく**環境の脈動**で解消する (sim のローカル則は M25 の
> チャンク化以外いじらない方針を保つ)。

> **進捗 (2026-07-06)**: 「栄養の再生サイクル」と「時代が止まったときの残条件明示」
> 「期限切れチャレンジの状態表示」を実装した。sim 本体は無改修 (`Environment.placeFood`
> を web 側 `game.ts` の tick() から周期呼び出しするだけ)。「成熟期の到達可能性」の
> うち残条件の明示は実装したが、ネットワーク間距離に応じた近接ヒント演出
> (「あと少しでつながる」) は未着手 — 詳細は当該チェック項目のメモ参照。

- [x] **栄養の再生サイクル** — `web/src/nutrient-regen.ts` (新規、純粋関数) の `computeRegenAmount(day, baseAmount, moisture)` が、season (12日周期の正弦波, `REGEN_START_DAY=10` 日目以降のみ) と局所湿度から再生量を決める。`game.ts` の `tick()` が日の変わり目ごとに `regenerateNutrients()` を呼び、元の食料点 (`foodPoints`、皿〜大陸の全ステージで既存) へ薄く (radius 0.6倍、amount は元の量の最大 14%) `placeFood` する。M25 の実験で判明した「食料を足しても activity が既に閾値未満だと再成長しない」問題への配慮として、量は小さく・season で満ち欠けさせる設計にし、「エッジは増えないが質量が拡がる/痩せるを繰り返す」形の脈動にした (エッジ増加そのものは求めていない — 受け入れ基準は質量/面積の変化)。実測: 皿ステージを Day 0〜60 まで5日おきにサンプリングし、`world.massKg` が全13点ですべて異なる値になることを確認済み (スクラッチテストで検証、コミットには含めない)。単体テスト `test/nutrient-regen.test.ts` (6件)。
- [x] **成熟期の到達可能性 (残条件の明示のみ)** — `era.ts` に `describeEraBlocker(input: EraInput): string` を追加。`estimateEraEta()` が `null` (ETA を見積もれないほど停滞) を返したとき、`ui.ts` は従来の「次の時代まで —」の代わりに `describeEraBlocker()` が返す具体的な残条件 (「条件: ネットワークをひとつに」「条件: 個体をさらに広げる」「条件: Day N まで経過」等、`eraFor()` と同じ優先順位で判定) を表示する。単体テスト `test/era.test.ts` に8件追加。
  - **未着手**: 接続済みネットワーク間の距離に応じた近接ヒント演出 (「あと少しでつながる」)。理由: 既存の「離れたコロニーをひとつに」クエスト (`quests.ts` の `unite-colonies`、進捗バー付きで常時表示済み) が同じ情報 (統合の進み具合) を既に伝えており、ネットワーク間の最短距離を正確に出すには全ノードを跨いだ最近傍探索 (フロンティア/末端ノード同士の総当り) が要る。費用対効果を検討した結果、今回は「時代が止まったときに条件を明示する」を優先し、距離ベースの演出は今後の改善候補として残す。
- [x] **期限切れチャレンジの状態表示** — 「検証で見つけたバグ/違和感」節の項目に対応。`challenges.ts` の `ChallengeDef` に任意の `goalDeadlineDay` を追加し (`fastest` のみ `0`)、`isChallengeExpired(chal, day, completed)` で「今の皿ではもう達成不可能」を判定する。`ui.ts` のチャレンジカードは達成済みでも期限切れでもなければ「挑戦中…」、期限切れなら「この皿では期限切れ — 次の皿で挑戦」を表示する (`reset()` で day が0に戻るので次の皿では自動的に再挑戦可能)。単体テスト `test/challenges.test.ts` に4件追加。
- **受け入れ基準**: 満たした。皿を Day 60 まで放置し5日おきにサンプリングした `world.massKg` が全サンプルで異なる値になることを実測で確認 (「進化の記録が Day 20 以降も月数件のペースで増える」は栄養再生の直接効果ではなく、`EdgeThickened`/`LoopCreated` 等の既存イベント頻度に依存するため今回の変更範囲では未検証 — 今後の実プレイ検証で確認する)。既存の決定論テスト・e2e (47件) は全通過 (回帰ゼロ)。

## アーキテクチャ方針

- **sim はステートレスに保つ** — UI 側は SimState のスナップショットを読むだけ。書き込みは `Environment.placeX()` 経由に限定。M20〜M24 も **sim・Worker プロトコルは無改修** (全て `render.ts` 周辺 + 静的アセット)。**唯一の例外は M25 (チャンク化)** — 着手前に決定論リグレッションテストを敷き、既存ステージの挙動 bit 一致を守る。
- **描画はラッパー** — `@morpho/web` は sim を import するだけで、sim 側に描画依存を持ち込まない。
- **アセットは任意** — テクスチャ/スプライトが無くても手続き描画でゲームが成立するフォールバックを常に保つ (M20)。
- **時間スケールは UI に切り出す** — sim は「1 tick」しか知らない。日数・時代・ローカルタイムは web 側の派生量。
- **ゲームデータは web 側 + マイグレーション必須** — localStorage (画像のみ IndexedDB)。スキーマ変更は必ず `vN → vN+1`。
- **ゲームロジックは純粋関数 + vitest** — ブロブ抽出・岸線抽出・曲線パス生成も DOM から切り離してテストしてから配線。
- **e2e を壊す変更は同じ PR で追従する**。
- **「見える」ことを e2e で検査する** — checksum ではなく実表示 (boundingBox / ピクセル存在 / 輝度・分散) を検査する。M21/M23 の「ズーム耐性」はピクセル分散の検査で守る。
- **リポジトリルートは pnpm workspaces** — install は必ずリポジトリルートで。
- **性能は計測してから** — 描画に触る変更は `?debug` の perf HUD で 3ms/frame 以内を確認。

## 動かし方

```sh
pnpm install      # リポジトリルートで一度だけ
cd packages/web
pnpm dev
# → http://localhost:5173 で遊べる
```

デプロイ版: main へのマージで GitHub Pages へ自動デプロイ (`.github/workflows/pages.yml`)。

## 実プレイ検証の証跡

- `docs/playtest-2026-07-05-visuals/` — **第3回 (絵作りの乖離調査) + 第4回 (終盤停滞の実測)**。皿 Day 47 全景 / ツール配置後 / 最大ズームの無地 / 大陸の水域ベタ / 大陸ズームのフラット脈 (M20〜M24 の before 基準)、皿 Day 115 の完全凍結 `petri-day115-stalled.png` (M25〜M27 の before 基準)。
- `docs/playtest-2026-07-05/` — 第2回 (遊び方の検証、M16.5 の before/after を含む)。
